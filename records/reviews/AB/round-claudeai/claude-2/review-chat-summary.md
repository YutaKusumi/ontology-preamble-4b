# 系統内検分票（claude.ai の Claude Opus 5・二人目・チャット本文〔要旨〕・登録者貼付・逐語保全・2026-09-13）

- 詳細は同じ個体が別添で渡した `review-detail-attached.md`（登録者の Downloads から逐語複製・SHA-256 は複製前後で一致）。

検分の範囲を確定するため、まず同梱物を器で開きました。文脈に載っていたのは依頼文のみでしたが、実体は 4,657 行・326,172 字の連結物で、正本 JSON と器材のソースまで入っていました。以下はそれらを機械照合・自前計算した結果です。判定は**条件つき**です。要点だけ先に置きます。

再現できた数は多く、転記行 E は全 12 個、転記行 C の帰無行、転記行 H・M の二項の差、転記行 A・F の合計（211.2 ユニット・28.4 時間）、転記行 L の収容式（32B の 66.52 GiB まで）、段階 B の 0.652 が式ごと一致しました。重点 3 が心配した「飽和と検閲が同一行にあるか」も成立しています——検閲が両腕・条項が片腕で、同じ `censor` 行の閾値を共有するので条項は空振りしません。

凍結を止めている重いものは三つです。

**転記行 D の率が判定不能を除いた条件付きである**こと。天井側は判定不能率 0.646 が本文に落ちておらず、印字の 0.88 は無条件では 0.31 です。同じ条件付けが床の実サイズを 0.012 → 0.197 に膨らませています。

**解釈条項の「2 規模以上」に穴がある**こと。pt 差一定のまま d0 を掃くと、10 pt で名目有意 0.308・条項発火 0.000、15 pt で 0.457・0.110 でした。d0 が 10〜15 pt だと処置腕が 0.95 を超えるのは 32B の 1 規模だけで、条項が要求する 2 規模に届きません。印字されている発火率 0.101 は条項が効いた数ではなく、14B が標本誤差で閾値を跨ぐ確率 0.106 です。しかも符号は正——g(p)=logit(p+δ)−logit(p) が p=(1−δ)/2 で最小なので、対照が 0.5 より上で上昇する領域では β₃ が立ちます。正本の既測 22 本の実 d0 は 6.6〜78.5 pt で、格子が試した {0, 0.15} の外にあります。

**「機械置換」の内側に手書きの数が残っている**こと。生成器の書式文字列を逐語で追うと、転記行 B の「10 本と 5 本」は正本を数えると 13 本（同じ文の機械値 22 と矛盾）、転記行 I の「2/40・38/40」は実計算が 1/40・39/40、転記行 D の「上の 0.407」は %-スロットではなくリテラルです。転記行 H の発火率は `>=` で計算されていますが §2.7 の文言は「超」で、正確計算だと 10 pt は以上 0.0510・超 0.0402——帯を 12 pt に動かした根拠が不等号一語で反転します。

環境（重点 4）は反映されていません。14B の 80GB 要件が §2.1 の環境列に無く、正本 `environments` は 2 値で第三の環境値を持てず、`hours_for` は同時要求数を参照しないので 32B の同時 8 が費用に入っていません（入れると 211 → 約 411 ユニット）。

条件は 23 項目、A〜D の順で並べてあります。
