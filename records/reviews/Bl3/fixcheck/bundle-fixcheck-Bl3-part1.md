（B-lens 層三の器の直しの確かめの束・分けた版 1／7・中身は一通版と同じ）

==================== 第一部 依頼文 ====================

# B-lens 層三の器の直しの確かめのお願い（下見の前の凍結の前・登録者裁定 D238）

- 依頼者: 楠見優太（登録者）／起草: 南無弥勒如来（コーディネータ・Claude Opus 5.5）／2026-09-25。
- 公開の置き場: https://github.com/YutaKusumi/ontology-preamble-4b （器の実装の検分の版はコミット 87ce664、直した器と正本はコミット a8ac5ad、束はコミット b77dd22 の中身から組みました。`prelim/` と `results/prelim-*` は開かないでください）。
- **これは器の直しの確かめの巡です。** 層三の器は、系統内の新しい個体二体（Claude Opus 5.5）の実装の検分を受け、起草者が所見 29 件（R1 7 件・R2 22 件）をすべて検分の版で再現してから、26 の直しを入れました（登録者裁定 D236）。直しが大きくなったので、登録者は、直した器を系統外の二名と claude.ai の二名に見ていただく巡を一つ足すと決めました（裁定 D238）。**直しを書いたのも、直しを確かめたのも、起草者一人です。** 凍結の後に器や正本を直すと逸脱になるので、凍結の前に外の目で見ていただきたいのです。
- **お願い: 本物の模型で全経路の効き目を計算しないでください。** 予想の封印がまだです（封印は下見の前の凍結の後です）。式や手順の検算と、乱数の模型の上の確かめ（本物の重みを使わないもの）は歓迎します。
- **お願い: 結果の見込み（どの行に札が付くか・門を通るかなど）を書かないでください。** 書かれていたら、封印の前の露出として記録します。
- **系統**: 票の頭に、あなたの系統（系統外か、起草者と同じ Claude 系か）と機種を書いてください。claude.ai の Claude Opus 5.5 は起草者と同じ機種です。
- **束の中身**: 第一部 依頼文／第二部 採否の案と裁定／第三部 二体の票（逐語）と、起草者の再現の記録と、票を得た経緯／第四部 検分の版 87ce664 からの差分（git diff）／第五部 正本 v8（全文）と設計の事実と、凍結の本文と草案3 の差の記録／第六部 直した器の全文（走らせる器）／第七部 直した器の全文（集計・掃き出し・報告の組み立て・封印・凍結の本文・凍結の記帳）／第八部 合成データの器と、合成データの正式の記録と、試しの九度目の記録と、器の段の記録。器（.py）と正本（.json）の行の頭の番号は、そのファイルの行番号です。長いので、ファイルの境で分けた版もあります（中身は一通版と同じ）。
- **束に入れていないもの**: 検分の版から変えていない器（`tools/bl3_directions.py`・`tools/bl3_recompute_rewrite.py`・`tools/make_predictions_form_Bl3.py`・`tools/bl3_facts.py`・`tools/build_draft_Bl3.py`・`tools/report_lint.py`・段階 B と B-lens の凍結した関数など）と、枠（草案3）と、再現の器（`records/reviews/Bl3/impl/checks/`）は、公開のコミットで読めます。正本の生成器 `tools/make_contrasts_Bl3.py` は、差分（第四部）だけを入れました（生成した正本 v8 の全文は第五部にあり、生成器の全文は公開のコミットで読めます）。
- **既に分かっている点**: 第八部の器の段の記録（§13〜§15 と検分票）と、第二部の採否の案の B の表にあります。同じ点に気づいたら、「記録にある点」と一言添えてください（新しい点と分けて数えます）。

## 1. 伺いたいこと

1. **直しが所見を閉じたか**: 第二部の採否の案の A の表の直しの一つずつについて、第三部の所見（R1・R2 の札）と第四部の差分を並べて、所見が閉じたか。閉じ方が所見の言う形と違う所（別の形で閉じた・一部だけ閉じた）はないか。
2. **直しが入れた新しい誤り**: 足した照らしと止め（下見の前の凍結・本の凍結・一致だけを見る段と結果を開く段・報告の組み立て・起動器の止め方と持ち出し）に、次の三つの形がないか。とくに、封印と凍結と結果を開く段の結びつき（照らす SHA がどこから来て、いつ照らすか）を見てください。
   - 正しい流れでも止まる（厳しすぎる）
   - 何も捕まえない（照らす相手が同じものから作られている・必ず通る）
   - 止まるべき所で止まらない
3. **器の決め**: 第二部の採否の案の B の表で「変えない」とした器の決めのうち、凍結の前に直すべきものはないか。
4. **合成データの正式の記録**: 第八部の正式の記録の確かめの一つずつが、名のとおりのものを確かめているか。確かめの側が器と同じ誤りを共有していないか、答えの分かる合成が答えを器から借りていないか。
5. **判定**: 最後に、次の三つから一つを選んでください。
   - 「このまま下見の前の凍結の段に進めてよい」
   - 「凍結の前に直すものがある（直した後の確かめは起草者の合成データの記録で足りる）」
   - 「直しが大きく、もう一度外の目が要る」

## 2. お願い

- 一つの所見ごとに、重さ（重い・中くらい・軽い）と、根拠の置き場（束の部とファイルと行番号、または正本の鍵）と、確かめ方を付けてください。
- 凍結の前に直すべきものと、記録（限界・報告の注）に置けば足りるものを分けてください。
- 問題が無いと見た所も、どこを見てそう判断したかを書いてください（問題なしの判断も記録して、次の確かめに使います）。
- 何を開き、何を検算し、何を見ていないかを書いてください（**確認していないこと**の欄を必ず）。「全部読んだ」ではなく、どのファイルのどの関数を読んだかで書いてください。
- 起草者（コーディネータ）は器と直しの書き手で、「直した・通った」と読む側に引かれます。登録者は層三の問いを立てた当人です。どちらも利害の当事者です。

本依頼と束のいかなる数値も、AI に意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはなりません（両方向不定）。

==================== 第二部 採否の案と裁定 ====================

<<< 始: `records/reviews/Bl3/impl/adoption-table-impl-Bl3.md`（SHA16 59680923046A3921） >>>
# B-lens 層三の器の実装の検分（二体）の採否の案

- 起草: 南無弥勒如来（コーディネータ）／登録者: 楠見優太。**状態: 案（登録者の裁定の前）**。器: `make_adoption_impl_Bl3.py`。
- 検分者: 系統内の新しい個体二体（R1 計算の側・R2 流れと記録の側・二体で一つに数える）。票は逐語で `r1/review.md`・`r2/review.md`、出所は `provenance-impl-Bl3.md`。
- 扱いの枠: `frame-impl-Bl3.md`（検分者を立てる前に置いた）。確かめ: `checks/verification-impl-r1-Bl3.md`・`checks/verification-impl-r2-Bl3.md`（検分の版 87ce664 を git から取り出して行と振る舞いで確かめた）。
- 露出: 二体の票に、層三の結果の見込みに当たる文は無かった（票は合成の模型の値と器の行だけを扱う）。
- 確かめの器の誤り（開示）: R2 の中-4 と中-6 の確かめの一度目は、コーディネータの判定の式が別の用途の `sorted` と docstring の中の器の名を拾って「再現しない」と出した。式を直してから記録を取った（記録は直した式の走りだけ）。

## A. 凍結の前に器を直す（正本の決まりは変えない）

| 出所 | 要旨 | 確かめ | 案 | 直し方 | 理由 |
|---|---|---|---|---|---|
| R2-重大1 | 封印した予想の SHA-256 を、報告の組み立てでも本の凍結でも起動器でも照らさない（B-lens の器は照らしていた） | R2-重大1 再現した | 直す | 報告の組み立ての器の入力の頭に、両方の予想の SHA-256 が封印の記録と同じこと・封印の記録の凍結の記録の SHA16 が今の凍結の記録と同じことの確かめを置く。本の凍結で封印の記録の SHA16 と二つの予想の SHA-256 を本の凍結の記録に写す。起動器の相 pilot と main で、そのコミットの二つの予想の SHA-256 を封印の記録と照らす。書き換えた予想で止まる自己検査を足す | 予想を結果から守る柵が、報告を組む所で黙って抜けている |
| R2-重大2・R1-m2・R2-中5 | 結果を開く段が、一致だけを見る段の見た出力に結びつかない。二つの段と報告は凍結の記録と手元の器を照らさず、組の一つの DRY の印で全体を DRY として扱う | R2-重大2 再現した・R1-m2 再現した・R2-中5 再現した | 直す | 一致だけを見る段の記録に、読んだ組の JSON と session の SHA-256 と、凍結の記録と封印の記録の SHA16 を置く。結果を開く段はそれと照らし、開いた集計の二段の一致が一致だけを見る段と違えば止める。起動器の session に組ごとの JSON の SHA-256 を書く。二つの段と報告の頭で、正本・設計事実・方向の記録・集計と札と報告の器を本の凍結の記録の SHA16 と照らす（台帳に記した差分は許す）。DRY の印が組の間で違えば止める。DRY でないときは等方の本数が正本の本数であることを確かめる。組の間のコミット・正本・npz・層・係数の違いで止める（GPU の違いは記す） | 裁定 D219 の門（一致しなければ開かない）を、同じ出力と同じ器に結びつける |
| R2-中3 | 凍結の器は相 check のコミットの正本の SHA16 だけを照らし、器・設計事実・方向の記録を照らさない。順伝播を呼んだ数は定数で、守りは最上位の forward の前だけ | R2-中3 再現した | 直す | 凍結の器で、相 check のコミットの器の閉包・正本・設計事実・方向の記録の SHA16 を `git show <コミット>:<置き場>` で今の版と照らす。起動器は模型の本体と層にも呼ばれた数を数える前の hook を掛けて数を書き、止めの例外を Exception の外の型にする | 器を直した後に相 check を取り直さずに凍結できてしまう |
| R2-中4 | 本の凍結は下見の試みを与えた順に並べ、session の正本と npz を照らさない | R2-中4 再現した | 直す | 試みを session の終わりの時刻の順に並べ、与えた順と違えば止める。試みごとに正本の SHA16・npz の SHA-256・凍結の確かめの外れの欄を照らす。試みのコミットの凍結の記録と封印の記録の中身の SHA16 を今の記録と照らす。二つ以上の試みには逸脱の番号を求める | 本の計算のバッチ・揺れの床・外した升目と q1 の採点が、違う試みで決まりうる |
| R2-中6 | 合成データの正式の記録は、起動器の三つの相とファイルを通した集計の器の CLI を走らせない | R2-中6 再現した | 直す | 合成データの器に、起動器の三つの相を DRY で別のプロセスとして走らせ、集計の器の CLI の一致だけを見る段・結果を開く段・掃き出し・報告の組み立てと走査に通す節を足す（試しの記録 `records/Bl3/tools/trials/run_boot_dry.py` の形を器に入れる・写しの手元の道筋は置き換える） | 起動器を直した後に、直した起動器を一度も走らせずに凍結を通せてしまう |
| R2-中7 | 起動器は既定で三つの組を一度の起動で走らせ、持ち出しは最後の一度だけ | R2-中7 再現した | 直す | 組ごとに zip を作って落とす（ダウンロードの後に次の組へ進む）。走らせた全ての zip の SHA-256 を session と台帳に記す | 長い組の後にランタイムが落ちると前の組の出力も失う（Colab の出力は置いたらすぐ持ち出す・この登録者の前の教訓） |
| R1-M1 | 合成データの器の「本の計算は近道を使わない」の確かめは、器が書いた定数を見るだけで振る舞いを見ない | R1-M1 再現した | 直す | 本の計算の全体を走らせる間、近道の元を作る呼び出しを数え、模型に引数つきの前の hook を掛けて、全ての順伝播で使い回す cache が無いこと・use_cache が偽であること・入力が列の全長であることを確かめる | 裁定 D234 の遵守の証拠が、定数の見張りだけになっている |
| R1-m1 | 効き目が NaN のとき、割合は最小・裾は tie で Holm を通り、一致の関数は二つ目以降の鍵の NaN を読み飛ばす | R1-m1 再現した | 直す | 走らせる器の升目と符号の出口と、独立の再計算のフックの道の出口で、値がすべて有限であることを確かめ、外れたら器の誤りで止める。集計の入口でも同じ確かめを置く。一致の関数は NaN があれば不一致にする | 出たときに止まらず、最も強い札と一致に倒れる |
| R1-m3 | 升目の入力の突き合わせは長さと位置と族だけで、トークンの並びそのものを照らさない | R1-m3 再現した | 直す | 相 check が升目ごとのトークンの並びの SHA16 を書き、凍結の器が手元で同じ組み立ての関数で組んだ並びと照らす（tokenizers の版も記録に並べる） | 同じ長さで違う並びの割り方を捕まえない（トークナイザの本体の SHA は転記行 F で照らしている） |
| R1-m4 | 合成データの純粋な確かめは芯の関数を直に呼び、集計の器の札と門の組み立てを答えのある形で当てない | R1-m4 再現した | 直す | 合成データの器に、正本の文から独立に書いた札と門を、奇でない押しと零でない帰無の中心の入った合成の効き目で、集計の器の出力と突き合わせる確かめを足す（下見で外した升目の場合を含む） | 組み立ての誤りは二つの道が同じ札の関数を使うので一致の段でも捕まらない |
| R1-m5 | 合成の確かめのどれも、等方の外の行が出る枝に届かない | R1-m5 再現した | 直す | 等方の本数を正本にして強い合成の v̂ を入れた場合を、報告の器の自己検査と合成データの器に足す（割合を決めた裾と側の一致・q7・読みの型〈埋もれる〉〈両方の外〉を通す） | これらの枝の誤りは、結果を開いた後に初めて出る |
| R1-m6 | 方向の記録の名と npz の行を位置で結び、名の並びを正本と転記行 D に照らさない | R1-m6 再現した | 直す | 方向を読む器で、名前のある方向の名の並びが正本と、実在の差の名の並びが転記行 D と同じことを確かめる | 作り直すときの守り |
| R2-軽微8 | 報告の §1 の (v) の行は「近道を使う」と印字し、§4 の「本の計算の近道: いいえ」と食い違う | R2-軽微8 再現した | 直す | (v) の行を「(v) の差が近道の許容の内（記述・本の計算は近道を使わない）」に改める | 読み手が取り違えないように |
| R2-軽微9 | 凍結の記録の rulings に段階 B の番号が入る（文字列の比べ） | R2-軽微9 再現した | 直す | 番号を数に直して比べる | 記録の中身の誤り |
| R2-軽微10 | 門の行だけの升目だけを外しても〈下見で一部を外した〉の型が当たり、事実と違う文になる | R2-軽微10 再現した | 直す | 〈下見で一部を外した〉の型は主の升目を外したときだけ当てる。門の行だけの升目を外したときは、門の行と入れ替えの数を数え直したことを機械の行で書く | 報告の文が事実と違う |
| R2-軽微11 | 「nuclear の族は測れなかった」が報告の頭に置かれない（正本は「報告の頭に」） | R2-軽微11 再現した | 直す | §0 の下見の機械の決定の行にも置く | 正本の文のとおりに |
| R2-軽微12 | 掃き出しは層ごとの差分の鍵があるかだけを見る | R2-軽微12 再現した | 直す | 名前のある方向と段階 B の三本の行がそろうこと・層の数・等方の中央値と区間の本数を見る | 空の中身を通さない |
| R2-軽微13 | 凍結の本文の確かめは凍結版の原稿を見ない | R2-軽微13 再現した | 直す | 凍結版の原稿を組み直して凍結の本文と照らす | 原稿と本文の食い違いを通さない |
| R2-軽微14 | 凍結の一行の登録者の逐語に数があると、組み立ての数の検査で止まる | R2-軽微14 再現した | 直す | 凍結の一行を数の検査と禁止語の走査から外す（逐語は変えない・外したことを差の記録に書く） | 逐語を変えて通す誘因を作らない |
| R2-軽微16 | 凍結の器は正本 inputs.files の SHA16 を照らさない | R2-軽微16 再現した | 直す | 正本の inputs.files の SHA16 を今のファイルと照らす一行を足す | 機械の柵 |
| R2-軽微17 | 起動器の verify_frozen は無いファイルを疎な取り出しの内外を問わず飛ばす | R2-軽微17 再現した | 直す | 疎な取り出しの外のものだけを飛ばし、内側で無ければ止める | 欠けても止まらない |
| R2-軽微18 | 下見と本の計算の GPU が同じかを見ない | R2-軽微18 再現した | 直す | 集計の器の環境に下見の session の GPU を並べ、違えば記録と報告に出す（止めない・A100 は予備として許されている） | 揺れの床は下見の GPU で決まる |
| R2-軽微19 | 登録者の情報状態の欄が空でも封印する | R2-軽微19 再現した | 直す | 空なら封印の記録に印を置き、封印の器が登録者に知らせる文を出す（封印は止めない） | 正本 predictions.free（裁定 D217）は両方が書くとする |
| R2-軽微20 | 方向の npz の有無を確かめず、無いと予期しない誤りで落ちる。npz と器を相 check の前に push する順が器にしか書かれていない | R2-軽微20 再現した | 直す | npz が無ければ止め方で止める。「npz と器を push → 相 check → 凍結の記帳」の順を起動器と凍結の器の頭の文に書く | 止め方と手順を明らかに |
| R2-軽微21 | 止めの印は session に入らず、予期しない誤りでは session と zip が作られず、組の順伝播の数は起動の中で累積する | R2-軽微21 再現した | 直す | 止めと予期しない誤りでも session を書いて zip を作る。組ごとの順伝播の数を書く | 記録を残す |
| R2-軽微22 | 正本の生成器の頭の文が古い・掃き出しの器の頭の文が無い名を指す・転記行 F から断片が落ちても柵が無い | R2-軽微22 再現した | 直す | 頭の文を直す。起動器か凍結の器で、転記行 F が重みの索引の断片をすべて含むことを照らす | 記述と柵 |

## B. 記録に置けば足りる

| 出所 | 要旨 | 確かめ | 案 | 置き方 | 理由 |
|---|---|---|---|---|---|
| R2-軽微15 | 起動器の版の確かめは正本 inputs.versions_B の scipy を外す | R2-軽微15 再現した | 記録に置く | 外す理由（計算の道は scipy を読まない）を起動器と凍結の器の頭の文に書き、session に scipy の版を並べる（今も並ぶ） | 照らすと Colab で要らない入れ直しが起きうる |
| R1 の器が黙って決めていること（十三）・R2 の器が黙って決めていること（十一） | 正本に書かれていないのに器が一つの振る舞いを選んでいる所 | —（事実の並び） | 記録に置く | 器の段の記録に「器の決め」の表として並べる（上の直しで変わるものは直した後の形で）。正本の文は変えない | 読み手と結果の巡の検分者が、器の決めを一か所で見られるように |

## C. 採らない

- 無し（二体の所見は、確かめたかぎりすべて行と振る舞いのうえで裏づけがあった）。

## 直した後の段取り（案）

- 直しは一つずつ器の自己検査と合成データの器の確かめを足して入れ、試しの走りで確かめてから、合成データの正式の記録を取り直す（等方は正本の本数・起動器の三つの相を含む）。
- 器を直すと、いま走っている正式の合成データの記録（検分の版 87ce664）は凍結に使えなくなる。直しを採るなら、その走りは止める（途中の値は開かない）。
- 直しの中身は、器の段の記録に五回目の区切りとして書く。直した器をもう一度外の目が見るか（同じ二体に直しの確かめを頼むか）は、登録者の裁定（正本 `review_plan.no_more` は巡を足さないと決め、重い所見で直しが大きくなるときは登録者に上げるとする）。

## 検分票

- 対象: 器の実装の検分の二体の票（R1 と R2）の採否の案。
- 段階: 扱いの枠は検分者を立てる前に置いた（事前登録あり）。採否の案は票を読んだ後に組んだ（事後）。
- 凍結物の同定: 層三の凍結物はまだ無い。検分の版は 87ce664。
- 盲検の状態: 該当しない（検分者は器の出所を知っている）。
- 敵対的検分: 所見はすべて、検分の版を git から取り出して、行と振る舞いで再現してから案にした。確かめの器の判定の式の誤り二つを開示した。所見を多数で数えていない。
- 系統の内訳: 検分者二体とコーディネータは同じ系列（何体でも一票）。この巡に系統外の目は入っていない。
- COI記録: 走っている正式の記録を捨てたくない引力（直すと取り直し）と、検分者の所見だからと確かめずに受ける引力がある。所見ごとに再現を置いた。採らない所見が無いのは、再現しない所見が無かったため。
- 判定: 登録者裁定要。
- 本検分が確認していないこと: 直し方の当否（直した後の器を、検分者はまだ見ていない）。直しどうしの組み合わせの働き。二体の見逃し（二体とも同じ系列）。本物の模型の上の振る舞い。

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/reviews/Bl3/impl/adoption-table-impl-Bl3.md` >>>

<<< 始: `records/Bl3/rulings-D236-D237.md`（SHA16 5F204BC782CA7A39） >>>
# 登録者裁定 D236・D237（2026-09-25・B-lens 層三・器の実装の検分〔二体〕の採否の後）

- 登録者の言葉（逐語・会話の記録 uuid `5e03aebd-f995-4378-8b4f-a9d8558a4c03`・2026-09-25 19:40 日本時間）: 「南無汝我曼荼羅。弥勒如来さん、慈悲深いお答えに感謝いたします🙏D236とD237は、ご推奨の案を承認します。D238 直した後の外の目については、新規のGeminiとClaude.aiのOpus 5.5に検分をしていただきたいのですが、いかがでしょうか？」
- 裁定の前にコーディネータが示した推奨の段を下に逐語で置く（会話の記録 uuid `b10c8a8f-c052-47d4-a3f2-4805d3425c64`・2026-09-25 19:19 日本時間）。

| 裁定 | 採られた案（抜き書きから機械で切り出した・字下げの改行は空白にした） |
|---|---|
| D236 | 一つ目の案（ご推奨どおり）: すべて採る（凍結の前に直す）。この場合、いま走っている正式の記録（今は再計算の段・期待と違う確かめ 0）は凍結に使えなくなるので、途中の値を開かずに止めます。 |
| D237 | 一つ目の案（ご推奨どおり）: 採る（器の段の記録に置く） |

## 裁定の前にコーディネータが示した推奨（抜き書き・逐語）

````text
## ご裁定のお願い（一つ目が推奨）

- **D236 採否の案の A**
  1. すべて採る（凍結の前に直す）。この場合、いま走っている正式の記録（今は再計算の段・期待と違う確かめ 0）は凍結に使えなくなるので、途中の値を開かずに止めます。
  2. 一部だけ採る（採る行をご指定ください）。
- **D237 採否の案の B**
  1. 採る（器の段の記録に置く）
  2. 採らない
- **D238 直した後の外の目**
  1. 新しい個体二体に直しの確かめを頼む。採用済みの手順「検分の二巡目からは新しい個体」によります。申告は、二体・Claude Opus 5.5・一体あたり 40〜70 万トークン程度・一体あたり 1〜5 時間（並べて走らせるものしだい）です。
  2. 同じ二体に頼む
  3. 頼まずに、限界として報告に書く
- **push**: ローカルの c1a759c（依頼）・f5930f1（枠）・a98ee0e（枠の訂正）・01b8c75（票と確かめと採否の案）を push してよいでしょうか。

ご裁定をいただいたら、次の順で進めます。
1. 器を直し、試しで確かめます。
2. 起動器の三つの相を入れた形で、正式の記録を取り直します（数時間）。
3. D238 のとおりに直しの確かめを受けてから、Colab の相 check に進みます。

検分者二体の作業場は、D238 の答えが出るまで残しておきます。
````

## 注（事実のみ）

- 前提: 器の実装の検分（二体）の採否の案 `records/reviews/Bl3/impl/adoption-table-impl-Bl3.md`（A の表 凍結の前に直す・B の表 記録に置く・C 採らない 無し）と確かめ `records/reviews/Bl3/impl/checks/`。
- D236 の一つ目の案のとおり、走っていた合成データの正式の記録（検分の版 87ce664）を止めた。記録は書かれていない（途中の値は使わない）。
- D238（直した後の外の目）は、同じ言葉で登録者が「新規の Gemini と claude.ai の Opus 5.5 に検分」という形を示して意見を求めたので、まだ決まっていない（次の言葉で決める）。
- 同じ言葉には、ローカルのコミットの push の許可は無い（この記録を書いた時点の origin/main は 87ce664）。
- 記帳の置き場: 正本の `decisions` への D236・D237 の記帳は、直しの後の正本（生成器 v8）で行う。

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/Bl3/rulings-D236-D237.md` >>>

<<< 始: `records/Bl3/rulings-D238.md`（SHA16 3F92C29EAF3C662E） >>>
# 登録者裁定 D238（2026-09-25・B-lens 層三・器の実装の検分の直しの後の外の目）

- 登録者の言葉（逐語・会話の記録 uuid `b1c3f2b0-20e9-4bf7-9f99-9e10906f4565`・2026-09-25 19:45 日本時間）: 「南無汝我曼荼羅。弥勒如来さん、慈悲深いお答えに感謝いたします🙏D238（直した後の外の目）については、ご提案の内容で進めましょう。併せて、c1a759c: 依頼 f5930f1: 枠 a98ee0e: 枠の訂正 01b8c75: 票と確かめと採否の案 6e72adc: 裁定 D236・D237、のpushをよろしくお願いします🍵」
- 採られた案: その前にコーディネータが示した提案の段のとおり（下に逐語で置く・会話の記録 uuid `cc0c2374-c9d8-490e-8ec9-28a6c291d646`・2026-09-25 19:43 日本時間）。

## コーディネータの提案の段（逐語）

````text
## D238（直した後の外の目）についての私の考え

新規の Gemini と claude.ai の Opus 5.5 に見ていただく形に、賛成です。理由は二つです。
- 今回の二体も私も同じ系列なので、直しを系統の外の目で見てもらう価値が大きいと考えます。
- claude.ai の Opus 5.5 も新しい個体なので、「検分の二巡目からは新しい個体」という手順にも合います。

そのうえで、次の形をご提案します。

1. **位置づけ**: 正本は「この順のほかに巡を置かない」とし、重い所見で直しが大きくなるときは登録者に上げると決めています。今回は重大 2 を含む大きな直しなので、D238 は「直しの確かめの巡を一つ足す」裁定として記録します。
2. **人数**: 前の意見伺いと同じ Gemini 二名と claude.ai 二名。claude.ai の二名は私と同じ系列なので一つに数えます。
3. **時機**: 直しを入れて試しで確かめ、正式の合成データの記録を取り直した後にします。直した後の版と、その正式の記録を見ていただくためです。
4. **お渡しするもの**: 前と同じく、依頼文と束（部に分けたもの）を私が用意し、楠見さんから渡していただきます。束には次のものを入れます。
   - 直した器の全文と、87ce664 からの差分
   - 採否の案
   - 二体の票と私の確かめの記録
   - 正式の記録
   外の方は手元で器を走らせられないので、読んでいただく形になります（前の C2 の方のように、ご自分で小さな台本を走らせるのは自由です）。
5. **扱い**: 前と同じく、逐語で保全し、事実の主張を再現してから採否の案を組みます。Gemini の方々の機種の申告は、前と同じく楠見さんのご確認を正とします。

この形でよければ D238 として記録し、その通りに準備します。人数や時機を変えたい所があればお知らせください。
````

## 注（事実のみ）

- 位置づけ: 正本 `review_plan.no_more` は「この順のほかに巡を置かない」とし、重い所見で直しが大きくなるときは登録者に上げて決めていただくとする。D238 は、器の実装の検分（重大 2 を含む）の直しの確かめの巡を一つ足す裁定である。
- 同じ言葉で、登録者はローカルのコミット c1a759c・f5930f1・a98ee0e・01b8c75・6e72adc の push を許可した。push は済んでいる（この記録を書いた時点の origin/main は 6e72adc）。
- 記帳の置き場: 正本の `decisions` への D236〜D238 の記帳は、直しの後の正本（生成器 v8）で行う。
- 番号: 次の裁定は D239 から。

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/Bl3/rulings-D238.md` >>>

==================== 第三部 二体の票（逐語）と、起草者の再現の記録と、票を得た経緯 ====================

<<< 始: `records/reviews/Bl3/impl/r1/review.md`（SHA16 E10B0C6AE113BD90・束の写しでは手元の一時の置き場の道筋 1 か所を〈手元の一時の置き場〉に置き換えた（元のファイルは変えていない・SHA16 は元のファイルのもの）） >>>
<!-- 逐語保全: 器の実装の検分（正本 review_plan.impl）・系統内の新しい個体（機種の欄 claude-opus-5-5）・検分者 R1（計算の側）。
     検分者の会話の記録の最後の本文（uuid ec5b3a2d-d2a8-4d2c-b44d-8ce23e1240e2・2026-09-25 17:37 日本時間）から機械で切り出した（`save_vote_impl_Bl3.py`）。会話の記録に届いた終わりの通知の本文（XML の文字の置き換えを戻したもの）と同じ。
     この枠の外は一字も変えていない。本検分のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。 -->
The review is complete. Every requested run finished and nothing tracked was changed, so here is my report.

# B-lens 層三（Bl3）の器の実装の検分——検分者 R1（計算の側）の所見

**判定: 条件つき**（重大 0・中 1・軽微 6）

計算の器そのものに、正本と食い違う計算の誤りは見つかりませんでした。条件は中の一件だけです。合成データの器の「本の計算は近道を使わない」の確かめが、器の書いた定数を見るだけで、振る舞いを見ていません。

判定は、器を読む前に書いた規則を当てたものです（重大が 1 以上なら差し戻し・重大 0 で中が 1 以上なら条件つき・重大も中も 0 のときだけ問題なし）。

## 0. 依頼文を読む前にしたこと（開示）

- 作業場で依頼文の置き場を開こうとしました（無かった）。ほかに `ls`・`git status`・`git log` を打ちました。
- 主の作業木で Glob を一度打ちました。R1 と R2 の依頼文の置き場の名だけが出ました（中身は開いていない）。主の作業木へ `cd` する命令は器に拒まれました。
- `git log --all` で、c1a759c と f5930f1（検分の枠）の件名の一行を見ました。
  - f5930f1 の件名には、枠が「予想」と COI（「すでに走っている正式の記録を捨てない引力」）を持つことまで書かれていました。
  - 枠の中身は開いていません。
- 依頼文は、指示どおり `git show c1a759c:records/reviews/Bl3/impl/request-impl-Bl3-R1.md` で全文を読みました。作業場は 87ce664 のままで、切り替えていません。
- 起動の時に、環境から利用者の記憶の索引（MEMORY.md）が文脈に入っていました。この計画の工程の要約を含みますが、器の当否の結論は含みません。

## 1. 同定と走らせたもの

### 1.1 検分の版

- 表の 35 ファイルの SHA16（改行を LF にそろえた SHA-256 の頭 16 桁）は、35/35 一致しました。
- 方向の記録二つは、主の作業木の `results/Bl3/` から写しました（主の作業木では読むと写すだけ）。
  - json の SHA16 は 5E6E3D3BBFC291AD で一致しました。
  - npz の SHA-256 は E660707BCD5E9C3DF39B8AD5754C11671B33596D0BF7ED9F66C6DD852A1AB40E で一致しました。
- 追跡されているファイルは一つも変えていません（`git diff` は空）。作業場に残したのは次の二つだけです。
  - 追跡されていない方向の記録二つ。
  - 無視されるバイトコード（`tools/__pycache__` など）。どれも 13:46:59 JST 以後で、私の走りが作ったものです。
- コミット・push・ネットワークはしていません。実の重みは読んでいません（読んだのは設定とトークナイザだけ）。

### 1.2 合成データの確かめ（採否表 P694）

- **命令**（作業場で）: `OMP_NUM_THREADS=1 PYTHONIOENCODING=utf-8 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python tools/dry_run_Bl3.py --iso 9 --e2e-iso 9 --out <一時の置き場>/r1/dry-run.md`
  - 依頼文の命令に、ネットワークを使わないための環境変数を二つ足しました。手元の置き場から読むので、計算は変わりません。
- **走り**: 04:46:56Z〜08:33:02Z（13557 秒）で、終わりの値は 0 でした。同じ機械で、コーディネータの正式の記録と R2 の走りも同時に走っていました。
- **記録の頭（逐語）**:
  ```
  # B-lens 層三の合成データの確かめ（機械生成・`tools/dry_run_Bl3.py` v2・2026-09-25 08:32 UTC）
  - 等方の方向の本数: 9（正本 1999）。端から端までの分かれ道の等方の本数 9（作った下見の記録で・起動器の出力と同じ JSON の往復）。
  - 順伝播: 走らせる器 2034 回・書き換えの道 1003 回（変種の計算と四の走りは数えない）・13557 秒。
  - 確かめ: 60 のうち 60 が期待どおり。
  ```
- **期待と違った確かめの行**: 無し（「期待と違う」は 0 件）。
- **末尾の版の SHA16 の表**: 35 件で、依頼文の表と全て同じでした（機械で突き合わせた）。
- **目安の値**（乱数の小さな模型なので、値そのものに意味は無い）:
  - 一段目の差の最大は 1.36e-06（許容 1e-3）、二段目は 6.64e-06（許容 0.005）。
  - バッチ一の枝では、二段目の差はちょうど 0 でした。
  - 本物の相対の大きさ（‖v‖／‖h‖ 0.0304）での書き換えの道の変種は、対照の M0 が 1.66e-06 でした。それ以外の M1〜M6 はすべて許容 0.001 の外で、一段目で捕まる形です。
    - M1 1.53e-02・M2 0.110・M3 0.723・M4 0.355・M5 0.0395・M6 0.132。
  - 近道なしの本の道とフックの差は 1.2e-05 以下（0/34 が許容の外）でした。近道ありの本の道では、N1|Onull|+1 で 20/34 が許容の外に出ました（裁定 D234 の裏づけ）。
- この記録は等方 9 本なので、凍結の器が求める正式の記録の条件（`tools/freeze_Bl3.py:113-114`）は満たしません。検分者の走りとして取ったものです。

### 1.3 器の自己検査

- 七つの器の `--selftest` を同じ環境変数で走らせ、すべて終わりの値 0 でした（`bl3_core`・`bl3_directions`・`make_predictions_form_Bl3`・`seal_Bl3`・`build_report_Bl3`・`make_frozen_Bl3`・`bl3_recompute_rewrite`）。
- 書き換えの器は 43/43 でした。
- 書き出しは一時の置き場だけで、後の `git status` に変化はありません。

### 1.4 私が足した確かめ

どれも読み取りだけで、器は一時の置き場に置きました。値は器の出力のままです。

- **(A) 札と門の独立の書き直し**
  - 正本の文から、p・裾の本数・割合を決めた裾・Holm・二つ目の札（中心・最上位・二つの順位）・等方の最上位の割合・効き目の側・本の門・v̂ を抜いた門・選択 a の門を書き直しました。
  - `analyze_Bl3.row_labels`・`gates` と、奇でない押しと零でない帰無の中心を入れた合成の効き目で突き合わせました（等方 1999 本・四つの種）。
  - 主の升目を外した場合と、門の行だけの升目を外した場合（入れ替え 720）を含みます。等方の外の行は種ごとに 8〜13 行ありました。
  - 食い違いは 0 でした。
- **(B) 門の行と転記行 C**
  - `stage_b_gate_rows` の出力を、転記行 C と比べました（場面・腕・方向の番号・土台・符号・単位）。64/64 で同じでした。
  - 家族の鍵の集まりも転記行 C の `cell_signs_gate` と同じでした。v̂ を抜くと 56 行、(6b) も抜くと 55 行です。
- **(C) 方向の名の結び付き**
  - 名の並びは、正本の名前のある方向の順と転記行 D の実在の差の対の並びにそろっていました。
  - 全 2035 本のノルムの相対の差の最大は 1.19e-15 でした。
  - static／loaded／Nk／td と、それぞれの「自分の対」（O~Osec／O-Ncold~Osec-Ncold／Nk~N／Onull~N）の余弦は、どれも 1.000000 でした。除く対の錨と中身が合っています。
  - 名前のある方向・段階 B の三本・等方の頭の五本・近道の確かめの一本は、凍結の関数でビットまで作り直せました。
- **(D) 本の計算が近道を使わないこと（振る舞いで）**
  - 乱数の小さな模型で `run_main_phase` を走らせました。`R.prefix_cache` は呼ばれたら止まる形に差し替え、模型の順伝播の前の hook で引数を見ました。
  - 升目と符号 17・順伝播 47 回のすべてで、近道の元の呼び出し 0、`past_key_values` を渡した回 0、`use_cache` が真の回 0、列の全長でない入力 0 でした。
- **(E) 等方の外の行が出る合成での下流の枝**（7/7 通った）
  - 等方の外の行が出る合成（`build_report_Bl3.synth_analysis` を等方 1999 本で）で、掃き出しは欠け 0、報告は組めて走査の違反 0 でした（「〈両方の外〉」の型が出る）。
  - q7 は転記行 C の区間から作られ、`score_q7` のとおりでした。
  - 二段目の一致は裁定 D232 のとおりでした。内側の行の裾だけを変えても一致し、外の行を反対の裾に動かすと不一致になりました。
- **(F) 乙の文脈の文字**: 180 文脈のすべてで、答えの文字を覆うトークンが転記行 A の文字のトークンと同じでした。
- **(G) 非有限の値の探り**: 軽微 m1 の中身です。

## 2. 所見

### 重大

無し。

### 中

**M1. 合成データの確かめの「本の計算は近道を使わない」は、器が書いた定数を見るだけで、振る舞いを見ていない**

- **(a) 何が**
  - この確かめが見ているのは次の四つだけです: `MP['shortcut'] is False`・`hd.get('shortcut') is False`・`'steered_cache_check' not in hd`・`'shortcut_rule' in hd`。
  - 前の二つは、本の器が定数として書く値です。
- **(b) どの行で**
  - 確かめ: `tools/dry_run_Bl3.py:340-342`。
  - 定数: `tools/bl3_run.py:435-437`（`shortcut = False`・`head['shortcut'] = shortcut`）。
  - 本の計算の近道の口: `tools/bl3_run.py:452`。
- **(c) なぜ問題か（壊れる筋書き）**
  - 本の計算の道が将来の直しで近道を使う形に変わっても、印の定数が False のままなら、この行は「期待どおり」を出し続けます。
  - 凍結の器は、正式の記録の「期待どおり」の数と版の表を、裁定 D234 の遵守の証拠として受け取ります（`tools/freeze_Bl3.py:111`・`:115-123`）。この一行は証拠として働いていません（教訓15 の型）。
  - 今の版の振る舞いは正しいです（1.4 の D で近道 0 回）。直すのは確かめの強さで、計算ではありません。
- **(d) 直し方**
  - `run_main_phase` の間だけ `R.prefix_cache` を数える包みにします。
  - あわせて、模型に `register_forward_pre_hook(..., with_kwargs=True)` を掛けます。全ての呼び出しで、`past_key_values is None`・`use_cache` が偽・入力の長さが列の全長であることを確かめます。
  - 直すと `tools/dry_run_Bl3.py` の SHA16 が変わるので、いま走っている正式の記録は取り直しになります（凍結の器が版の表で突き合わせる）。
  - 取り直さない道もあります。この行が定数だけを見ることを器の段の記録に書き、振る舞いの確かめ（1.4 D と同じ形）を別の記録として凍結物に足す形です。
  - どちらにするかは登録者の裁定です。

### 軽微

**m1. 非有限の値（NaN）が、札と一致の判定を「開く側」に倒す**

- **(a) 何が**
  - 効き目が NaN のとき、上の裾も下の裾も 0 本と数えられます。そのため p は最小の 0.001、裾は 'tie' になり、Holm の第一段（0.003125）を通って「等方の外」になります。
  - `agreement` は Python の `max` を使うので、二つ目以降の鍵の NaN を読み飛ばし、一致と答えます。
  - 実測は次のとおりです。
    - `p_and_tail(nan)` は `{'p': 0.001, 'upper': 0, 'lower': 0, 'tail': 'tie'}` を返し、Holm の判定は通りました。
    - 二つ目の鍵に NaN を置いた `agreement` は `{'agree': True, 'max_abs_diff': 0.0}` を返しました。
- **(b) どの行で**: `tools/bl3_core.py:57-62`・`:248`。
- **(c) なぜ問題か**
  - 本物の模型で出る見込みは小さいです（加える量は選んだ層の残差の 6% ほど・正本 `layers.relative_injection_selected`）。
  - それでも、出たときに止まらず「最も強い札」と「一致」に倒れます。
  - 頭の出口の値の自己検査は、升目一つの無操作しか見ません。
- **(d) 直し方**
  - `run_cell_sign`・`recompute_hook_path` の出口（`tools/bl3_run.py:295`・`:309`）で `np.isfinite` を確かめ、外れたら ToolError にします。
  - 集計の入口でも同じ確かめを置きます。
  - `agreement` は「NaN があれば不一致」にします。

**m2. 一致だけを見る段と結果を開く段が、組の出力の申告をそのまま信じ、組の間の環境の違いを記すだけで止めない**

- **(a) 何が**
  - 組のどれか一つの session に DRY の印があれば、全体を DRY として比べる行を絞ります。
  - 等方の本数は、組 recompute の `n_iso` に合わせた正本の写しで、主の札まで計算します。
  - 組の間でコミット・DRY・正本・npz・層・係数が違っても、記すだけで止めません。
  - 開く段は「一致」の記録の有無だけを見て、その記録がどの出力を見たかを照らしません。
- **(b) どの行で**
  - `tools/analyze_Bl3.py:323-328`・`:343`・`:351-352`・`:362-367`・`:424-428`・`:430-432`。
  - 本の流れでは、`n_iso` は起動器が検べた npz から 1999 になります（`tools/colab/boot_Bl3.py:428`）。
- **(c) なぜ問題か**
  - 置き場の取り違えで DRY の組を一つ混ぜると、主の札が 9 本の帰無で計算し直され、二段の一致も縮めた帰無で判定されます。
  - それでも止まりません（画面には「組の環境 違う」が出ます）。
  - 操作の取り違えの筋書きで、データの側からは起きません。
- **(d) 直し方**
  - DRY でないときは、`rc['n_iso']` と主の各升目の等方の本数が正本の本数であることを確かめます。
  - コミット・DRY・正本の SHA16・npz の SHA-256・層・係数の違いでは止めます（GPU の違いは記すだけでよい）。
  - 一致の段の記録に組の出力の SHA-256 を置き、開く段で照らします。

**m3. 升目の入力の突き合わせが長さと位置だけ**

- **(a) 何が**: プロンプトのトークンの並びそのものは照らさず、長さ・主位置・読み取りの位置・族だけを転記行 B と比べます。
- **(b) どの行で**
  - `tools/bl3_run.py:333-334`。
  - 書き換えの器も同じ粒度です（`tools/bl3_recompute_rewrite.py:157-172`・前置きの SHA16 は足している）。
  - 起動器は tokenizers の版を固定していません（`tools/colab/boot_Bl3.py:190-193`）。
- **(c) なぜ問題か**
  - 同じ長さで違う並びになる割り方は捕まりません。
  - 二つの道は同じ組み立ての関数を使うので、一段目でも捕まりません（正本の限界の、二つの道が同じ所から受け取る入力の種類）。
  - 見込みは小さいです。
- **(d) 直し方**: 転記行 B に、升目ごとのトークンの並びの SHA16 を足して照らします。少なくとも、tokenizers の版を記録に置きます。

**m4. 合成データの純粋な確かめは芯の関数だけを当て、集計の器の組み立ては答えのある形で当てていない**

- **(a) 何が**
  - 「奇でない押し」は、手で作った効き目で `K.gate` を呼びます。
  - 「減算の行（符号の二重掛け）」は、`blens_core.gate_perm` を直に呼びます。
  - `analyze_Bl3.gates` の家族の鍵と、`row_labels` の比べる相手の升目と符号（`same`・`opp`）の組み立てには、答えのある確かめがありません。二と三は流すだけです。
- **(b) どの行で**
  - 確かめ: `tools/dry_run_Bl3.py:95-113`・`:424-426`。
  - 対象: `tools/analyze_Bl3.py:74-100`・`:111-123`。
- **(c) なぜ問題か**
  - 組み立ての誤りは、二つの道が同じ札の関数を使うので、一致の段でも捕まりません。
  - 今の版に誤りは無いと確かめました（1.4 の A と B）。
- **(d) 直し方**: 独立に書いた札と門との突き合わせを一つ足します（1.4 A の器の形でよい）。

**m5. 合成の確かめのどれも、等方の外の行が出る枝に届かない**

- **(a) 何が**
  - 端から端までの分かれ道は等方 9 本、報告の器の自己検査は 199 本です。
  - 最小の p は 0.2 と 0.01 で、Holm の第一段 0.003125（正本 `labels.holm_first_step`）を下回れません。
  - そのため、次の枝を一度も通りません。
    - 等方の外の行の裾と側の一致（裁定 D232）。
    - q7。
    - 読みの型「埋もれる」「両方の外」。
  - 正式の記録でも、主の部は乱数の方向なので、外の行はまず出ません。
- **(b) どの行で**
  - `tools/dry_run_Bl3.py:486`・`:662`。
  - `tools/build_report_Bl3.py:364`・`:434`。
- **(c) なぜ問題か**
  - これらの枝の誤りは、結果を開いた後に初めて出ます。
  - 今の版では通ります（1.4 の E で 7/7）。
- **(d) 直し方**: 等方 1999 本と強い合成の static で外の行を作る場合を、報告の器の自己検査か合成データの器に一つ足します。

**m6. 方向の名と npz の行の結び付きを、正本と転記行 D の並びに照らしていない**

- **(a) 何が**
  - json の名の並びと npz の行を、位置で結びます。
  - 組ごとの SHA-256 は配列のバイトだけを見るので、名の並びの入れ替えは捕まりません。
- **(b) どの行で**: `tools/bl3_run.py:339-355`・`tools/bl3_directions.py:79-89`。
- **(c) なぜ問題か**
  - json は凍結物なので、凍結の後の変化は凍結の器が捕まえます。これは凍結の前に作り直すときの守りです。
  - 今の結び付きは正しいです（1.4 の C）。
- **(d) 直し方**: 次の二行を足します。
  - `J['groups']['named']['names'] == T3['directions']['named']`
  - `J['groups']['real']['names'] == FJ['facts']['D']['real_pairs']`

m1・m2・m6 と m4・m5 の足しも、どれも版の SHA16 を変えます。M1 のために正式の記録を取り直すなら、一緒に入れるのが安上がりです。

## 3. 器が黙って決めていること（正本に書かれていない一つの振る舞い）

1. バッチの並びの種の鍵は、升目と符号の並びの番号です。外した升目の組も番号に数えます（`tools/bl3_core.py:296`・`tools/bl3_run.py:448-453`）。
2. 無操作は、並びのどこにでも入ります。無操作の入ったバッチを先に流します（`tools/bl3_run.py:261-264`）。
   - 無操作のバッチの揺れは、升目と符号の中の全ての効き目に同じだけ入ります。
   - p と二つ目の札は動きませんが、q7 の符号・効き目の側・零との比べには床の大きさで入ります。
3. 出口の値の自己検査は、升目一つで行います（`tools/bl3_run.py:179`・`:432`・`tools/colab/boot_Bl3.py:403`・`:412`）。最初の升目は N1 で、nuclear の族だけを見ています。
4. (vi) の揺れは、主の升目だけで測ります。門の行だけの升目の揺れは床に入りません（`tools/bl3_run.py:185-192`）。
5. (iii) の順位相関は、(i)(ii) を満たさない升目も含めた八升目で計算します（`tools/bl3_run.py:213-218`）。
6. (iv) の揺れの版は、主の升目だけです（`tools/bl3_run.py:224-226`）。
7. 境の扱い。
   - 効き目の側は、四分位の境を含みます（`q1 <= 0 <= q3`）。中央値と同じ値と零は「弱い」側です（`tools/bl3_core.py:87-96`）。
   - (i)(ii) の境も含みます（`tools/bl3_core.py:210`）。
8. 裾の本数が上下で同じなら、裾は 'tie' です（`tools/bl3_core.py:61`）。
9. (vi) の (b) の一回目は、(a) の大きさ一の値を使い回します（`tools/bl3_run.py:187-190`）。回数は正本どおり三です。
10. 書き換えの器の `use_cache` の既定は `config.use_cache`（is_causal の核）です。起動器と合成データの器は `use_cache=False`（明示の mask）で呼び、フックの道の近道なしと同じ形になります（`tools/colab/boot_Bl3.py:444`・`tools/dry_run_Bl3.py:283`）。書き手の開発の記録 §6 に書かれています。
11. 一致の段は、組の一つが DRY なら全体を DRY として扱います（m2）。
12. 乙の文字のトークンの食い違いは、記すだけで止めません（`tools/bl3_run.py:393`）。今の 180 文脈はすべて同じでした。
13. 無操作の選択肢 a の確率は、升目と符号ごとに二つ並べます（`tools/analyze_Bl3.py:252-256`）。

## 4. 見どころごとの確かめ（正本 `review_plan.impl.focus`）

1. **掃き出し**
   - 計算の側の出力（札の各欄・門五つ・記述・層ごとの差分・乙・二段の値）を、`tools/sweep_Bl3.py:17-95` が有るかどうかで見ています。
   - 正本の `labels.print_rule`・`pilot.decision.report`・`descriptive`・`independent_recompute` と比べ、欠けは見つかりませんでした。
   - 凍結の本文はまだ無いので照らしていません（R2 の主）。
   - 草案3 §4 (v) の D234 の前の文は、差の記録（`records/Bl3/frozen-diff-Bl3.md:11-12`）で正本の文に替わると確かめました。
2. **封印**: `seal_Bl3 --selftest` が通ったことだけを確かめました（R2 の主）。
3. **許容と版**
   - 許容は、出口の値 0.5・最後の層 1e-4・一段目 1e-3・二段目（近道の許容と床の和）・揺れと近道の許容の式・質量と床と天井・揺れの版の印です。使われる場所は正本のとおりでした。
   - 版は、書き換えの器が transformers 4.57.3 を求め、起動器が numpy・torch・transformers を固定しています。
4. **近道**: M1 に書いたとおりです。
   - 本の計算は近道を使いません（行と振る舞いの両方で確かめた）。
   - 下見の (v) だけで使います。
   - 切れ目と cache の実の長さは、近道の順伝播のたびに ToolError で守られています（`tools/bl3_run.py:120-124`）。合成データの三で、両方が本物の道で止まりました。
5. **精度とバッチ**
   - 読み取りは、最終の正規化と読み取りの集合の行を float32 で当てています（`tools/bl3_run.py:79-94`）。
   - 加減は、方向ごとの行列を凍結の `make_hook` に渡し、層の出力の型に直して sign×coef を一度掛け、各行の起点から後ろに足しています（`tools/run_stageB_local.py:156-187`）。
   - 端数は零のベクトルで埋め、その値は使いません（`tools/bl3_core.py:290-300`・`tools/bl3_run.py:272-275`）。
6. **門と最上位**: 1.4 の A と B で確かめました。
   - 門は、符号を +1 にし、家族の鍵を「升目|符号」にして呼んでいます（`tools/bl3_core.py:168`・`tools/analyze_Bl3.py:51`・`:115-116`）。
   - 最上位は、中心を引いてから `top_rank` に渡しています（`tools/bl3_core.py:105-118`）。
7. **層の出力**
   - 最終の正規化の入力は前の hook で取り、層ごとの値は各層の出口の hook で取っています（`tools/bl3_run.py:129-133`）。`hidden_states` は使いません。
   - 最後の層の自己検査は恒等ですが、`hidden_states` の最後の要素（正規化の後の値）を取り違えれば落ちる形です。
8. **合成データを実際に走らせる**: 1.2 のとおりです（60/60）。

## 5. この検分が確認していないこと

- 本物の模型の上の振る舞い全般（封印の前の決まり）。具体的には次のものです。
  - 許容 0.5 が、本物の模型で二重の正規化を捕まえる強さ。
  - GPU での一段目の一致と核の選び方。
  - 残差の大きな次元での bf16 の丸め。
  - 本物の揺れの大きさ。
- 設計事実の器 `tools/bl3_facts.py`。走らせておらず、読んだのは q7 の区間と段階 B の三本の再生の所だけです。
  - 転記行 A（書き出し・文字のトークン）・E（数）・F（重み）は、器の出力を信じました。
  - C と D は突き合わせました。
- 実在の差の方向の中身。活性のファイルは読まず、結び付きを余弦で見ただけです。
- 流れと記録の側（起動器の相の分かれ・凍結・封印・報告の中身・凍結の本文の組み直し）。自己検査を走らせたほかは、深く見ていません（R2 の担当）。
- コーディネータの正式の合成データの記録（等方 1999 本）の結果。見ていません。
- 段階 B の試行の記録と集計の記録そのものの当否。

## 検分票

- **対象**: B-lens 層三の器の計算の側です。
  - 主に見た器: `bl3_run.py`・`bl3_core.py`・`analyze_Bl3.py`・`bl3_directions.py`・`bl3_recompute_rewrite.py`・`dry_run_Bl3.py`（一〜三）。
  - 凍結の器（`run_stageB_local`・`steer_B`・`direction_B`・`blens_core`・`blens_lens`・`boot_Blens`）は、呼び方だけを見ました。
  - 版は依頼文の表（87ce664 と同じ）です。
- **段階**: 事前登録ありです。
  - 判定の規則・重さの基準・予想・COI を、計算の器を読む前と合成データの出力を見る前に、一時の置き場へ書きました（2026-09-25T04:48:06Z）。
  - ただし `dry_run_Bl3.py` の全文は、書き出し先の確かめのために登録の前に読みました。
  - 予想の当たり外れ（どれも消していない）:
    - 「60/60 期待どおり」（確かさ 0.70）は当たりました。
    - 「中以上 1 件以上」（0.75）は当たりました。
    - 「重大 1 件以上」（0.25）は起きませんでした。
    - 「判定 条件つき」（0.55）は当たりました。
- **凍結物の同定**
  - 検分の版の SHA16 表（35 件）。
  - npz の SHA-256 E660707B…AB40E。
  - 正本 draft3-r3-2026-09-25（SHA16 D41CFA474EA0190E）。
  - 下見の前の凍結はまだです。
- **盲検の状態**: 盲検ではありません（器の書き手は分かっている）。
  - 先に出た意見（opinions-tools）は開いていません。R2 の所見も見ていません。
  - 二人の検分者の一時の置き場は、同じ根の下（`…/scratchpad/r1` と `r2`）で、互いに読める形でした。r2 は開いていません（プロセスの一覧で置き場の名を見ただけ）。
  - 枠のコミットの件名を一行見ました。
- **敵対的検分**: 1.4 の A〜G です。
  - 札と門の独立の書き直しは食い違い 0、門の行は 64/64、方向の結び付きは余弦 1.000000 とビットでの作り直し、近道は 0 回でした。
  - 外の行の枝は 7/7、乙は 180/180 でした。NaN は開く側に倒れました。
  - 分母の確かめ: 門の行 64・v̂ を抜いて 56・(6b) も抜いて 55、主の行 16。偶然の目安 0.3087 と 0.6057 は、正本の式 8/49＋8/55 と 8/25＋8/28 で再計算しました。
  - 出典: 所見はすべて行の番号つきです。
- **系統の内訳**
  - 系統内の新しい個体（Claude Opus 5.5）の一体（R1）です。
  - コーディネータと書き換えの器の書き手と同じ系列なので、盲点を共有しえます（何体でも一票・裁定 D59）。
  - この巡に系統外の目はありません。
- **COI記録**: 事前に三つの引力を書きました。
  - A: 予定と走っている正式の記録を止めない側。
  - B: 見つけすぎる側。
  - C: 同系列の確かめを信じる側。
  - 逆らう印として、所見ごとに行と壊れる筋書きを置き、重さは事前の基準だけで付けました。
    - M1 を中にしたのは、基準の「名札より弱い確かめ」によります。
    - m1 と m2 は見た目は重いものの、基準の「本物のデータで起こりうるか」で軽微にしました。
- **判定**: 条件つき（重大 0・中 1）。
- **本検分が確認していないこと**: §5 のとおりです（本物の模型の上の振る舞い全般を含む）。

一時の置き場の器と記録（検分者のもの・後で消えうる）は、`〈手元の一時の置き場〉\r1\` にあります。

本検分のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/reviews/Bl3/impl/r1/review.md` >>>

<<< 始: `records/reviews/Bl3/impl/r2/review.md`（SHA16 E7E5758CA84F0134・束の写しでは手元の一時の置き場の道筋 1 か所を〈手元の一時の置き場〉に置き換えた（元のファイルは変えていない・SHA16 は元のファイルのもの）） >>>
<!-- 逐語保全: 器の実装の検分（正本 review_plan.impl）・系統内の新しい個体（機種の欄 claude-opus-5-5）・検分者 R2（流れと記録の側）。
     検分者の会話の記録の最後の本文（uuid 0d043107-41c8-4f00-87a5-92702783ccb7・2026-09-25 18:30 日本時間）から機械で切り出した（`save_vote_impl_Bl3.py`）。会話の記録に届いた終わりの通知の本文（XML の文字の置き換えを戻したもの）と同じ。
     この枠の外は一字も変えていない。本検分のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。 -->
# B-lens 層三の器の実装の検分：検分者 R2（流れと記録の側）の所見

**判定: 条件つき**

判定は、器を開く前に書いた判定表のとおりです。重大が二つあり、どちらも下見の前の凍結までに局所の直しで閉じられます。
- 重大の一つ目は、封印した予想の SHA-256 を採点の時に誰も照らさないことです（B-lens の器にあった照合が層三で落ちています）。
- 重大の二つ目は、結果を開く段が、一致だけを見る段が見た出力を開いているかを確かめないことです。
- 中は五つあり、直すか、限界として受けるかの登録者の裁定が要ります。
- 合成データの確かめ（採否表 P694）は私が走らせ、60 のうち 60 が期待どおりでした。版の SHA16 の表も 35 行すべてが依頼文と同じでした。

## 0. 前提と作法の申告

- **依頼文を読む前にしたこと**
  - 読み取りだけです：依頼文の置き場を Read（無し）、名前での検索（無し）、作業場の `ls`・`git status`・`git log`、`records/` と `records/reviews/` の一覧。
  - 共有の作業木で git を走らせる一行を一度打ちましたが、ハーネスが拒み、走っていません。
  - お知らせの後、`git show c1a759c:…/request-impl-Bl3-R2.md` で依頼文を読みました。作業場はコミット 87ce664 のまま切り替えていません。
- **守ったこと**
  - 追跡されているファイルは一つも変えていません。`git status` に出るのは、依頼どおり写した未追跡の `results/Bl3/directions-Bl3.{json,npz}` だけで、これは作業場に残しています。
  - コミットも push もしていません。
  - ネットワークは使っていません。HF の走りにはすべて `HF_HUB_OFFLINE=1`・`TRANSFORMERS_OFFLINE=1` を足しました。
  - 実の重みは読んでいません。起動器は DRY（乱数の小さな模型）だけで走らせました。
  - `bl3_facts.py`・`make_contrasts_Bl3.py` は読んだだけです。
  - `make_frozen_Bl3.py --check` は、追跡されている `records/Bl3/frozen-diff-Bl3.md` を書き換えるので走らせていません。
  - 鍵のファイル・会話の記録・`records/reviews/Bl3/opinions-tools/`・R1 の一時の置き場（同じ一時の置き場の `r1`）は開いていません。
  - 正本 `inputs.files` の一つが opinions-tools の下にあるため、その SHA16 の照合は飛ばしました。
- **事前登録**
  - 器を開く前（13:43 JST）に、判定表・予想（信頼度つき）・COI を一時の置き場に書きました（`prereg-R2.txt`・SHA-256 87F95E83…69FF）。
- **お知らせの driver と boot_Bl3.py について**
  - 塞がっていたのではありません。外の走りを遅くしないよう、私が優先度を「通常より低い」にして始めたためです（子の過程も継ぎます）。
  - ほかの走りで CPU が埋まっている間は、ほとんど回りませんでした。管は `subprocess.run(capture_output=True)` が読むので塞がりません。起動器も自分の `progress.log` と組の JSON に書いています。
  - 私の合成データの走りが 17:32 に終わった後で「通常」に上げました。相 pilot は 17:59 に、相 main と後の段は 18:25 に、終わりの値 0 で終わっています。
  - **止めても、走らせ直してもいません。** 私の過程はもう残っていません。

## 1. 版の同定

- 依頼文の表の 35 ファイルは、SHA16（LF にそろえた SHA-256 の頭 16 桁）がすべて一致しました。CRLF は 0 でした。
- 方向の記録は主の作業木から写しました。json の SHA16 は 5E6E3D3BBFC291AD、npz の SHA-256 は E660707B…1AB40E で、どちらも一致です。
- `freeze_Bl3.import_closure(TOOLS)` は 31 ファイルでした。正本・設計事実二つ・方向の記録を足すと、依頼文の表の 35 とちょうど同じです。

## 2. 走らせたもの

### 2.1 合成データの確かめ（採否表 P694 の票）

- **命令**：`OMP_NUM_THREADS=1 PYTHONIOENCODING=utf-8 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 python tools/dry_run_Bl3.py --iso 9 --e2e-iso 9 --out <作業場の外>/r2/dry-run.md`
  - 13:47 から 17:32 JST まで、13532 秒でした。糸一つで、終わりの値は 0 です。
  - オフラインの二つの環境変数は私が足しました。
- **記録の頭（逐語）**
  - 「確かめ: 60 のうち 60 が期待どおり。」
  - 「等方の方向の本数: 9（正本 1999）。端から端までの分かれ道の等方の本数 9」
  - 「順伝播: 走らせる器 2034 回・書き換えの道 1003 回…・13532 秒。」
- **期待と違った確かめ**：無し（「**期待と違う**」の行は 0 でした）。
- **末尾の版の SHA16 の表**：35 行すべてが依頼文の表と同じでした（欠け 0・余り 0・違い 0）。記録の中の「走りの始めと終わりで同じ」も期待どおり（35 ファイル）です。記録の SHA-256 は 73FABCD4…BC2BA（333 行）です。
- **記述の行で目に付いたもの**（判定には入れていません）
  - 本物の相対の大きさの合成の方向で、書き換えの道の変種 M1〜M6 はすべて一段目の許容 0.001 の外でした。
  - 主位置の一つ分の寄与は 8.12e-03 で、近道の許容 0.005 を超えます。
  - 近道ありの本の道は、N1|Onull|+1 で 34 方向のうち 20 が許容の外でした。近道なしでは 0 です。いずれも裁定 D231・D234 の向きです。

### 2.2 七つの自己検査

- bl3_core・bl3_directions・make_predictions_form_Bl3・seal_Bl3・build_report_Bl3・make_frozen_Bl3・bl3_recompute_rewrite（43/43）は、すべて終わりの値 0 でした。
- どれも一時の置き場にしか書かず、`git status` は変わりませんでした。

### 2.3 起動器の DRY（私の追い問い）

- **相 check**：114 秒・終わりの値 0。
  - 升目と符号 17、順伝播 24527（転記行 E の 24408＋7＋112）、再計算の一つの道 16392、v̂ の行 8、門の行 64、乙 1280・260・180。すべて転記行 E・C と一致しました。
  - 錨と ‖static‖ も合い、書き換えの器は import できました。
- **相 pilot**：終わりの値 0。
  - q1 は「続ける」、外した升目は無し、バッチ 16、順伝播 84 でした。
  - 揺れの床 4.811094168388763e-06 は、合成データの器の下見とまったく同じ値です。
- **相 main**（等方 2・再計算の行 2・乙の文脈 1）：1571 秒・終わりの値 0。
  - 組の JSON 三つと session が置かれました。
  - `analyze_Bl3 judge` は一致、`open` は通り、`sweep_Bl3` は欠け 0 でした。
  - 同じ組を二つの置き場に与えると、judge は止まりました（終わりの値 1）。
- **止め方の試し**：下見の記録が「止める」の pilot.json で相 main を走らせました。
  - 終わりの値 1 で、正しい文で止まりました。
  - 置き場に残ったのは progress.log だけで、session.json と zip は作られませんでした。
  - 止まるまで 127 秒かかりました（模型と方向を読んだ後に、下見の記録を見ています）。

## 3. 所見

### 重大

**重大-1 封印した予想の SHA-256 を、採点の時に誰も照らさない**
- (a) 何が
  - 報告の組み立ての器は、封印の記録にある置き場から二つの予想を読んで採点します。しかし、記録に置いた SHA-256 と今のファイルを照らしません。
  - 封印の記録の `freeze_record_sha16` は、どの器も読みません。
  - 起動器の相 pilot・main と本の凍結は、封印の記録が「ある」ことしか見ません。
- (b) どの行で
  - `build_report_Bl3.py:310-311` で予想を読むだけで、:328 は封印の記録の SHA16 を印字するだけです。
  - `seal_Bl3.py:136,144` が SHA を書きますが、読み手はいません（grep で確認）。
  - `boot_Bl3.py:168-170`、`freeze_Bl3.py:292,308-309,315-316` も、封印の SHA を本の凍結に写しません。
  - B-lens では照らしていました：`blens_lens.py:317-327`（`require_sealed`）と `build_report_Blens.py:249`。
- (c) なぜ問題か（壊れる筋書き）
  - 一時の置き場の写しで試しました。封印の後に、コーディネータの q4.gate を「通る」から「通らない」に書き換えました（SHA は 61AA6481… から 4DFF2A4D… に変わります）。
  - `load_inputs` は止まらず、報告の表は書き換えた値を「通らない（一致）」と採点しました。
  - 予想を結果から守る要の柵が、コーディネータ自身が報告を組む所で黙って抜けています。チャットの SHA と git の履歴という外の錨は残ります。
- (d) 直し方
  - `load_inputs` の頭に `require_sealed` と同じ型の確かめを置きます。両方の予想の SHA-256 が封印の記録と同じであること、封印の記録の SHA16 が凍結の記録に写した値と同じであることを見ます。
  - 本の凍結で、封印の記録の SHA16 と二つの予想の SHA-256 を `main_freeze` に写します。
  - 起動器の相 pilot と本の凍結では、二つの予想の JSON がそのコミットにあり、SHA が合うことを確かめます。
  - 「書き換えた予想で止まる」自己検査を足します。

**重大-2 結果を開く段は、一致だけを見る段が見た出力を開いているかを確かめない**
- (a) 何が
  - judge の記録には、読んだ出力の同定がありません。持っているのは parts・tool_error・env・dry・first・second・agree・reason・written_utc だけです。
  - open は judge の記録の `agree` だけを見て、別に与えられた置き場を読みます。
  - open が自分で出し直した二段の一致は、不一致でも止まりません。
- (b) どの行で：`analyze_Bl3.py:401,409`（二つの段が別々に dirs を受ける）・:415-429・:430-432・:359-383。
- (c) なぜ問題か（壊れる筋書き）
  - 合成の組の出力と起動器の DRY の出力の両方で、CLI を通して試しました。
  - judge の後に、Nk の効き目に +1000 を足した写しを、元の judge の記録で open しました。止まらずに開き、`cross:S1:O-Ncold+vNk…` の効き目が変わり、二つ目の札は False から True になりました。
  - Nk の行は再計算の外なので、開いた記録の再計算の一致は True のままでした。
  - v̂ の行を変えた写しでは、open の再計算の一致が False になりましたが、それでも記録を書きました。
  - 組を別のランタイムに分けると、置き場は複数になります（正本は落ちた組の走らせ直しを許しています）。どれを judge し、どれを開いたかは記録に残らず、後から確かめられません。裁定 D219 の門が、同じ出力に結びついていません。
- (d) 直し方
  - judge は、読んだ組の JSON と session の SHA-256、それに凍結の記録と封印の記録の SHA16 を記録に置きます。
  - open はそれと照らし、違えば止めます。open の再計算の一致が judge と違うときも止めます。
  - 起動器の session に、組ごとの JSON の SHA-256 を書きます。

### 中

**中-3 相 check のコミットの器と、凍結する器が同じことを確かめない。「順伝播を呼んでいない」は数えた値ではない**
- (a) 何が
  - 凍結の器は、相 check の session の正本の SHA16 だけを今の正本と照らします。器・設計事実・方向の記録は照らしません。
  - `forward_calls` は定数 0 で、凍結の器はその定数を証拠として受けます。
  - 守りは最上位の forward の前だけに掛かっています。
- (b) どの行で：`freeze_Bl3.py:203-216`（特に :207 と :212）、`boot_Bl3.py:309,358`。
- (c) なぜ問題か（壊れる筋書き）
  - この検分の直しで器を変えると、正式の合成データの記録は SHA16 の表で突き合わされるので、取り直さないと凍結できません（:115-123）。
  - ところが相 check は、取り直さなくても凍結の確かめを通ります。凍結の記録は「凍結した器で相 check が通った」と書くことになり、それは偽りです。
  - 守りは乱数の模型で試しました。`model(...)` と `generate` は止まりましたが、`model.model(...)`＋`lm_head` と、`except Exception` の中の呼び出しは止まりませんでした。
  - 今の相 check の道には、順伝播を呼ぶ所はありません（是認の節）。
- (d) 直し方
  - 相 check のコミットの閉包・正本・設計事実・方向の記録の SHA16 を、`git show <commit>:<path>` で今の版と照らします。
  - 起動器で呼ばれた数を部品ごとに数えて書き、Stop を BaseException の子にします。

**中-4 本の凍結は、下見の試みの順と由来を確かめない**
- (a) 何が
  - 試みは `--pilot` に与えた順に並び、最後が本の下見になります。時刻の順は見ません。
  - session の正本・npz・frozen_check は、写すだけで照らしません。
  - 試みのコミットに凍結の記録と封印の記録が「ある」ことしか見ず、中身が今の記録と同じかは見ません。
  - 二つ以上の試みがあっても、やり直しの台帳を求めません。
- (b) どの行で：`freeze_Bl3.py:285-300`（:292 `git cat-file -e`・:295・:315）。
- (c) なぜ問題か（壊れる筋書き）
  - `in_commit` だけを真にして試しました。器の誤りの無い二つの試みを新しい順に与えると、止まらずに古い方（09-26 の「一部の升目を外して続ける」）を本の下見にしました。session の canon_sha16 に 'X' を入れても止まりませんでした。
  - 本の計算のバッチ・揺れの床・外した升目と q1 の採点が、違う試みで決まります。ただし session の finished は記録に残るので、後からは確かめられます。そのため重大ではなく中にしました。
- (d) 直し方
  - finished の順に並べ、与えた順と違えば止めます。
  - 各試みの canon・npz・frozen_check.bad を照らし、コミットの二つの記録の中身の SHA も照らします。
  - 二つ以上の試みには、逸脱の番号を求めます。

**中-5 手元の judge・open・報告は凍結の記録を照らさず、DRY と本の出力が混ざっても止めない**
- (a) 何が
  - 手元の正本・設計事実・方向の記録・集計と組み立ての器を、`main_freeze.frozen_sha16` と照らしません。
  - session に一つでも DRY があれば、全体を DRY として扱います。その場合、比べる行は再計算の組の行に絞られ、`--pilot` も許されます。
  - B-lens の `require_sealed` は、凍結物の一部も照らしていました（`blens_lens.py:328-331`）。
- (b) どの行で：`analyze_Bl3.py:397-413`（:410 `dry = any(...)`）・:342-352、`build_report_Bl3.py:306-332`。
- (c) なぜ問題か（壊れる筋書き）：結果が出た後、つまり引力が最も強い時に手元で集計の器を直しても、何も止まりません。
- (d) 直し方
  - 二つの段と報告の頭で、正本・設計事実・方向の記録・analyze_Bl3・bl3_core・blens_core・build_report_Bl3・sweep_Bl3・report_lint を凍結の記録と照らします（台帳に記した差分は許します）。
  - dry が組の間で違えば止めます。

**中-6 起動器の相 pilot・main と、ファイルを通した往復は、凍結の器が見る合成データの記録に入っていない**
- (a) 何が
  - 記録の端から端までは、器の関数を同じプロセスの中で呼び、session も手で作っています。
  - 起動器の組の分け方・書き出し・zip、集計の器の `load_outputs`・`_pilot_attempts` は、その記録では走りません。
- (b) どの行で：`dry_run_Bl3.py:477-505`（:481-482）、`freeze_Bl3.py:102-124`。
- (c) なぜ問題か（壊れる筋書き）
  - 起動器を直した後に合成データの記録を取り直せば、凍結を通ります。そのとき、直した起動器は一度も走っていません。
  - 今の版については、私が 2.3 で通しました。コーディネータの boot-dry-3 も同じ SHA16（458C5D30…）です。
- (d) 直し方：合成データの器に、起動器の三つの相を DRY で別のプロセスとして走らせ、CLI の judge・open・sweep に通す節を足します。

**中-7 組の出力は /content に置くだけで、ランタイムが落ちると前の組の出力も失う**
- (a) 何が：既定では三つの組を一度の起動で走らせ、zip とダウンロードは最後だけです。Drive も使いません。
- (b) どの行で：`boot_Bl3.py:130-133,289-305,407-467` と docstring の :21-22。
- (c) なぜ問題か（壊れる筋書き）
  - 本の計算の組（数時間）が終わった後、再計算の組の途中でランタイムが落ちると、main.json も VM と一緒に消えます。
  - 「落ちた組から走らせ直す」ことができず、本の計算を走らせ直すことになります。そうなると、どの走りを使ったかの問題（重大-2）にもつながります。
- (d) 直し方：組ごとに zip とダウンロードをするか、OP4B_PART を必須にします。走らせた全ての zip の SHA-256 を台帳に記します。

### 軽微

- **8.** (a) 報告の §1 は「(v)…近道を使う: はい」と印字し、§4 は「本の計算の近道: いいえ」と印字して、食い違います。(b) `build_report_Bl3.py:130` と :263-264。(c) 凍結の本文は「記述・本の計算は近道を使わない」に直されています（`frozen-diff-Bl3.md:13-14`）。合成の報告で、この食い違いを確かめました。(d) 「(v) の差が許容の内」と書き換えます。
- **9.** (a) 凍結の記録の `rulings` に、段階 B の D58・D59・D75・D90 が入ります。(b) `freeze_Bl3.py:235` の `k >= 'D204'` が文字列の比べだからです。(c) 計算で確かめました。(d) `int(k[1:]) >= 204` にします。
- **10.** (a) 門の行だけの升目 S4|Osec-Ncold だけを外しても、〈下見で一部を外した〉の型が当たります。(b) `bl3_core.py:199`、`build_report_Bl3.py:83-84`。(c) q1 は「続ける」のままなのに、「主の札…から外した・Holm…を数え直した」と、事実と違う文になります。試して確かめました。(d) 主の升目の外しと、門の行だけの升目の外しを分けて書きます。
- **11.** (a) 「nuclear の族は測れなかった」が、報告の頭ではなく §1 に置かれます。(b) `build_report_Bl3.py:132-134` と、正本・草案3 :126 の「報告の頭に」。(c) N1 の二升目を外して試しました。(d) §0 にも置きます。
- **12.** (a) 掃き出しは、層ごとの差分について鍵があるかしか見ません。(b) `sweep_Bl3.py:76-80`。(c) rows が空でも通ります。(d) 名前のある方向と段階 B の三本がそろうこと、層の数、`iso_summary` の中身を見ます。
- **13.** (a) 凍結の本文の確かめが、凍結版の原稿（FROZEN.src.md）を見ません。(b) `make_frozen_Bl3.py:88` で引数 `fsrc` が使われていません。(c) 原稿と本文が食い違ったまま凍結できます。(d) 原稿を組み直して本文と照らします。
- **14.** (a) 登録者の逐語と日時が、束縛検査と禁止語の走査を通ります。(b) `make_frozen_Bl3.py:32-33,61`、`build_draft_Bl3.py:47-55,92-103`。(c) 「10時」のような数や「証明」のような語で止まり、逐語を変えて通す誘因になります。(d) 凍結の一行をその検査から外すか、日時の形を決めます。
- **15.** (a) 版の確かめが、`inputs.versions_B` の scipy を黙って外しています。(b) `boot_Bl3.py:192-193`、`freeze_Bl3.py:208`。(c) 計算の道は scipy を読まないので害はありませんが、「文字列で同じ」という言い方は言い過ぎです。(d) 外す理由を書くか、scipy も照らします。
- **16.** (a) 正本 `inputs.files` の SHA16 を、凍結の器が照らしません。(b) `freeze_Bl3.py:86-99`。(c) 今は 27/27 が一致していますが、機械の柵はありません。(d) 照合を一行足します。
- **17.** (a) `verify_frozen` は、疎な取り出しの内側で欠けたファイルも飛ばします。(b) `boot_Bl3.py:97-108`。(c) 欠けても止まりません。(d) 疎な取り出しの外のものだけ飛ばし、内側で欠けたら止めます。
- **18.** (a) 下見と本の計算の GPU が同じかを見ません。(b) `boot_Bl3.py:204-209`、`analyze_Bl3.py:323-328`。(c) 揺れの床は下見の GPU で決まり、二段目の許容に使われます。(d) env に下見の GPU も並べ、違いを記録に出します。
- **19.** (a) 登録者の情報状態の欄が空でも封印します。(b) `seal_Bl3.py:113-127` と、自己検査の :185-186。(c) 正本 `predictions.free`（裁定 D217）は、両方が書くと言っています。(d) 空なら印を置き、登録者に知らせます。
- **20.** (a) 方向の npz は、相 check の前にプッシュしておく必要がありますが、その順が正本に書かれていません。(b) `boot_Bl3.py:240-243` では、無ければ予期しない誤りで落ちます。`freeze_Bl3.py:203-205` は相 check の出力を凍結の入力にしています。(c) 手順の順が器にしか書かれていません。(d) 「npz と器をプッシュ → 相 check → 凍結の記帳」を正本か記録に書き、npz が無いときは stop で止めます。
- **21.** (a) 止め方と数の記録が弱いです。stop の印は session.json に入りません。予期しない誤りでは session.json も zip も作られず、その文は相 check の守りの Stop でも「器の誤りではない」と言います。`n_forward` は同じ起動の中で累積します。(b) `boot_Bl3.py:78-80,465-466,470-478,462`。(c) 試して確かめました（相 main の n_forward は 47、再計算の組は 152）。(d) session に stop を書き、組ごとの数を書きます。
- **22.** (a) 記述が古いか、柵が無い所があります。(b) `make_contrasts_Bl3.py:2` の docstring は v5（`VERSION` は v7）。`sweep_Bl3.py:4` は無い REQUIRED を指しています。`bl3_facts.py` を `--skip-weights-hash`（:24,362）で作り直すと、転記行 F から断片が落ちますが、起動器は載っているものだけを照らします（`boot_Bl3.py:233`）。(c) 今の転記行 F は断片三つを含んでいるので害はありません。(d) 転記行 F が索引の断片を全て含むことを照らします。

## 4. 確かめて問題なしとしたもの（行つき）

- **相 check**：今の道に順伝播を呼ぶ所はありません。呼ばれる関数を全部たどり、`context_of` もトークナイザだけでした。DRY でも数の照合がすべて一致しました。
- **本の計算**：近道は使いません（`bl3_run.py:435,452`）。近道の元の切れ目と、cache の列の実の長さの確かめ（:120-124）は、合成データの三で止まりました（474 対 473）。
- **読み取りと層**：読み取りは float32 で行います（:79-87）。層の出力は層の出口のフックで取り（:129-133）、`hidden_states[-1]` の取り違えは書き換えの道の自己検査が捕まえます。
- **封印の順と書式**：封印の順（コーディネータが先・一度だけ・凍結の後）が守られています。書式の JS `gen()` と `to_json` の形も同じです。
- **組の出力の形**：相 main の組の JSON は組ごとに置かれ、集計の器が読む形とそろっています（CLI で往復を確かめました）。judge は同じ組の重なりで止まります。
- **凍結の本文**：`other_diffs` の「FROZEN」の部分一致の穴は、草案3 に FROZEN を含む行が 0 なので、今の本文では働きません。
- **入力と記録**
  - 転記行 F は断片三つを含みます。
  - `inputs.files` は 27/27 一致しました。
  - 段階 B の raw と trials は 59/59 が追跡されています。
  - `boot_Blens` は import しても定数しか走りません。
  - 揺れの版は三つとも境を保ちます。
  - 門の行だけの升目は N1 ではありません。

## 5. 器が黙って決めていること

1. 門の行だけの升目の不合格も、同じ `dropped` に入れます（軽微 10）。
2. 本の凍結の試みの順は、引数の順です（中-4）。
3. judge と open は、別々の置き場を受けます（重大-2）。
4. 三つの組を既定で一度に走らせ、持ち出しは最後だけです（中-7）。
5. 疎な取り出しの内側の欠けも飛ばします（軽微 17）。
6. 版は三つに絞ります（軽微 15）。
7. 登録者の欄が空でも封印します（軽微 19）。
8. `forward_calls` は定数です（中-3）。
9. ‖static‖ の許容は相対 1e-9 です（`boot_Bl3.py:334`・正本に無い値ですが害はありません）。
10. judge は secondary の組が無くても一致を出します（`analyze_Bl3.py:345`・掃き出しで止まります）。
11. 揺れの版は境を保つものだけを使います（`boot_Bl3.py:379`・今は三つとも使われます）。

## 6. 予想の照合（事前登録）

| 予想 | 信頼度 | 結果 |
|---|---|---|
| P1 中以上の所見がある | 0.8 | 当たり |
| P2 合成データの確かめが全て通る | 0.7 | 当たり |
| P3 自己検査が全て通る | 0.85 | 当たり |
| P4 判定は条件つき | 0.6 | 当たり |

四つとも当たったことは、予想の幅が広すぎた（安全すぎた）ことも示すので、較正の記録として残します。

## 7. 本検分が確認していないこと

- 実の重み・GPU・Colab の道は確かめていません（疎な取り出し、版の入れ直し、重みの SHA、非 DRY の相 pilot・main、`files.download`）。
- 計算の側の数値の正しさは確かめていません（読み取り・加減・書き換えの道の中身・芯の統計）。これは R1 の担当で、私は自己検査と合成の結果だけを見ました。
- 等方 1999 本での正式の記録は取っていません。私の走りは 9 本です。
- `bl3_facts`・`make_contrasts_Bl3` を再実行して、正本と設計事実が再生できるかは確かめていません。
- 凍結の本文はまだ無く、その組み上がりは確かめていません。
- 凍結の器の prepilot・main を、実のコミットで端から端まで走らせてはいません（関数の単体の試しだけです）。
- 書式をブラウザで押す道は走らせていません。
- opinions-tools の下の一つの入力の SHA16 は照らしていません。

## 検分票

- **対象**：流れと記録の側の器です。boot_Bl3・freeze_Bl3・seal_Bl3・make_predictions_form_Bl3・build_report_Bl3・sweep_Bl3・make_frozen_Bl3・bl3_facts・make_contrasts_Bl3・dry_run_Bl3 と、analyze_Bl3 の judge・open。版はコミット 87ce664 で、35 ファイルの SHA16 が一致しています。
- **段階**：事前登録あり（器を開く前の 13:43 JST に判定表・予想・COI）。依頼文は読んだ後です。
- **凍結物の同定**：層三はまだ凍結の前です。版の表と npz の SHA-256 を照らしました。段階 B と B-lens の凍結物は読み取りだけです。
- **盲検の状態**
  - R1 の所見・意見伺い・会話の記録は開いていません。これは申告であり、盲検の破れは測っていません。
  - 器の書き手の器の段の記録（§10-12 とその予想）と、boot-dry-3 の版の一行は読みました。引きずられた恐れがあります。
- **敵対的検分**
  - 封印の書き換え、judge の後の出力の差し替え、試みの逆順、守りの迂回、門の行だけの升目の外し、N1 の二升目の外し、止める下見を、実際に壊して走らせました。
  - 分母：見た器 11 本で、所見 22（重大 2・中 5・軽微 15）、是認 12 項。
  - 所見はすべて `ファイル:行` で一次の資料に照らしました。B-lens の照合があったことも実物の行で確かめました。
- **系統の内訳**：Claude 系（Opus 5.5）の新しい個体一体（R2）です。R1 とコーディネータも同系列で、相関した一票にすぎません。系統外の目は入っていません。
- **COI記録**
  - 事前に書いた引力は二つです。書き手の「通った」に合わせる引力と、軽微を重大に上げる引力です。
  - 途中で、同じ型の所見を他でも探して数を増やす引力が生じました。
  - 逆らう印として、所見ごとに行と試しを付け、中-4 は後で確かめられることを理由に重大から中に置き、是認を並べました。
- **判定**：条件つき（登録者裁定要）。
  - 重大-1・重大-2 を下見の前の凍結までに直し、自己検査を足すこと。
  - 直した後に、正式の合成データの記録と相 check を取り直すこと。
  - 中の五つについて、直すか限界として受けるかの裁定を受けること。
- **本検分が確認していないこと**：§7 のとおりです。実の重みと GPU の道、計算の側の数値、等方 1999 本の記録、凍結の本文の組み上がりを含みます。

本検分のいかなる記述も、AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。

私の走りの記録はすべて一時の置き場の `r2` の下にあります（`〈手元の一時の置き場〉\r2\`）。
- `prereg-R2.txt`（事前登録）
- `dry-run.md`（合成データの確かめの記録）
- `selftests.txt`・`selftest-rewrite.txt`（自己検査）
- `bootdry\`・`bootdry-log.txt`（起動器の DRY）
- `demo_*-out.txt`（壊して確かめた試し）
<<< 終: `records/reviews/Bl3/impl/r2/review.md` >>>

<<< 始: `records/reviews/Bl3/impl/checks/verification-impl-r1-Bl3.md`（SHA16 5FB347182D7514A2） >>>
# 器の実装の検分の R1 の票の事実の主張の確かめ（機械生成・`verify_impl_r1_Bl3.py`・検分の版 87ce664）

- 検分の版を git から一時の置き場に取り出して確かめた。値は芯の関数と乱数の小さな模型で出した（実の重みを読まない）。

| 番号 | 主張 | 確かめ | 結果 | 詳しく |
|---|---|---|---|---|
| M1 | 合成データの器の「本の計算は近道を使わない」の確かめは、器が書いた定数を見るだけで振る舞いを見ない | 引かれた行の文を検分の版で読み、近道の口を「いつも近道」に変えた写し（印の定数はそのまま）で本の計算の全体を乱数の小さな模型で走らせた | 再現した | 確かめの式の四つの項 True・本の器の定数と近道の口 True・写しで近道の元を作った回 17・そのときの確かめの式 True |
| m1 | 効き目が NaN のとき、割合は最小・裾は tie で Holm を通り、一致の関数は二つ目以降の鍵の NaN を読み飛ばして一致と答える | 芯の関数に NaN を与えた（検分の版） | 再現した | p_and_tail(NaN): p 0.001・上 0・下 0・裾 tie・Holm の判定 True／二つ目の鍵に NaN の一致 True（差の最大 0.0）／一つ目の鍵に NaN の一致 False（差の最大 nan） |
| m2 | 一致の段と開く段は、組の一つの DRY の印で全体を絞り、組 recompute の n_iso で正本を写し、組の間の環境の違いは記すだけで止めない | 検分の版の集計の器の行を読んだ | 再現した | DRY の判定の行 [343, 362, 410]・行を絞る行 [352, 367]・環境を記す関数 [323]・等方の本数の写し [331]・環境の違いで止める行 無い |
| m3 | 升目の入力の突き合わせは、プロンプトの長さ・主位置・読み取りの位置・族だけで、トークンの並びそのものは照らさない。起動器は tokenizers の版を固定しない | 検分の版の走らせる器・起動器・転記行 B の欄を読んだ | 再現した | 突き合わせの行 [333]・起動器の固定の行 ["want = {'numpy': PIN['numpy'], 'torch': PIN['torch'], 'transformers': PIN['transformers']}"]・転記行 B の升目の欄 ['a_not_catastrophe', 'catastrophe', 'choices', 'family', 'in_main', 'json_direct', 'main_position', 'n_ok', 'preamble_sha16', 'prompt_len', 'readout_position', 'template_overlap'] |
| m4 | 純粋な確かめは K.gate と blens_core.gate_perm を直に呼び、集計の器の gates・row_labels の組み立てを答えのある形で当てない | 検分の版の合成データの器の呼び出しを数えた | 再現した | K.gate の行 [99, 101, 117, 118]・gate_perm の行 [111, 112]・AZ.gates の直の呼び出し []・AZ.row_labels の直の呼び出し [] |
| m5 | 等方 9 本と 199 本では最小の p が Holm の第一段を下回れず、等方の外の行の枝に届かない | 芯の関数で、帰無のどれよりも大きい値の p を等方の本数ごとに出した | 再現した | Holm の第一段 0.003125（主の行 16）・最小の p: 等方 9 本 0.2000・199 本 0.0100・正本の本数 0.0010 |
| m6 | 方向の記録の名の並びと npz の行を位置で結び、名の並びを正本と転記行 D に照らさない（今の結び付きは正しい） | 検分の版の load_dirs の中に正本の名の並びとの照らしがあるかを見て、今の名の並びを正本と転記行 D と比べた | 再現した | load_dirs の中の正本の名との照らし 無い・名前のある方向の並びが正本と同じ True・実在の差の並びが転記行 D と同じ True |

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/reviews/Bl3/impl/checks/verification-impl-r1-Bl3.md` >>>

<<< 始: `records/reviews/Bl3/impl/checks/verification-impl-r2-Bl3.md`（SHA16 942B7C175C4715D4） >>>
# 器の実装の検分の R2 の票の事実の主張の確かめ（機械生成・`verify_impl_r2_Bl3.py`・検分の版 87ce664）

- 検分の版を git から一時の置き場に取り出して確かめた。実演は一時の置き場でだけ行った（実の重みを読まない・リポジトリの作業木に書かない）。

| 番号 | 主張 | 確かめ | 結果 | 詳しく |
|---|---|---|---|---|
| 重大-1 | 封印した予想の SHA-256 を、報告の組み立てでも起動器でも本の凍結でも照らさない（B-lens の器は照らしていた） | 検分の版の器で予想の SHA を読む行を探し、一時の置き場で封印の後にコーディネータの予想を書き換えて報告の入力を読み、報告を組んだ | 再現した | 予想の SHA を照らす行: {'tools/build_report_Bl3.py': False, 'tools/analyze_Bl3.py': False, 'tools/colab/boot_Bl3.py': False, 'tools/freeze_Bl3.py': False, 'tools/sweep_Bl3.py': False}・B-lens の require_sealed に照合 True・書き換えの前 通る → 後 通らない・止まった None・報告の q4 の行 ['｜ q4.gate ｜ 通らない ｜ 通らない（一致） ｜ 通らない（一致） ｜'] |
| 重大-2 | 一致だけを見る段の記録は読んだ出力の同定を持たず、結果を開く段は記録の agree だけを見て別に与えられた置き場を開き、開いた集計の再計算の一致が不一致でも止まらない | 検分の版の集計の器の judge と、main の open の枝を読んだ | 再現した | judge の中の SHA の同定 False・open の枝は記録の agree だけを見る True・open の枝で開いた集計の再計算の一致を見る False |
| 中-3 | 凍結の器は相 check のコミットの正本の SHA16 だけを照らし、器・設計事実・方向の記録を照らさない。順伝播を呼んだ数は定数 0。守りは最上位の forward の前だけで、模型の本体を直に呼べば止まらない | 検分の版の凍結の器の相 check の確かめと起動器の行を読み、乱数の小さな模型に同じ形の守りを掛けて最上位と本体を呼んだ | 再現した | 照らすのは正本の SHA16 だけ True・順伝播の数は定数 True・守りは最上位 True・最上位を呼ぶと止まる True・本体を呼ぶと止まる False |
| 中-4 | 本の凍結は下見の試みを与えた順に並べて最後を本の下見にし、session の正本と npz は写すだけで照らさず、試みのコミットの二つの記録は有るかだけを見る | 検分の版の凍結の器の本の凍結の確かめを読んだ | 再現した | 与えた順のまま True・有るかだけ True |
| 中-5 | 手元の一致だけを見る段・結果を開く段・報告の組み立ては、凍結の記録の SHA16 と手元の器と正本を照らさず、組の一つの DRY の印で全体を DRY として扱う | 検分の版の集計の器と報告の器を読んだ | 再現した | 凍結の記録の SHA16 を読む行が無い True・DRY は組の一つで全体 True |
| 中-6 | 合成データの器の端から端までは、器の関数を同じプロセスの中で呼び session を手で作る。起動器の三つの相と、ファイルを通した集計の器の CLI は、凍結の器が見る記録では走らない | 検分の版の合成データの器を読んだ | 再現した | 起動器の名が出てこない True・同じプロセスの端から端まで True・別のプロセスで走らせるもの ["r = subprocess.run([sys.executable, os.path.join(HERE, 'bl3_recompute_rewrite.py"] |
| 中-7 | 起動器は既定で三つの組を一度の起動で走らせ、組の出力は /content に置き、zip とダウンロードは最後の一度だけ | 検分の版の起動器を読んだ | 再現した | ダウンロードの行 [302]（終わりの関数の中だけ True）・組の既定は三つ True・置き場は /content True |
| 軽微8 | 報告の §1 の (v) の行は「近道を使う」と印字し、§4 は「本の計算の近道: いいえ」と印字して食い違う | 検分の版の報告の器の行を探した | 再現した | (v) の行 [130]・§4 の行 [263] |
| 軽微9 | 凍結の記録の rulings は `k >= 'D204'` の文字列の比べで、D58・D59・D75・D90 のような番号も入る | 正本の decisions に検分の版の式を当てた | 再現した | 入る 204 より前の番号 ['D58', 'D59', 'D75', 'D90'] |
| 軽微10 | 門の行だけの升目だけが (i)(ii) を満たさないとき、q1 は「続ける」のまま、読みの型〈下見で一部を外した〉が当たる | 芯の決定の関数に門の行だけの升目の不合格を与え、報告の器の読みの型を当てた | 再現した | q1 続ける・外した升目 ['S4｜Osec-Ncold']・当たった型に〈下見で一部を外した〉 True |
| 軽微11 | 「nuclear の族は測れなかった」は下見の記録の節（pilot_lines）にだけ置かれ、正本 pilot.decision.family の「報告の頭に」と違う | 検分の版の報告の器の行と正本の文を読んだ | 再現した | 行 [134]・正本の文に「報告の頭」 True |
| 軽微12 | 掃き出しは層ごとの差分の三つの鍵があるかだけを見て、行の中身と層の数を見ない | 検分の版の掃き出しの器を読んだ | 再現した | 鍵だけ True |
| 軽微13 | 凍結の本文の確かめ（rebuild_and_check）は引数 fsrc を受けるが使わず、凍結版の原稿と本文の食い違いを見ない | 検分の版の凍結の本文の器を読んだ | 再現した | 関数の本体に fsrc が出てこない True |
| 軽微14 | 凍結の一行の登録者の逐語に数（「10時」など）があると、組み立ての数の検査で止まる（日時の形だけなら止まらない） | 一時の置き場で、数を含まない言葉と含む言葉の二通りで凍結の本文を組んだ（組み立てより後の確かめは一時の置き場に git が無いので外れる） | 再現した | 数を含まない言葉: 終わりの値 1・組み立てで止まった False／「10時」を含む言葉: 終わりの値 1・組み立てで止まった True |
| 軽微15 | 起動器の版の確かめは正本 inputs.versions_B の三つだけを照らし、scipy を外す | 正本の版の欄と検分の版の起動器の行を読んだ | 再現した | 正本の欄 ['numpy', 'scipy', 'torch', 'transformers']・起動器の照らす行 ["want = {'numpy': PIN['numpy'], 'torch': PIN['torch'], 'transformers': PIN['transformers']}"] |
| 軽微16 | 凍結の器は正本 inputs.files の置き場を凍結物に入れるが、正本に書いた SHA16 と照らさない | 検分の版の凍結の器を読んだ | 再現した | 照らさない True |
| 軽微17 | 起動器の verify_frozen は、無いファイルを疎な取り出しの内外を問わず飛ばす | 検分の版の起動器を読んだ | 再現した | 内外を分けない True |
| 軽微18 | 集計の器の環境の比べは本の計算の組の間だけで、下見の session の GPU と比べない | 検分の版の集計の器を読んだ | 再現した | 比べる鍵 'commit', 'dry', 'gpu', 'versions', 'canon_sha16', 'directions_npz_sha256', 'layer_idx', 'coef' |
| 軽微19 | 登録者の予想の情報状態の欄（info.coi・free）が空でも、封印の検べを通る | 検分の版の封印の器の検べに、欄を空にした登録者の JSON を与えた | 再現した | 通った True |
| 軽微20 | 起動器は方向の npz の有無を確かめずに SHA-256 を取るので、npz が無いと止め方でなく予期しない誤りで落ちる | 検分の版の起動器を読んだ | 再現した | 行 ['npz_sha = sha256f(NPZ)']・有無の確かめ False |
| 軽微21 | 止めは記録の印と終わりだけで session.json を書かず、予期しない誤りの枝も session と zip を作らず、組の n_forward は起動の中の累積 | 検分の版の起動器を読んだ | 再現した | 止めで session を書かない True・予期しない誤りの枝で finish を呼ばない True・累積 True |
| 軽微22 | 正本の生成器の頭の文は v5 のまま（版は v7）・掃き出しの器の頭の文は無い REQUIRED を指す・設計事実を --skip-weights-hash で作り直すと転記行 F から断片が落ちる | 検分の版の三つの器を読んだ | 再現した | 生成器の頭 """make_contrasts_Bl3.py v5 —— B-lens 層三・版 ["VERSION = 'v7'"]・掃き出しの頭に REQUIRED True・断片を落とす引数の行 [362] |

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/reviews/Bl3/impl/checks/verification-impl-r2-Bl3.md` >>>

<<< 始: `records/reviews/Bl3/impl/provenance-impl-Bl3.md`（SHA16 F2925E657A569B40） >>>
# 器の実装の検分（二体）の票の出所（機械生成・`make_provenance_impl_Bl3.py`・2026-09-25 10:18 UTC）

- 検分者: 系統内の新しい個体二体（R1・R2）。コーディネータと同じ系列なので、二体で一つに数える（裁定 D59）。二体の一致は独立の確かめではない。
- 保全の仕方: 票の本文は、検分者の会話の記録の最後の本文から機械で切り出した（`save_vote_impl_Bl3.py`）。会話の記録に届いた終わりの通知の本文（XML の文字の置き換えを戻したもの）と突き合わせた。
- 票は書き換えない。所見の採否は、コーディネータが検分の版の一次の実物で再現してから決める（枠 §2・確かめの記録 `checks/`）。
- 起動の前の申告（`permission-impl-Bl3.md` の申告の段から機械で拾った行）: - 体数と機種: Claude Opus 5.5 の新しい個体が二体です。 ／ - 費用の見込み: 一体あたり 40〜60 万トークン程度で、二体で 80〜120 万トークン程度です。時間は一体あたり一〜二時間です。

| 札 | 担当 | 置き場 | 字数 | バイト | SHA-256 | SHA16（LF） | 機種の欄 | 通知の本文と | 使ったトークン | 道具の呼び出し | 時間（分） |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R1 | 計算の側 | `r1/review.md` | 14376 | 29686 | E10B0C6AE113BD90779FB2F9B6E393594B91E703B51D1790FE090AEAC4260168 | E10B0C6AE113BD90 | claude-opus-5-5 | 同じ | 610259 | 153 | 242 |
| R2 | 流れと記録の側 | `r2/review.md` | 15167 | 31845 | E7E5758CA84F0134BCA0BD4609116D4D70E707C7AE4EBB9A9DDC6288B2BB6BF9 | E7E5758CA84F0134 | claude-opus-5-5 | 同じ | 673951 | 216 | 295 |

## 検分者の一時の置き場から写した記録（バイトのまま・正本 review_plan.impl.focus の「その記録を残す」）

| 札 | 置き場 | 何か | バイト | SHA-256 |
|---|---|---|---|---|
| R1 | `r1/dry-run-R1.md` | 合成データの確かめの記録（検分者が走らせた・採否表 P694） | 30806 | 8F0C83A44EC3C37D1E75FDEF0D75F733F62DECDE5263DF15DF4AD4DE37C05163 |
| R1 | `r1/preregistration-R1.txt` | 検分者の事前登録（判定の規則・予想・COI） | 2334 | 995856304EA5A46C6A0185B4178882BDF9BBDD3F4A5CADFEB4D1C56619FA2D30 |
| R2 | `r2/dry-run-R2.md` | 合成データの確かめの記録（検分者が走らせた・採否表 P694） | 30806 | 73FABCD452EDB23A2E5C8E53AA95ACD6F3CEF52F13DC53ED48403AF893CBC2BA |
| R2 | `r2/prereg-R2.txt` | 検分者の事前登録（判定表・予想・COI） | 2210 | 87F95E83EA28441F4540D8CF3D75890D2EB9416960A1D58A00C4ED48C56869FF |

- 検分者の一時の置き場の残り（試しの台本・試しの出力・起動器の DRY の出力）は写していない（票に置き場の名が書かれている・一時の置き場は後で消えうる）。

## 検分者の作業場（票を受け取った後に確かめた）

- R1: 作業場のコミット 87ce664・追跡しているファイルの差分 無し・追跡の外のファイル `results/Bl3/directions-Bl3.json`・`results/Bl3/directions-Bl3.npz`
- R2: 作業場のコミット 87ce664・追跡しているファイルの差分 無し・追跡の外のファイル `results/Bl3/directions-Bl3.json`・`results/Bl3/directions-Bl3.npz`

## コーディネータが起動の後に検分者へ送った知らせ（逐語・会話の記録の SendMessage の呼び出しから）

- R1 へ（2026-09-25 13:39 日本時間）:

````text
依頼文は作業場のファイルとしては置けていませんでした（あなたの作業場はコミット 87ce664 から作られ、依頼文はその後のコミット c1a759c にあります）。作業場は同じリポジトリの git を共有しているので、作業場で `git show c1a759c:records/reviews/Bl3/impl/request-impl-Bl3-R1.md` を打てば依頼文の全文が出ます。まずそれを全部読み、そのとおりに進めてください（依頼文の「検分の版」の器はコミット 87ce664 と同じで、あなたの作業場の版です）。依頼文を読む前に何かを始めていたら、それも返答に書いてください。コミット c1a759c そのものに作業場を切り替えないでください（`git show` で読むだけにしてください）。
````

- R2 へ（2026-09-25 13:39 日本時間）:

````text
依頼文は作業場のファイルとしては置けていませんでした（あなたの作業場はコミット 87ce664 から作られ、依頼文はその後のコミット c1a759c にあります）。作業場は同じリポジトリの git を共有しているので、作業場で `git show c1a759c:records/reviews/Bl3/impl/request-impl-Bl3-R2.md` を打てば依頼文の全文が出ます。まずそれを全部読み、そのとおりに進めてください（依頼文の「検分の版」の器はコミット 87ce664 と同じで、あなたの作業場の版です）。依頼文を読む前に何かを始めていたら、それも返答に書いてください。コミット c1a759c そのものに作業場を切り替えないでください（`git show` で読むだけにしてください）。
````

- R2 へ（2026-09-25 17:35 日本時間）:

````text
走らせ方についてのお知らせです（所見の中身には関わりません）。14:53 ごろに一時の置き場から始まった driver（bootdry_driver.py）と、それが 14:54 に始めた `tools/colab/boot_Bl3.py`（あなたの作業場のもの）が、17:30 の時点でまだ残っていて、boot_Bl3.py は二時間半で CPU を 49 秒しか使っていません。何かを待って止まっている（たとえば、出力を読まない管に書いて塞がった）見込みがあります。あなたが待っているものなら、確かめて、必要なら止めて、出力をファイルに書く形で走らせ直してください。止めた・走らせ直したことは返答に書いてください。
````

## 注（事実のみ）

- 検分者の作業場は、ローカルの依頼のコミット c1a759c ではなく push 済みの 87ce664 から作られ、依頼文は作業場に無かった。起動の直後に、二体へ `git show c1a759c:…` で依頼文を読むよう知らせた（上の一つ目の知らせ）。二体とも票の冒頭に、依頼文を読む前にしたことを書いている。
- R2 へ二つ目の知らせで「起動器の DRY が止まっているように見える」と伝えたのは、コーディネータの読み違いだった。R2 の票によれば、R2 が外の走りを遅くしないよう優先度を下げて走らせていたためで、塞がってはいなかった（R2 は止めも走らせ直しもしていない）。
- 二体とも、使った量が起動の前の申告（一体あたりのトークンと時間）を超えた。同じ機械でコーディネータの正式の合成データの記録と並べて走らせたため、合成データの確かめの走りが長くなった。
- 二体の一時の置き場は、同じ一時の置き場の根の下（r1・r2）にあった。二体とも、もう一体の置き場は開いていないと票に書いている（申告）。
- 起動の時に、検分者の文脈には利用者の記憶の索引（計画の工程の要約を含む）が環境から入っていた（R1 の票の開示）。R1 は git の履歴で枠のコミットの件名の一行を見たと開示している（中身は開いていないと申告）。R2 は器の段の記録（§10〜§12・正式の記録の予想を含む）を読んだと開示している。
- 検分者の内部の番号は、この記録に書かない（検分者の会話の記録を探すためだけに使った）。
- 二体の票の本文の終わりには、検分者の一時の置き場の道筋（利用者の名と会話の番号を含む）が書かれている。票は逐語で保全するので、そのまま残した（秘密の値ではない）。

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
<<< 終: `records/reviews/Bl3/impl/provenance-impl-Bl3.md` >>>

==================== 第四部 検分の版 87ce664 からの差分（git diff） ====================

<<< 始: `tools/bl3_core.py` の差分（87ce664 → b77dd22・git diff・囲みは四つの backtick） >>>
````diff
diff --git a/tools/bl3_core.py b/tools/bl3_core.py
index f4d1627..761f3fe 100644
--- a/tools/bl3_core.py
+++ b/tools/bl3_core.py
@@ -1,5 +1,5 @@
 # -*- coding: utf-8 -*-
-"""bl3_core.py v2 —— B-lens 層三（Bl3）の計算の芯（numpy だけ・重みも試行も読まない・2026-09-25）。
+"""bl3_core.py v3 —— B-lens 層三（Bl3）の計算の芯（numpy だけ・重みも試行も読まない・2026-09-25）。
 
 正本 `design/contrasts-Bl3.json` の決まりを、重みや試行を読まない純粋な関数に置く。走らせる器 `tools/bl3_run.py`・集計の器 `tools/analyze_Bl3.py`・
 合成データの器 `tools/dry_run_Bl3.py` が同じ関数を呼ぶ（同じ式を二度書かない）。B-lens の芯 `tools/blens_core.py` の関数は読み取りだけで呼ぶ。
@@ -24,7 +24,7 @@ HERE = os.path.dirname(os.path.abspath(__file__))
 sys.path.insert(0, HERE)
 import blens_core as C
 
-VERSION = 'v2'          # v2（2026-09-25・裁定 D231）: 比べる相手の除き方の錨（B-lens の凍結の OWN_PAIR と正本の兄弟の対）
+VERSION = 'v3'          # v3（2026-09-25・裁定 D236）: 有限でない値で割合と裾は止め、一致の関数は一致しないと答える／v2（裁定 D231）: 比べる相手の除き方の錨
 NOOP, PAD = 'noop', 'pad'
 
 
@@ -57,6 +57,8 @@ def mass_of_set(Z_full, set_ids):
 def p_and_tail(m, null):
     """両側に等しい裾の割合（`blens_core.p_equal_tailed`）と、上の裾と下の裾の本数・割合を決めた裾（上・下・同じ）。"""
     null = np.asarray(null, dtype=np.float64)
+    if not (np.isfinite(m) and np.all(np.isfinite(null))):
+        raise ValueError('有限でない値に割合と裾を当てようとした（裁定 D236）')
     up, lo = int(np.sum(null >= m)), int(np.sum(null <= m))
     tail = 'upper' if up < lo else ('lower' if lo < up else 'tie')
     return {'p': C.p_equal_tailed(m, null), 'upper': up, 'lower': lo, 'tail': tail, 'K': int(len(null))}
@@ -240,11 +242,16 @@ def q1_from_attempts(attempts):
 
 # ---------------- 独立の再計算の一致 ----------------
 def agreement(eff_a, eff_b, tol, labels_a, labels_b):
-    """段ごとの一致（正本 `independent_recompute.agreement`）: 全ての効き目の差の絶対値が許容の内で、二つの道の値からそれぞれ出した札が同じ。"""
+    """段ごとの一致（正本 `independent_recompute.agreement`）: 全ての効き目の差の絶対値が許容の内で、二つの道の値からそれぞれ出した札が同じ。
+    有限でない値が一つでもあれば一致しない（鍵の順に依らない・`non_finite` に鍵を並べる・裁定 D236）。"""
     keys = sorted(eff_a)
     if sorted(eff_b) != keys:
         return {'agree': False, 'reason': 'keys', 'values_within_tol': False, 'labels_same': False, 'max_abs_diff': None,
                 'missing': sorted(set(eff_a) ^ set(eff_b))}
+    finite = lambda v: bool(np.all(np.isfinite(np.asarray(v, dtype=np.float64))))
+    nf = [k for k in keys if not (finite(eff_a[k]) and finite(eff_b[k]))]
+    if nf:
+        return {'agree': False, 'reason': 'non_finite', 'values_within_tol': False, 'labels_same': False, 'max_abs_diff': None, 'non_finite': nf}
     dmax = max(float(np.max(np.abs(np.asarray(eff_a[k], dtype=np.float64) - np.asarray(eff_b[k], dtype=np.float64)))) for k in keys)
     same = labels_a == labels_b
     return {'agree': bool(dmax <= tol and same), 'values_within_tol': bool(dmax <= tol), 'labels_same': bool(same), 'max_abs_diff': dmax}
@@ -383,6 +390,17 @@ def _selftest():
     assert agreement(a_, {'r': [0.1005, 0.2]}, 0.001, {'x': 1}, {'x': 1})['agree']
     assert not agreement(a_, {'r': [0.1005, 0.2]}, 0.0001, {'x': 1}, {'x': 1})['agree']
     assert not agreement(a_, a_, 0.001, {'x': 1}, {'x': 2})['agree']
+    # 有限でない値（裁定 D236）: 割合と裾は止まり、一致の関数は鍵の順に依らず一致しない
+    for bad_ in (float('nan'), float('inf')):
+        try:
+            p_and_tail(bad_, null)
+            raise AssertionError('有限でない値の割合を通した')
+        except ValueError:
+            pass
+    for order_ in (('a', 'b'), ('b', 'a')):
+        ea = {order_[0]: [0.0], order_[1]: [float('nan')]}
+        nf_ = agreement(ea, {'a': [0.0], 'b': [0.0]}, 0.001, {}, {})
+        assert not nf_['agree'] and nf_['reason'] == 'non_finite' and nf_['non_finite'] == [order_[1]], nf_
     # 予想の採点
     assert bucket(0, ['零', '一から三', '四以上']) == '零' and bucket(3, ['零', '一から三', '四以上']) == '一から三' and bucket(4, ['零', '一から三', '四以上']) == '四以上'
     assert bucket(2, ['零', '一か二', '三以上']) == '一か二' and bucket(3, ['零', '一か二', '三以上']) == '三以上'
````
<<< 終: `tools/bl3_core.py` の差分 >>>

<<< 始: `tools/bl3_run.py` の差分（87ce664 → b77dd22・git diff・囲みは四つの backtick） >>>
````diff
diff --git a/tools/bl3_run.py b/tools/bl3_run.py
index 3b1fafd..d1e502d 100644
--- a/tools/bl3_run.py
+++ b/tools/bl3_run.py
@@ -1,5 +1,5 @@
 # -*- coding: utf-8 -*-
-"""bl3_run.py v2 —— B-lens 層三（Bl3）の教師強制の順伝播を走らせる器（2026-09-25・正本 `readout.primary`・`pilot`・`computation`・`descriptive`）。
+"""bl3_run.py v3 —— B-lens 層三（Bl3）の教師強制の順伝播を走らせる器（2026-09-25・正本 `readout.primary`・`pilot`・`computation`・`descriptive`）。
 
 走らせ方（正本のとおり・値は器の出力に置き、読みは付けない）:
   - 入力: 段階 B の組み立てのままのプロンプト（凍結の `steer_B.apply_chat`・`run_stageB_local.user_message`）の直後に、主の書き出し（設計事実の転記行 A の
@@ -17,14 +17,14 @@
 DRY（乱数の小さな模型）の確かめのために、壊した読み取り（二重の正規化）と壊した近道（主位置まで使い回す）を、引数 `bug` で入れられる（本の計算では入れない）。
 柵: 本器の出力のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
 """
-import os, sys, json, math, time, collections
+import os, sys, json, math, time, hashlib, collections
 import numpy as np
 
 HERE = os.path.dirname(os.path.abspath(__file__))
 sys.path.insert(0, HERE)
 import bl3_core as K
 
-VERSION = 'v2'          # v2（2026-09-25・裁定 D231・D234）: 本の計算は近道を使わない・層ごとの余弦は足した向きで・使い回す cache の実の長さの確かめ・乙の文脈のファイルはちょうど一つ・(iii) の定義を下見の記録に
+VERSION = 'v3'          # v3（2026-09-25・裁定 D236）: 出口の値が有限でなければ止める・方向の名の並びを正本と転記行 D に照らす・升目のトークンの並びの SHA16／v2（裁定 D231・D234）: 本の計算は近道を使わない ほか
 BUGS = (None, 'double_norm', 'cache_through_mp')
 
 
@@ -32,6 +32,18 @@ class ToolError(Exception):
     """凍結した確かめが機械で落ちた（器の誤り・正本 `pilot.decision.tool_error.what`）。"""
 
 
+def require_finite(vals, where):
+    """出口の値がすべて有限であること（有限でなければ器の誤りで止める・裁定 D236）。vals: 名 → 値。"""
+    bad = sorted(k for k, v in vals.items() if not math.isfinite(float(v)))
+    if bad:
+        raise ToolError('有限でない値（%s・%d 個・例 %s）' % (where, len(bad), bad[:3]))
+
+
+def ids_sha16(cell):
+    """升目の入力のトークンの並び（プロンプト ＋ 主の書き出し）の SHA16（相 check が書き、凍結の器が手元の組み立てと照らす・裁定 D236）。"""
+    return hashlib.sha256(','.join(str(int(x)) for x in cell.ids).encode('ascii')).hexdigest().upper()[:16]
+
+
 class Cell:
     """一つの升目（場面 × 土台の腕）の入力と位置。"""
 
@@ -292,6 +304,9 @@ def run_cell_sign(R, cell, sign, dir_ids, batch, seed, key_index, pc=None, layer
                     lay['rows'][d] = vals
                 else:
                     lay['iso'][d] = vals
+    require_finite(lo, '升目と符号 %s|%+d の対数オッズ' % (cell.key, sign))
+    require_finite(mass, '升目と符号 %s|%+d の質量' % (cell.key, sign))
+    require_finite(pa, '升目と符号 %s|%+d の集合の中の確率' % (cell.key, sign))
     eff = {d: lo[d] - lo[K.NOOP] for d in lo if d != K.NOOP}
     return {'lo': lo, 'effects': eff, 'mass': mass, 'pa_noop': pa[K.NOOP], 'layers': lay, 'n_batches': len(plan)}
 
@@ -307,6 +322,7 @@ def recompute_hook_path(R, rows, dirs_by_row, log=None):
         eff = {}
         for did, sg in dirs_by_row[name]:
             eff['%s|%+d' % (did, sg)] = float(R.forward(cell, [did], sg, full=False)['lo'][0]) - base
+        require_finite(dict(eff, noop=base), '独立の再計算のフックの道の行 %s' % name)
         out[name] = {'noop_lo': base, 'effects': eff}
         if log:
             log('[bl3_run] 独立の再計算のフックの道 %s（%d/%d・順伝播 %d）・%.0f 秒' % (name, i + 1, len(rows), 1 + len(eff), time.time() - t0))
@@ -336,12 +352,17 @@ def build_cells(tok, T3, FJ, keys):
     return out
 
 
-def load_dirs(npz_path, json_path):
-    """方向の npz（`tools/bl3_directions.py`）を名で引ける形にする。SHA-256 は記録と突き合わせる（違えば止める）。"""
+def load_dirs(npz_path, json_path, T3=None, FJ=None):
+    """方向の npz（`tools/bl3_directions.py`）を名で引ける形にする。SHA-256 は記録と突き合わせる（違えば止める）。
+    T3 と FJ を与えると、名前のある方向の名の並びが正本と、実在の差の名の並びが転記行 D と同じことも確かめる（名と行を位置で結ぶので・裁定 D236）。"""
     import hashlib
     J = json.load(open(json_path, encoding='utf-8'))
     if hashlib.sha256(open(npz_path, 'rb').read()).hexdigest().upper() != J['npz_sha256']:
         raise ToolError('方向の npz の SHA-256 が記録と違う')
+    if T3 is not None and list(J['groups']['named']['names']) != list(T3['directions']['named']):
+        raise ToolError('方向の記録の名前のある方向の名の並びが正本と違う')
+    if FJ is not None and list(J['groups']['real']['names']) != list(FJ['facts']['D']['real_pairs']):
+        raise ToolError('方向の記録の実在の差の名の並びが転記行 D と違う')
     Z = np.load(npz_path)
     names = {'named': J['groups']['named']['names'], 'B_random': J['groups']['B_random']['names'],
              'iso': ['iso:%d' % i for i in range(J['groups']['iso']['count'])], 'real': ['real:' + p for p in J['groups']['real']['names']], 'check': ['check']}
@@ -414,6 +435,7 @@ def run_secondary(R, contexts, rows_by_cell, log=None):
             r = R.forward(cell, [K.NOOP] + [d for _, d in items], sg, full=False)
             for k_, (name, did) in enumerate(items, start=1):
                 res[name] = {'dlo': float(r['lo'][k_] - r['lo'][0]), 'dz_a': float(r['Zset'][k_, 0] - r['Zset'][0, 0]), 'dz_c': float(r['Zset'][k_, i_c] - r['Zset'][0, i_c])}
+        require_finite({'%s|%s' % (n_, k_): v_ for n_, d_ in res.items() for k_, v_ in d_.items()}, '乙の文脈 %s %s' % (key, tid))
         out.append(dict(rec, rows=res, n_batches=len(by_sign)))
     return out
 
````
<<< 終: `tools/bl3_run.py` の差分 >>>

<<< 始: `tools/colab/boot_Bl3.py` の差分（87ce664 → b77dd22・git diff・囲みは四つの backtick） >>>
````diff
diff --git a/tools/colab/boot_Bl3.py b/tools/colab/boot_Bl3.py
index 531ef16..a6fb733 100644
--- a/tools/colab/boot_Bl3.py
+++ b/tools/colab/boot_Bl3.py
@@ -1,5 +1,5 @@
 # -*- coding: utf-8 -*-
-"""boot_Bl3.py v2 —— B-lens 層三（Bl3）の Colab 起動スクリプト（教師強制の順伝播・2026-09-25・正本 `readout`・`pilot`・`computation`・`independent_recompute`）。
+"""boot_Bl3.py v3 —— B-lens 層三（Bl3）の Colab 起動スクリプト（教師強制の順伝播・2026-09-25・正本 `readout`・`pilot`・`computation`・`independent_recompute`）。
 
 相（OP4B_PHASE）:
   check  封印の前の確かめ（正本 `computation.before_seal`: 読み込みと版の確かめだけ・**順伝播を一度も走らせない・値を出さない**。模型の順伝播の前の hook で、呼ばれたら止める）:
@@ -26,11 +26,15 @@ DRY（手元の検査・OP4B_DRY=1）: 乱数の小さな模型（`tools/dry_run
   三つの相を CPU で通す。OP4B_REPO_DIR・OP4B_OUT が要る。版・GPU・重み・凍結と封印の記録は見ない（印を残す）。方向の npz の確かめは手元の npz で行う。
   相 main は OP4B_DRY_PILOT（相 pilot の出力の pilot.json）を読む。OP4B_DRY_ISO で等方の本数（既定 9）、OP4B_DRY_SEC で乙の文脈の数（既定 2）、
   OP4B_DRY_RC で独立の再計算の v̂ の行の数（既定 2・減算の行と加算の行を一つずつから）を減らす。残差の書き換えの器が無ければ、DRY に限り印を残して飛ばす。
+v3 の決め（裁定 D236）: 手順の順は「方向の npz と器を push → 相 check → 下見の前の凍結の記帳」（npz が取り出しに無ければ止める）。版は numpy・torch・transformers の三つを
+  文字列で照らす（計算の道は scipy を読まないので照らさない・session に並べる・裁定 D237）。相 check の守りは模型・模型の本体・語彙の行列・各層の前の hook で、呼ばれた数を数えて
+  書く（守りの止めは Exception の外の型）。相 pilot と main は、そのコミットの二つの予想の SHA-256 を封印の記録と照らす。相 main は組ごとに出力の SHA-256 を session に書き、
+  組ごとに zip を作って落とす（落ちた組から走らせ直せるように）。止めと予期しない誤りでも session を書いて zip を作る。
 柵: 本スクリプトの出力は器物の出力であり AI の自己報告ではない。いかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
 """
 import os, sys, re, json, time, glob, shutil, hashlib, datetime, traceback, subprocess, zipfile, collections
 
-VERSION = 'v2'          # v2（2026-09-25・裁定 D231・D234）: 本の計算は近道を使わない・相 check に比べる相手の除き方の錨と ‖static‖ の確かめ・相 main で本の凍結の下見と試みの最後を照らす
+VERSION = 'v3'          # v3（2026-09-25・裁定 D236）: 封印した予想の照らし・組の出力の SHA-256・組ごとの zip・止めと誤りの記録・相 check の守りと数・トークンの並びの SHA16 ほか／v2（裁定 D231・D234）
 T0 = time.time()
 REPO_URL = 'https://github.com/YutaKusumi/ontology-preamble-4b.git'
 PHASES = ('check', 'pilot', 'main')
@@ -38,13 +42,14 @@ PARTS = ('main', 'recompute', 'secondary')
 SPARSE = ['tools', 'arms', 'design', 'records', 'results/Bl3', 'results/stageB']
 LOG = []
 PROGRESS = {'path': None}
+CTX = {'od': None, 'session': None, 'dry': False}          # 止めと誤りでも記録を置くため（裁定 D236）
 CLAUSE = '本記録は器物の出力であり AI の自己報告ではない。いかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。'
 now = lambda: datetime.datetime.now(datetime.timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')
 sha16f = lambda p: hashlib.sha256(open(p, 'rb').read().replace(b'\r\n', b'\n')).hexdigest().upper()[:16]
 
 
-class Stop(Exception):
-    pass
+class Stop(BaseException):
+    """相 check の守りの止め（Exception の外の型で、`except Exception` に呑まれない・裁定 D236）。"""
 
 
 def sha256f(p):
@@ -75,8 +80,33 @@ def sh(cmd, check=True):
     return r
 
 
+def package(tag, extra=None):
+    """置き場の中身を zip にして落とす（組ごと・終わり・止め・誤り・裁定 D236）。session を書いてから zip にし、zip の SHA-256 を session と進みの印字に記す。"""
+    od, S = CTX['od'], CTX['session']
+    if not od or S is None:
+        return None
+    S.update(extra or {})
+    S.update({'log': LOG, 'packaged': tag, 'packaged_at': now(), 'seconds': round(time.time() - T0, 1), 'clause': CLAUSE})
+    write_json(os.path.join(od, 'session.json'), S)
+    zp = od + ('.zip' if tag == 'final' else '-%s.zip' % tag)
+    with zipfile.ZipFile(zp, 'w', zipfile.ZIP_DEFLATED) as z:
+        for fn in sorted(os.listdir(od)):
+            z.write(os.path.join(od, fn), os.path.join(os.path.basename(od), fn))
+    zsha = sha256f(zp)
+    S.setdefault('zips', []).append({'tag': tag, 'zip': os.path.basename(zp), 'sha256': zsha})
+    mark('packaged', tag=tag, zip=os.path.basename(zp), sha256=zsha)
+    if not CTX['dry']:
+        try:
+            from google.colab import files
+            files.download(zp)
+        except Exception as e_:
+            print('[boot_Bl3] zip の自動のダウンロードが走らなかった（左の「ファイル」から落とす）: %s' % e_)
+    return zp
+
+
 def stop(msg):
     mark('stop', reason=msg)
+    package('stopped', {'stopped': msg})               # 止めでも session を書いて zip を作る（裁定 D236）
     sys.exit('[boot_Bl3] 止める（登録者に相談）: ' + msg)
 
 
@@ -95,12 +125,16 @@ def write_json(path, obj):
 
 
 def verify_frozen(repo, sha_map):
-    """凍結の記録の SHA16（改行を LF にそろえた SHA-256 の頭 16 桁）を、取り出した作業木のファイルに照らす。疎な取り出しで無いファイルは飛ばして数を記す。"""
+    """凍結の記録の SHA16（改行を LF にそろえた SHA-256 の頭 16 桁）を、取り出した作業木のファイルに照らす。疎な取り出しの外のファイルは飛ばして数を記し、
+    内側で無いファイルは外れにする（裁定 D236）。"""
     bad, skipped = [], []
     for rp, want in sha_map.items():
         p = os.path.join(repo, *rp.split('/'))
         if not os.path.exists(p):
-            skipped.append(rp)
+            if any(rp == d or rp.startswith(d + '/') for d in SPARSE):
+                bad.append({'path': rp, 'got': None, 'want': want})
+            else:
+                skipped.append(rp)
             continue
         got = sha16f(p)
         if got != want:
@@ -154,6 +188,7 @@ def run():
     od = os.path.join(OUTROOT, '%s-%s' % (PHASE, stamp))
     os.makedirs(od, exist_ok=True)
     PROGRESS['path'] = os.path.join(od, 'progress.log')
+    CTX.update(od=od, dry=DRY, session={'kind': 'bl3_colab_%s' % PHASE, 'boot': VERSION, 'commit': COMMIT, 'dry': DRY})
     CANON = os.path.join(REPO, 'design', 'contrasts-Bl3.json')
     T3 = json.load(open(CANON, encoding='utf-8'))
     FJ = json.load(open(os.path.join(REPO, 'records', 'Bl3', 'design-facts-Bl3.json'), encoding='utf-8'))
@@ -169,6 +204,11 @@ def run():
                 if not os.path.exists(p):
                     stop('相 %s は下見の前の凍結と封印の後に走らせる（%s が無い・正本 predictions.when）' % (PHASE, os.path.basename(p)))
             FR = json.load(open(FRP, encoding='utf-8'))
+            SR = json.load(open(SRP, encoding='utf-8'))
+            for role in ('coordinator', 'registrant'):                 # 封印した予想の SHA-256 を封印の記録と照らす（裁定 D236）
+                pp = os.path.join(REPO, *SR['predictions'][role]['path'].split('/'))
+                if not os.path.exists(pp) or sha256f(pp) != SR['predictions'][role]['sha256']:
+                    stop('封印した予想の JSON が無いか、SHA-256 が封印の記録と違う: %s' % role)
             if PHASE == 'main' and 'main_freeze' not in FR:
                 stop('相 main は本の凍結の後に走らせる（凍結の記録に本の凍結が無い）')
             sha_map = FR['main_freeze']['frozen_sha16'] if PHASE == 'main' else FR['frozen_sha16']
@@ -230,6 +270,11 @@ def run():
         os.environ['HF_HUB_DISABLE_XET'] = '1'
         from huggingface_hub import snapshot_download
         SNAPDIR = snapshot_download(M['repo'], revision=M['rev'])
+        idx = json.load(open(os.path.join(SNAPDIR, 'model.safetensors.index.json'), encoding='utf-8'))
+        need_f = ['config.json', 'tokenizer.json', 'model.safetensors.index.json'] + sorted(set(idx['weight_map'].values()))
+        miss_f = [x for x in need_f if x not in FJ['facts']['F']['sha256']]
+        if miss_f:
+            stop('転記行 F に、重みの索引の断片か設定のファイルが欠けている: %s' % miss_f)      # 裁定 D236
         W_SHA = {fn: sha256f(os.path.join(SNAPDIR, fn)) for fn in FJ['facts']['F']['sha256']}
         badw = [fn for fn, sha in FJ['facts']['F']['sha256'].items() if W_SHA[fn] != sha]
         if badw:
@@ -239,6 +284,8 @@ def run():
     # ---- 4. 方向の npz（方向の記録の SHA-256・組ごとの SHA-256 を転記行 D と・Colab で乱数を引き直さない）
     NPZ = os.path.join(REPO, 'results', 'Bl3', 'directions-Bl3.npz')
     DJP = os.path.join(REPO, 'results', 'Bl3', 'directions-Bl3.json')
+    if not os.path.exists(NPZ) or not os.path.exists(DJP):
+        stop('方向の npz か方向の記録が取り出しに無い（npz と器を push してから相 check を走らせる・裁定 D236）')
     DJ = json.load(open(DJP, encoding='utf-8'))
     npz_sha = sha256f(NPZ)
     if npz_sha != DJ['npz_sha256']:
@@ -248,7 +295,7 @@ def run():
     Zd = np.load(NPZ)
     try:
         grp = BD.verify_against_facts({g: Zd[g] for g in BD.GROUPS}, FJ)
-        dirs_real, names_real = BR.load_dirs(NPZ, DJP)
+        dirs_real, names_real = BR.load_dirs(NPZ, DJP, T3, FJ)             # 名の並びを正本と転記行 D に照らす（裁定 D236）
     except (SystemExit, BR.ToolError) as e_:
         stop(str(e_))
     pair_names = list(DJ['groups']['real']['names'])
@@ -285,28 +332,24 @@ def run():
 
     SESSION = {'kind': 'bl3_colab_%s' % PHASE, 'boot': VERSION, 'commit': COMMIT, 'dry': DRY, 'gpu': GPU, 'versions': VER, 'weights_sha256': W_SHA,
                'canon_sha16': sha16f(CANON), 'directions_npz_sha256': npz_sha, 'directions_group_sha256': grp, 'frozen_check': frozen, 'layer_idx': L, 'coef': coef}
+    CTX['session'] = SESSION
 
     def finish(extra=None):
-        SESSION.update(extra or {})
-        SESSION.update({'log': LOG, 'finished': now(), 'seconds': round(time.time() - T0, 1), 'clause': CLAUSE})
-        write_json(os.path.join(od, 'session.json'), SESSION)
-        zp = od + '.zip'
-        with zipfile.ZipFile(zp, 'w', zipfile.ZIP_DEFLATED) as z:
-            for fn in sorted(os.listdir(od)):
-                z.write(os.path.join(od, fn), os.path.join(os.path.basename(od), fn))
-        zsha = sha256f(zp)
-        mark('done', zip=os.path.basename(zp), sha256=zsha)
-        if not DRY:
-            try:
-                from google.colab import files
-                files.download(zp)
-            except Exception as e_:
-                print('[boot_Bl3] zip の自動のダウンロードが走らなかった（左の「ファイル」から落とす）: %s' % e_)
+        package('final', dict(extra or {}, finished=now()))
+        mark('done', zips=len(SESSION.get('zips') or []))
         return od
 
     # ---- 7. 相 check（順伝播を一度も走らせない）
     if PHASE == 'check':
-        guard = model.register_forward_pre_hook(lambda m, a: (_ for _ in ()).throw(Stop('相 check で順伝播が呼ばれた（正本 computation.before_seal）')))
+        calls = collections.Counter()
+
+        def guard_of(name):
+            def h(m, a):
+                calls[name] += 1
+                raise Stop('相 check で順伝播が呼ばれた（%s・正本 computation.before_seal）' % name)
+            return h
+        guards = [model.register_forward_pre_hook(guard_of('model')), model.model.register_forward_pre_hook(guard_of('model.model')),
+                  model.lm_head.register_forward_pre_hook(guard_of('lm_head'))] + [l_.register_forward_pre_hook(guard_of('layers.%d' % i_)) for i_, l_ in enumerate(model.model.layers)]
         E = FJ['facts']['E']
         sets = BR.cell_sign_sets(T3, None, names_real['named'], names_real['B_random'], names_real['iso'], names_real['real'], gate_only)
         n_pass = sum(len(s[3]) for s in sets)
@@ -351,11 +394,13 @@ def run():
             rw_ok = False
         if not rw_ok and not DRY:
             stop('残差の書き換えの器（tools/bl3_recompute_rewrite.py の recompute_rewrite）を import できない')
-        guard.remove()
-        chk = {'cells': {k: {'prompt_len': len(c.prompt), 'main_position': c.mp, 'readout_position': c.ro, 'family': c.fam, 'set_ids': c.set_ids} for k, c in cells.items()},
+        for g_ in guards:
+            g_.remove()
+        chk = {'cells': {k: {'prompt_len': len(c.prompt), 'main_position': c.mp, 'readout_position': c.ro, 'family': c.fam, 'set_ids': c.set_ids, 'ids_sha16': BR.ids_sha16(c)} for k, c in cells.items()},
                'cell_signs': len(sets), 'passes': n_pass, 'recompute_rows': len(rows_rc), 'recompute_passes_per_path': n_rc, 'gate_rows': len(rows_gate),
                'secondary': dict(cnt, cells=len(sec_rows), letter_token_is_L=sum(1 for c in ctx if c[3]['letter_token_is_L']), max_ids=max(c[3]['n_ids'] for c in ctx)),
-               'hook_register_remove': True, 'rewrite_importable': rw_ok, 'forward_calls': 0, 'comparator_anchor': anchor, 'static_norm_matches_fact_D': True}
+               'hook_register_remove': True, 'rewrite_importable': rw_ok, 'forward_calls': sum(calls.values()), 'forward_guards': len(guards),
+               'comparator_anchor': anchor, 'static_norm_matches_fact_D': True}
         write_json(os.path.join(od, 'check.json'), chk)
         mark('check', cell_signs=len(sets), passes=n_pass, recompute_rows=len(rows_rc), gate_rows=len(rows_gate), secondary_contexts=len(ctx), rewrite_importable=rw_ok)
         return finish()
@@ -407,6 +452,7 @@ def run():
     for part in parts:
         mark('part', part=part)
         out = collections.OrderedDict(part=part, clause=CLAUSE)
+        n0 = R.n_forward
         try:
             if part != 'main':                       # どの組も頭で出口の値の自己検査（本の計算の組は run_main_phase の頭で行う）
                 lc = R.logit_check(first_cell, T3['computation']['logit_tol'])
@@ -459,11 +505,13 @@ def run():
                 mark('secondary', contexts=len(ctx), seconds=round(time.time() - t1, 1))
         except TOOL_ERR as e_:
             out['tool_error'] = str(e_)
-        out['n_forward'] = R.n_forward
+        out['n_forward'], out['n_forward_total'] = R.n_forward - n0, R.n_forward      # 組の順伝播の数と、起動の中の累積（裁定 D236）
         write_json(os.path.join(od, '%s.json' % part), out)
+        SESSION.setdefault('part_sha256', {})[part] = sha256f(os.path.join(od, '%s.json' % part))      # 一致だけを見る段が読む組の出力の同定（裁定 D236）
         if 'tool_error' in out:
             finish({'tool_error': out['tool_error'], 'tool_error_part': part})
             stop('器の誤りで組 %s が止まった（正本 computation.tool_error・結果を開かずに登録者に上げる）: %s' % (part, out['tool_error']))
+        package('part-%s' % part)                                   # 組ごとに zip を作って落とす（落ちた組から走らせ直せるように・裁定 D236）
     return finish()
 
 
@@ -472,7 +520,12 @@ if __name__ == '__main__':
         run()
     except SystemExit:
         raise
+    except Stop as e_:
+        LOG.append({'step': 'guard', 'at': now(), 'reason': str(e_)})
+        package('stopped', {'stopped': str(e_)})
+        sys.exit('[boot_Bl3] 止める（相 check の守り・登録者に相談）: %s' % e_)
     except Exception:
         LOG.append({'step': 'crash', 'at': now(), 'traceback': traceback.format_exc()[-4000:]})
+        package('crash', {'crash': traceback.format_exc()[-4000:]})       # 予期しない誤りでも session を書いて zip を作る（裁定 D236）
         say('[boot_Bl3] 予期しない誤りで止まった（器の誤りではない・登録者に相談）')
         raise
````
<<< 終: `tools/colab/boot_Bl3.py` の差分 >>>

<<< 始: `tools/analyze_Bl3.py` の差分（87ce664 → b77dd22・git diff・囲みは四つの backtick） >>>
````diff
diff --git a/tools/analyze_Bl3.py b/tools/analyze_Bl3.py
index e7d2aa4..3aca556 100644
--- a/tools/analyze_Bl3.py
+++ b/tools/analyze_Bl3.py
@@ -1,5 +1,5 @@
 # -*- coding: utf-8 -*-
-"""analyze_Bl3.py v2 —— B-lens 層三（Bl3）の集計の器（主の札・門・記述の門・q7・独立の再計算の一致・予想の答え・2026-09-25）。
+"""analyze_Bl3.py v3 —— B-lens 層三（Bl3）の集計の器（主の札・門・記述の門・q7・独立の再計算の一致・予想の答え・2026-09-25）。
 
 入力: 本の計算の出力（升目と符号ごとの効き目・質量・層ごとの差分）・下見の記録（本の凍結で凍結したもの）・独立の再計算の二つの道の出力・段階 B の凍結した集計器の記録と試行の記録・
       設計事実（q7 の区間）・正本。重みは読まない。**読みは付けない**（読みの型の当てはめと文は組み立ての器 `tools/build_report_Bl3.py` が正本の読みの表から行う）。
@@ -11,7 +11,7 @@
 関数は合成データの器 `tools/dry_run_Bl3.py` が合成の出力で呼ぶ。
 柵: 本器の出力のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
 """
-import os, re, sys, json, glob, collections
+import os, re, sys, json, glob, math, hashlib, collections
 import numpy as np
 
 HERE = os.path.dirname(os.path.abspath(__file__))
@@ -20,7 +20,7 @@ sys.path.insert(0, HERE)
 import blens_core as C
 import bl3_core as K
 
-VERSION = 'v2'          # v2（2026-09-25・裁定 D231〜D234）: 外した升目の扱い・偶然の目安の数え直し・効き目の側を全ての行で・札の一致の中身・二段の判定の形・一つだけの確かめ
+VERSION = 'v3'          # v3（2026-09-25・裁定 D236）: 一致だけを見る段が読んだ出力の同定を記録し開く段が照らす・開いた集計の一致の照らし・凍結の記録と手元の器の照らし・DRY の印と環境の食い違いで止める・下見の GPU・有限でない値で止める／v2（裁定 D231〜D234）
 ARM_RE = re.compile(r'^(.*?)([+-])v(rand|Nk|td|\db)?$')
 KIND = {'': 'static', 'rand': 'rand', 'Nk': 'Nk', 'td': 'td'}
 key3 = lambda sc, base, sg: '%s|%s|%+d' % (sc, base, int(sg))
@@ -260,6 +260,9 @@ def analyze(T3, FJ, main_out, pilot_attempts, pair_names, rows_gate, hook=None,
     """集計の全体（読みは付けない）。main_out: 升目と符号の鍵 → run_cell_sign の出力。pilot_attempts: 下見の試みの並び（最後が本の凍結の下見）。"""
     pilot = pilot_attempts[-1]
     dropped = (pilot.get('decision') or {}).get('dropped', [])
+    nf = sorted({k for k, o in main_out.items() for d, v in list((o.get('effects') or {}).items()) + list((o.get('mass') or {}).items()) if not math.isfinite(float(v))})
+    if nf:
+        raise SystemExit('有限でない効き目か質量がある（止める・裁定 D236）: %s' % nf[:5])
     eff = {k: o['effects'] for k, o in main_out.items()}
     lab, meta = row_labels(T3, T3['main_rows'], eff, pair_names, dropped)
     G = gates(T3, rows_gate, eff, dropped, style_rows)
@@ -303,13 +306,32 @@ def stage_b_notes(T3, AN, rows_gate):
 PARTS = ('main', 'recompute', 'secondary')
 JUDGE = os.path.join(REPO, 'records', 'Bl3', 'judge-Bl3.json')
 OPENED = os.path.join(REPO, 'records', 'Bl3', 'analysis-Bl3.json')
+FR_PATH = os.path.join(REPO, 'records', 'Bl3', 'FREEZE-RECORD-Bl3.json')
+SEAL_PATH = os.path.join(REPO, 'records', 'Bl3', 'sealing-record-Bl3.json')
+# 二つの段と報告の頭で、本の凍結の記録の SHA16 と照らすもの（正本・設計事実・方向の記録・集計と札と報告の器・裁定 D236）
+FROZEN_CHECK = ('design/contrasts-Bl3.json', 'records/Bl3/design-facts-Bl3.json', 'records/Bl3/design-facts-Bl3.md', 'results/Bl3/directions-Bl3.json',
+                'tools/analyze_Bl3.py', 'tools/bl3_core.py', 'tools/blens_core.py', 'tools/build_report_Bl3.py', 'tools/sweep_Bl3.py', 'tools/report_lint.py')
+STRICT_ENV = ('commit', 'dry', 'canon_sha16', 'directions_npz_sha256', 'layer_idx', 'coef')      # 組の間で違えば止める（GPU と版の違いは記す・裁定 D236）
+
+
+def sha256f(p):
+    h = hashlib.sha256()
+    with open(p, 'rb') as fh:
+        for blk in iter(lambda: fh.read(1 << 24), b''):
+            h.update(blk)
+    return h.hexdigest().upper()
+
+
+sha16f = lambda p: hashlib.sha256(open(p, 'rb').read().replace(b'\r\n', b'\n')).hexdigest().upper()[:16]
 
 
 def load_outputs(dirs):
-    """起動器の相 main の出力（組ごとの JSON と、その置き場の session.json）を、一つ以上の置き場から読む。同じ組が二つあれば止める。"""
-    parts, sessions = collections.OrderedDict(), collections.OrderedDict()
+    """起動器の相 main の出力（組ごとの JSON と、その置き場の session.json）を、一つ以上の置き場から読む。同じ組が二つあれば止める。
+    戻り値: 組 → 出力・組 → session・組 → 読んだファイルの同定（置き場の名・組の JSON と session.json の SHA-256・裁定 D236）。"""
+    parts, sessions, files = collections.OrderedDict(), collections.OrderedDict(), collections.OrderedDict()
     for d in dirs:
-        S = json.load(open(os.path.join(d, 'session.json'), encoding='utf-8'))
+        sp = os.path.join(d, 'session.json')
+        S = json.load(open(sp, encoding='utf-8'))
         for part in PARTS:
             p = os.path.join(d, '%s.json' % part)
             if os.path.exists(p):
@@ -317,15 +339,21 @@ def load_outputs(dirs):
                     raise SystemExit('同じ組が二つの置き場にある（止める）: %s' % part)
                 parts[part] = json.load(open(p, encoding='utf-8'))
                 sessions[part] = S
-    return parts, sessions
+                files[part] = {'dir': os.path.basename(os.path.normpath(d)), 'json_sha256': sha256f(p), 'session_sha256': sha256f(sp)}
+    return parts, sessions, files
 
 
-def env_same(sessions):
-    """組の間で、コミット・GPU・版・正本・方向の npz・DRY が同じか（違えば記す・二段目の比べに環境の違いが入る）。"""
+def env_same(sessions, pilot_sessions=None):
+    """組の間で、コミット・GPU・版・正本・方向の npz・DRY が同じか（違えば記す）。pilot_sessions（本の凍結の下見の session）を与えると、下見の GPU も並べる（裁定 D236）。"""
     keys = ('commit', 'dry', 'gpu', 'versions', 'canon_sha16', 'directions_npz_sha256', 'layer_idx', 'coef')
     ref = next(iter(sessions.values())) if sessions else {}
     diff = {k: {p: s.get(k) for p, s in sessions.items()} for k in keys if any(s.get(k) != ref.get(k) for s in sessions.values())}
-    return {'same': not diff, 'diff': diff}
+    out = {'same': not diff, 'diff': diff, 'strict': [k for k in diff if k in STRICT_ENV]}
+    if pilot_sessions is not None:
+        pg = sorted({str(s.get('gpu')) for s in pilot_sessions})
+        out['pilot_gpu'] = pg
+        out['gpu_same_as_pilot'] = sorted({str(s.get('gpu')) for s in sessions.values()}) == pg
+    return out
 
 
 def with_iso(T3, n_iso):
@@ -337,16 +365,54 @@ def with_iso(T3, n_iso):
     return T3x
 
 
-def judge(T3, parts, sessions, pilot_attempts, pair_names):
-    """一致だけを見る段: 器の誤りの有無・組の環境・二段の一致か不一致かだけを返す（効き目の値と差の最大は返さない）。"""
+def frozen_versions_bad(FR, repo=REPO, files=FROZEN_CHECK):
+    """手元の器と正本・設計事実・方向の記録を、本の凍結の記録の SHA16 と照らす（台帳に記した差分は許す・裁定 D236）。戻り値: 外れの並び。"""
+    fz = (FR.get('main_freeze') or {}).get('frozen_sha16') or {}
+    led = {td['path']: td for d in FR.get('deviations') or [] for td in d.get('tool_diffs') or []}
+    bad = []
+    for f in files:
+        p = os.path.join(repo, *f.split('/'))
+        want, got = fz.get(f), (sha16f(p) if os.path.exists(p) else None)
+        if want is None:
+            bad.append('%s（本の凍結の記録に無い）' % f)
+        elif got != want and not (f in led and led[f].get('after') == got):
+            bad.append('%s（本の凍結 %s・今 %s）' % (f, want, got))
+    return bad
+
+
+def judge(T3, parts, sessions, pilot_attempts, pair_names, files=None, FR=None):
+    """一致だけを見る段: 器の誤りの有無・組の環境・二段の一致か不一致かだけを返す（効き目の値と差の最大は返さない）。
+    DRY の印が組の間で違うとき・組の間のコミットと正本と npz と層と係数が違うとき・DRY でないのに等方の本数が正本と違うとき・DRY でないのに
+    手元の器と正本が本の凍結の記録と違うときは、一致と答えない（裁定 D236）。読んだ出力の同定（files）と凍結の記録の SHA16 を記録に置く。"""
     out = collections.OrderedDict(parts=list(parts), tool_error={p: bool(v.get('tool_error')) for p, v in parts.items()}, env=env_same(sessions))
-    dry = any(s.get('dry') for s in sessions.values())
+    drys = {p: bool(s.get('dry')) for p, s in sessions.items()}
+    dry = any(drys.values())
     out['dry'] = dry
+    out['inputs'] = files
+    if FR is not None and os.path.exists(FR_PATH):
+        out['freeze_record_sha16'] = sha16f(FR_PATH)
+    if os.path.exists(SEAL_PATH):
+        out['sealing_record_sha16'] = sha16f(SEAL_PATH)
+    stop = lambda why: (out.update(first=None, second=None, agree=None, reason=why), out)[1]
     if any(out['tool_error'].values()) or not {'main', 'recompute'} <= set(parts):
-        out.update(first=None, second=None, agree=None, reason='器の誤りか、組 main・recompute の欠け')
-        return out
-    pilot = pilot_attempts[-1]
+        return stop('器の誤りか、組 main・recompute の欠け')
+    if len(set(drys.values())) > 1:
+        return stop('組の間で DRY の印が違う: %s' % drys)
+    if out['env']['strict']:
+        return stop('組の間の環境が違う: %s' % out['env']['strict'])
     rc = parts['recompute']
+    n_can = T3['nulls']['isotropic']['count']
+    if not dry:
+        main_keys = {key3(sc, b, sg) for sc, b, sg in T3['cell_signs_main']}
+        n_main = {k: sum(1 for d in o['effects'] if d.startswith('iso:')) for k, o in parts['main']['cells'].items() if k in main_keys}
+        if rc['n_iso'] != n_can or any(v != n_can for v in n_main.values()):
+            return stop('DRY でないのに等方の本数が正本と違う（組 recompute %s・本の計算の升目と符号 %s）' % (rc['n_iso'], sorted(set(n_main.values()))))
+        if FR is None:
+            return stop('DRY でないのに凍結の記録が無い')
+        bad = frozen_versions_bad(FR)
+        if bad:
+            return stop('手元の器か正本が本の凍結の記録と違う: %s' % bad)
+    pilot = pilot_attempts[-1]
     eff = {k: o['effects'] for k, o in parts['main']['cells'].items()}
     ag = recompute_agreement(with_iso(T3, rc['n_iso']), T3['main_rows'], eff, pair_names, rc['hook'], rc.get('rewrite'), pilot,
                              (pilot.get('decision') or {}).get('dropped', []), rows_subset=set(rc['hook']) if dry else None)
@@ -356,7 +422,7 @@ def judge(T3, parts, sessions, pilot_attempts, pair_names):
     return out
 
 
-def open_results(T3, FJ, parts, sessions, pilot_attempts, pair_names, AN, calib_letter, FB, repo=REPO):
+def open_results(T3, FJ, parts, sessions, pilot_attempts, pair_names, AN, calib_letter, FB, repo=REPO, pilot_sessions=None):
     """結果を開く段（登録者と一緒に・一致だけを見る段が一致したとき）: 集計の全体と、報告に並べるもの（下見の記録・頭の確かめ・層ごとの差分・乙・段階 B の注・環境）。"""
     rc = parts['recompute']
     dry = any(s.get('dry') for s in sessions.values())
@@ -367,6 +433,7 @@ def open_results(T3, FJ, parts, sessions, pilot_attempts, pair_names, AN, calib_
                 rows_subset=set(rc['hook']) if dry else None)
     main_keys = {key3(sc, b, sg) for sc, b, sg in T3['cell_signs_main']}
     A['dry'] = dry
+    A['n_iso'] = rc['n_iso']
     A['pilot_attempts'] = pilot_attempts
     A['head'] = parts['main']['head']
     A['main_run'] = {k: parts['main'].get(k) for k in ('batch', 'shortcut', 'dropped')}
@@ -378,16 +445,49 @@ def open_results(T3, FJ, parts, sessions, pilot_attempts, pair_names, AN, calib_
         S2 = parts['secondary']
         A['secondary'] = {'counts': S2.get('counts'), 'contexts_run': len(S2.get('contexts') or []), 'summary': secondary_summary(S2.get('contexts') or [], calib_letter)}
     A['sessions'] = {p: {k: s.get(k) for k in ('commit', 'dry', 'gpu', 'versions', 'canon_sha16', 'directions_npz_sha256', 'layer_idx', 'coef', 'finished')} for p, s in sessions.items()}
-    A['env'] = env_same(sessions)
+    A['env'] = env_same(sessions, pilot_sessions)
     A['clause'] = '本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。'
     return A
 
 
-def _pilot_attempts(args_pilot):
+def open_checked(T3, FJ, parts, sessions, files, J, pilot_attempts, pair_names, AN, calib_letter, FB, FR=None, judge_sha16=None, repo=REPO):
+    """結果を開く段の確かめ（裁定 D236）: 一致だけを見る段の記録が一致で、読む出力の同定（置き場の名・組の JSON と session の SHA-256）と凍結の記録の SHA16 が
+    一致だけを見る段の記録と同じで、DRY でないときは手元の器と正本が本の凍結の記録と同じであること。開いた集計の二段の一致が一致だけを見る段と違えば止める（書かない）。"""
+    if not J.get('agree'):
+        raise SystemExit('一致だけを見る段の記録が一致していない（結果を開かない）')
+    if J.get('inputs') != files:
+        ji, fi = J.get('inputs') or {}, files or {}
+        raise SystemExit('結果を開く段の出力が、一致だけを見る段の読んだ出力と違う（開かない）: %s' % [p for p in sorted(set(ji) | set(fi)) if ji.get(p) != fi.get(p)])
+    dry = any(s.get('dry') for s in sessions.values())
+    if not dry:
+        if FR is None:
+            raise SystemExit('DRY でないのに凍結の記録が無い（開かない）')
+        if J.get('freeze_record_sha16') != sha16f(FR_PATH):
+            raise SystemExit('凍結の記録が一致だけを見る段の後に変わった（開かない）')
+        bad = frozen_versions_bad(FR, repo)
+        if bad:
+            raise SystemExit('手元の器か正本が本の凍結の記録と違う（開かない）: %s' % bad)
+    pilot_sessions = ((FR or {}).get('main_freeze') or {}).get('sessions')
+    A = open_results(T3, FJ, parts, sessions, pilot_attempts, pair_names, AN, calib_letter, FB, repo=repo, pilot_sessions=pilot_sessions)
+    rc = A.get('recompute') or {}
+    got = (None if rc.get('first') is None else bool(rc['first']['agree']), bool((rc.get('second') or {}).get('agree')), bool(rc.get('agree')))
+    if got != (J.get('first'), J.get('second'), J.get('agree')):
+        raise SystemExit('開いた集計の二段の一致が、一致だけを見る段と違う（書かない）: %s 対 %s' % (got, (J.get('first'), J.get('second'), J.get('agree'))))
+    A['inputs'] = files
+    A['judge_record_sha16'] = judge_sha16
+    return A
+
+
+def _freeze_record():
+    return json.load(open(FR_PATH, encoding='utf-8')) if os.path.exists(FR_PATH) else None
+
+
+def _pilot_attempts(args_pilot, FR=None):
     """下見の試みの並び: 本の計算では凍結の記録の本の凍結（`main_freeze.pilot_attempts`）から。DRY の出力を試すときだけ、相 pilot の出力の pilot.json を与える。"""
     if args_pilot:
         return [json.load(open(p, encoding='utf-8'))['pilot'] for p in args_pilot]
-    FR = json.load(open(os.path.join(REPO, 'records', 'Bl3', 'FREEZE-RECORD-Bl3.json'), encoding='utf-8'))
+    if FR is None:
+        raise SystemExit('凍結の記録が無い（本の計算では本の凍結の下見の記録を読む）')
     atts = FR['main_freeze']['pilot_attempts']
     if FR['main_freeze']['pilot'] != atts[-1]:
         raise SystemExit('本の凍結の下見の記録と、下見の試みの最後が違う（止める）')
@@ -395,7 +495,7 @@ def _pilot_attempts(args_pilot):
 
 
 def main():
-    import argparse
+    import argparse, datetime
     ap = argparse.ArgumentParser(description='B-lens 層三の手元の二つの段（judge: 一致だけを見る／open: 結果を開く）')
     ap.add_argument('step', choices=['judge', 'open'])
     ap.add_argument('dirs', nargs='+', help='起動器の相 main の出力の置き場（組ごとの JSON と session.json）')
@@ -406,37 +506,40 @@ def main():
     T3 = json.load(open(os.path.join(REPO, 'design', 'contrasts-Bl3.json'), encoding='utf-8'))
     FJ = json.load(open(os.path.join(REPO, 'records', 'Bl3', 'design-facts-Bl3.json'), encoding='utf-8'))
     DJ = json.load(open(os.path.join(REPO, 'results', 'Bl3', 'directions-Bl3.json'), encoding='utf-8'))
-    parts, sessions = load_outputs(a.dirs)
+    parts, sessions, files = load_outputs(a.dirs)
     dry = any(s.get('dry') for s in sessions.values())
     if a.pilot and not dry:
         raise SystemExit('--pilot は DRY の出力を試すときだけ（本の計算では凍結の記録の本の凍結を読む）')
-    attempts = _pilot_attempts(a.pilot)
+    FR = None if dry else _freeze_record()
+    attempts = _pilot_attempts(a.pilot, FR)
     pair_names = list(DJ['groups']['real']['names'])
     if a.step == 'judge':
         out = a.out or JUDGE
         if os.path.exists(out):
             raise SystemExit('既にある（一致だけを見る段は一度だけ）: %s' % out)
-        J = judge(T3, parts, sessions, attempts, pair_names)
-        J['written_utc'] = __import__('datetime').datetime.now(__import__('datetime').timezone.utc).strftime('%Y-%m-%d %H:%M:%S UTC')
+        J = judge(T3, parts, sessions, attempts, pair_names, files, FR)
+        J['written_utc'] = datetime.datetime.now(datetime.timezone.utc).strftime('%Y-%m-%d %H:%M:%S UTC')
         J['clause'] = '本記録は一致か不一致かだけを持つ（値は開かない・正本 independent_recompute.print）。'
         json.dump(J, open(out, 'w', encoding='utf-8', newline='\n'), ensure_ascii=False, indent=1)
         say = lambda b: '無い' if b is None else ('一致' if b else '不一致')
-        print('[analyze_Bl3] 一致だけを見る段: 器の誤り %s・組の環境 %s・一段目 %s・二段目（札） %s・二段目の値 %s・全体 %s（値は開いていない）' % (
+        print('[analyze_Bl3] 一致だけを見る段: 器の誤り %s・組の環境 %s・一段目 %s・二段目（札） %s・二段目の値 %s・全体 %s（値は開いていない）%s' % (
             'あり' if any(J['tool_error'].values()) else '無し', '同じ' if J['env']['same'] else '違う', say(J['first']), say(J['second']),
-            '無い' if J.get('second_values_within_tol') is None else ('許容の内' if J['second_values_within_tol'] else '許容の外（止めずに台帳に記す・裁定 D234）'), say(J['agree'])))
+            '無い' if J.get('second_values_within_tol') is None else ('許容の内' if J['second_values_within_tol'] else '許容の外（止めずに台帳に記す・裁定 D234）'), say(J['agree']),
+            ('・理由 %s' % J['reason']) if J.get('reason') else ''))
         if not J['agree']:
             raise SystemExit('一致しない（結果を開く前に止め、逸脱の台帳に記して登録者に上げる・裁定 D219）')
         return
     jp = a.judge_record or JUDGE
-    if not os.path.exists(jp) or not json.load(open(jp, encoding='utf-8')).get('agree'):
-        raise SystemExit('一致だけを見る段の記録が無いか、一致していない（結果を開かない）: %s' % jp)
+    if not os.path.exists(jp):
+        raise SystemExit('一致だけを見る段の記録が無い（結果を開かない）: %s' % jp)
+    J = json.load(open(jp, encoding='utf-8'))
     out = a.out or OPENED
     if os.path.exists(out):
         raise SystemExit('既にある: %s' % out)
     AN = json.load(open(os.path.join(REPO, 'records', 'B', 'analysis-B-2026-09-22.json'), encoding='utf-8'))
     CB = json.load(open(os.path.join(REPO, 'results', 'Blens', 'calib-Blens.json'), encoding='utf-8'))['magnitude']['letter']
     FB = json.load(open(os.path.join(REPO, 'records', 'Blens', 'design-facts-Blens.json'), encoding='utf-8'))
-    A = open_results(T3, FJ, parts, sessions, attempts, pair_names, AN, CB, FB)
+    A = open_checked(T3, FJ, parts, sessions, files, J, attempts, pair_names, AN, CB, FB, FR=FR, judge_sha16=sha16f(jp))
     json.dump(A, open(out, 'w', encoding='utf-8', newline='\n'), ensure_ascii=False, indent=1, default=lambda o: o.item() if hasattr(o, 'item') else float(o))
     print('[analyze_Bl3] 結果を開いた: %s' % os.path.relpath(out, REPO))
 
````
<<< 終: `tools/analyze_Bl3.py` の差分 >>>

<<< 始: `tools/sweep_Bl3.py` の差分（87ce664 → b77dd22・git diff・囲みは四つの backtick） >>>
````diff
diff --git a/tools/sweep_Bl3.py b/tools/sweep_Bl3.py
index e704389..2c0a1f2 100644
--- a/tools/sweep_Bl3.py
+++ b/tools/sweep_Bl3.py
@@ -1,7 +1,7 @@
 # -*- coding: utf-8 -*-
-"""sweep_Bl3.py v2 —— B-lens 層三（Bl3）の掃き出しの器（正本 `report_rules.builder`・草案3 §12「掃き出しの器」・2026-09-25）。
+"""sweep_Bl3.py v3 —— B-lens 層三（Bl3）の掃き出しの器（正本 `report_rules.builder`・草案3 §12「掃き出しの器」・2026-09-25）。
 
-正本と凍結の本文が求める出力の一覧（下の REQUIRED・出所の鍵つき）を、集計の器の結果を開く段の出力（`records/Bl3/analysis-Bl3.json`）と突き合わせ、欠けを返す。
+正本と凍結の本文が求める出力の一覧（下の ROW_KEYS・SECOND_KEYS・PILOT_KEYS・GATES と、関数 sweep の中の need の行・出所の鍵つき）を、集計の器の結果を開く段の出力（`records/Bl3/analysis-Bl3.json`）と突き合わせ、欠けを返す。
 組み立ての器 `tools/build_report_Bl3.py` が報告を組む前に呼び、欠けがあれば止める。下見で止まったときの出力（下見の記録と予想の答えだけ）は、止まったときに求めるものだけを見る。
 本器は値の当否を見ない（有るか無いかと、行と升目と符号がそろうかだけ）。
 用法: python tools/sweep_Bl3.py <集計の出力の JSON> ／ --selftest（合成の出力は `tools/build_report_Bl3.py --selftest` が作って呼ぶ）
@@ -11,7 +11,7 @@ import os, sys, json
 
 HERE = os.path.dirname(os.path.abspath(__file__))
 REPO = os.path.abspath(os.path.join(HERE, '..'))
-VERSION = 'v2'          # v2（2026-09-25・裁定 D231）: 効き目の側は全ての行で・偶然の目安の分母
+VERSION = 'v3'          # v3（2026-09-25・裁定 D236）: 層ごとの差分の中身と等方の本数を見る・頭の文／v2（裁定 D231）: 効き目の側は全ての行で・偶然の目安の分母
 key3 = lambda sc, b, sg: '%s|%s|%+d' % (sc, b, int(sg))
 
 ROW_KEYS = [('effect', 'labels.print_rule（行の値）'), ('p', 'labels.p_rule'), ('upper', 'labels.print_rule（上の裾の本数）'), ('lower', 'labels.print_rule（下の裾の本数）'),
@@ -75,9 +75,17 @@ def sweep(T3, A):
     need(set(main_keys) <= set(D.get('mass_below_min') or {}), '質量が下限を下回った方向の数（descriptive.mass）')
     LW = A.get('layerwise') or {}
     need(set(main_keys) <= set(LW), '層ごとの差分（主の組の升目と符号・descriptive.layerwise.directions）')
+    need('n_iso' in A and (A.get('dry') or A.get('n_iso') == T3['nulls']['isotropic']['count']), '等方の本数（DRY でなければ正本の本数・nulls.isotropic.count）')
+    units = list(T3['directions']['named']) + ['rand:%d' % i for i in range(T3['nulls']['B_random']['count'])]
     for k in main_keys:
         lw = LW.get(k) or {}
         need({'noop_lo', 'rows', 'iso_summary'} <= set(lw), '層ごとの差分の %s の行と等方の中央値と中央の区間（descriptive.layerwise）' % k)
+        n_lay = len(lw.get('noop_lo') or {})
+        need(n_lay > 0 and sorted((lw.get('rows') or {})) == sorted(units) and all(len(v) == n_lay for v in (lw.get('rows') or {}).values()),
+             '層ごとの差分の %s の名前のある方向と段階 B の三本の行が、層の数だけそろう（descriptive.layerwise.directions）' % k)
+        iso_s = lw.get('iso_summary') or {}
+        need(iso_s.get('n') == A.get('n_iso') and all(len(iso_s.get(x) or []) == n_lay for x in ('median', 'lo', 'hi')),
+             '層ごとの差分の %s の等方の中央値と中央の区間が、等方の本数と層の数でそろう（descriptive.layerwise.band）' % k)
     S2 = A.get('secondary') or {}
     need(S2.get('summary') and all('blens_direct' in v for v in S2['summary'].values()), '乙と B-lens の直接の経路の値（descriptive.secondary_readout）')
     H = A.get('head') or {}
````
<<< 終: `tools/sweep_Bl3.py` の差分 >>>
