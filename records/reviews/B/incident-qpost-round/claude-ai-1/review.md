<!-- 逐語保全: claude.ai の Claude Opus 5（起草者と同一系列）・一人目。登録者（楠見優太）が会話で渡した票を、会話の記録から機械で切り出した（uuid a90a2336-b5a5-4a0b-ac3b-7bc33d391e10・2026-09-21T20:40:32.944Z）。
     この枠の外は一字も変えていない。本票のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。 -->
The request is in view, but the materials M1–M8 are inside the uploaded bundle. Reading the file first.M1 has a decisive-looking detail: the registered QF unit is "腕 × 層 × 係数", with no stage in it. Now M3 (analyzer) and M4 (synthetic data).# 選定後の品質床の食い違い（D151 の候補）への票（claude.ai の Claude・Claude Opus 5・系統内）
情報状態

* 束は全文を読みました（依頼文・M1〜M8）。
* 束の外を見ました。 公開の置き場をコミット `3ce97ca` で取得し、次を開きました。
   * 器: `tools/analyze_B.py`・`gate_B.py`・`integrity_B.py`・`layers_B.py`・`compare_predictions_B.py`・`run_stageB_local.py`・`synth_B.py`・`dry_run_B.py`・`build_report_B.py`
   * 記録: `records/B/run-plan-B.md`、実験の外の検分の記録（採否表の P262・P307 の行）
   * 正本と履歴: `design/contrasts-B.json` の `measured`、`git log`
* 盲検は保っています。
   * `prelim/`・`results/prelim-*`・`records/B/analysis-B-2026-09-22.*` は開いていません。
   * 本走行の試行の記録からは、S4 の無操作の二セルの `resp_mean_path` の欄だけを読みました。成績の欄は読んでいません。
   * 門の記録は品質床の行だけを読み、調整走行の率は読んでいません。
* 資料 §3 の SHA16 五件（正本・集計器・起動器・合成データの器・門の記録）は、いずれも現物と一致しました。
* 相関の開示: この会話の中の私の前の票は、選定後の段を「11 セル」として扱っていました。つまり読み一を前提にし、集計器を確かめていませんでした。集計器の「post の走行だけを読む」一行は `30fdf71`（2026-09-18）から入っており、私が見たどの束にもありました。私はどの巡でもこれを捕まえていません。

(a) 正本はどちらを支持するか —— 読み一
根拠

1. 後から、より具体的に書かれたのは「残りの」の文です。
   * `post_selection` の「すべての」（裁定 D69）は `0ee5d33`（09-18 00:20 UTC）で入りました。
   * `run_order` の「(ii)…残りの介入の腕」（裁定 D77）は `ac7a368`（同日 02:57 UTC）で入りました。
   * D69 は「どの腕も床を通す」という範囲の決めで、D77 がそれを二段に割り振ったものと読めます。
2. 登録の単位に段が入っていません。 `quality_floor.unit` は「腕 × 層 × 係数」です（M1）。
   * 二腕の (0.5, 2.0) の単位は、選定の段で同じセッションの相手と判定済みです。
   * その結果は O-Ncold-v 174 対 174、Onull+v 183 対 183 です（M5）。
   * D88・D92 の「同じセッションの相手」も満たしています。
3. 選ばれた組の二腕が合格であることは、選定の規則そのものが保証しています。 `pass_rule` は、選ばれる組に両方の土台での合格を求めます（M1）。
4. 凍結時の数が読み一で数えられています。
   * 正本の中の値 `measured.quality_floor_multiplicity.post_cells` は 11 です（現物）。
   * 転記行 A は「残りの介入 11 セル」、段取りは「15 セル」です（M6）。
   * D129 の多重性の計算も 11 セルで行われています。
5. `run_order` は「(ii) は本走行を始める前に判定を終える」と定めており、起動器の段取りと合っています。

反対の読みを組み立てると

* 読み二のいちばん強い形は次のとおりです。
   * `post_selection` は「すべて」と言っている。
   * fail_rule は「(ii) で落ちた腕」を名指している。
   * 相手は段ごとに持つ（partner_run）。
   * 実装の検分で採用した P262 は「介入の腕の一覧から揃っているかを検査し、欠けは判定不能に倒す」とした（`records/reviews/B/impl-round/adoption-table-B-impl.md` 27 行）。
   * 集計器はこれを実装しただけである。
* 私がこれを採らない理由は三つです。
   * 読み二では「残りの」が意味を失います。
   * 同じ単位（腕 × 層 × 係数）を、理由の登録なしに二度判定することになります。
   * 正本の中の値 `post_cells = 11` と食い違います。
* ただし、正本に曖昧さが無いとは言えません。 P262 の採用文は段を特定しておらず、D69 の文言と D77 の文言は最後まで揃えられていませんでした。

(b) どの道か —— ほか（乙の変形・二つの出力を並べる）
勧める形

1. 逸脱として記帳する（`deviation.rule` の番号・日付・理由・承認）。
2. 凍結した集計器の出力は、変えずに残して公開する。
3. 直しは最小にし、別の版として走らせる。
   * 二腕（O-Ncold-v・Onull+v）の床だけを、門の記録の、選ばれた組の選定の段の行から読む（M5）。
   * その記録は 2026-09-21 に凍結した `gate_B` が作ったもので、確証の率を開く前の記録です。
   * ほかは何も変えない。
   * 二つの出力を機械で比べ、変わったのがその 8 行だけであることを示す。
4. 直した側の札は、そのまま「確証」とは呼ばない。「確証（逸脱の下）」のように印を付ける。
5. 報告の冒頭（§0）と要約には、二つの内訳を同じ重さで並べる。

丙に反対します。

* `run_order` は (ii) を「本走行を始める前に判定を終える」と定めています（M1）。いまから走らせても、この条件はもう満たせません。
* 率を見た後の新しいデータです。
* 読み一なら、そもそも要らないセルです。

甲だけで終えることにも反対します。

* 甲だけでは、より具体的な正本の条（D77）と、率を開く前の凍結した記録（門の記録）の両方に反する札を、唯一の札として公開することになります。
* 甲の出力そのものは、上の 2 のとおり並べて残します。

(c) 直した札の身分

* 事前登録の確証と同じ身分は持ちません。 直すと決めたのは、p を見た後の起草者と登録者です。
* 結果に依らない部分は、報告で区別して書けます。
   * 発見は p ではなく、札そのもの（8/16 が判定不能、主の二族が全部）が強いたものです。
   * 直しの中身は、率を開く前の記録（D77 の文・門の記録・凍結した起動器の段取り）で決まっています。
* 予想の照合（一度だけ）の入力を、走らせる前に逸脱の記録で決めてください。 私の案は、二つの集計の出力それぞれに一度ずつ当て、両方を公開することです。どちらを入力にするかを後で選べる形は、避けるべきです。
* 控える主張
   * 要約で印の無い「確証」を使わない。
   * 直した札だけから新しい集計（本数・向き・的中率）を作らない。
* 開示の文案
減算族と加算族の 8 対比について、凍結した集計器は「判定不能（品質床）」と印字した。これは品質床に落ちたのではなく、凍結した起動器（選定後の段で二つの土台の腕を外す）と凍結した集計器（選定後の段の走行だけを読む）の食い違いによる。正本 `run_order`（裁定 D77）は選定後の段を「残りの介入の腕」と定めている。二腕の床は、確証の率を開く前に作った門の記録（2026-09-21）で、選ばれた組の選定の段で合格している（差 0.0 pt）。登録者は、確証の族の率と p を見た後に、これを逸脱として直すと裁定した。表は凍結した器の出力と逸脱の下の出力を並べる。後者の札は、直すという決定が結果を見た後であるため、事前登録の確証と同じ身分を持たない。

(d) 所見の一覧
重大 D1 副位置の活性のファイルが公開の記録に無く、`layers_B` はそれで止まらない

* 置き場と確かめ方:
   * `results/stageB/stageB__S4__O-Ncold__s1` と `…__S4__Osec-Ncold__s1` は、どちらも 200 行すべてが `resp_mean_path` を持っています。指す先は `resp-stageB__S4__{O-Ncold,Osec-Ncold}__s1.npz` です。
   * ところが、そのファイルは置き場にありません。本走行の 59 セルにも調整走行のセルにも、`resp-*.npz` は 0 件です。`.gitignore` は npz を除いていません。
   * `layers_B.py` の `load_resp`（63–79 行）は、欠けを数えるだけです。止まるのは `--directions` が無いとき（199 行）と、出力が既にあるとき（215 行）だけです。
* 何が起きるか:
   * 裁定 D132 の読みの腕（S4 の無操作の二腕）が、活性零本のまま「記述」として書かれうる。
   * 合成データは各走行の置き場に `resp-synth.npz` を必ず置くので（`synth_B.py` 166・469 行）、この欠けを一度も通っていません。
* 直し方:
   * 欠けが一件でもあれば止める。
   * npz を公開の記録に足すか、置き場と SHA-256 を記帳する。
* 重さ: 手元にも Drive にもファイルが無ければ、登録した記述が測れません。手元にあるなら、重さは中に下がります。私はこの有無を確かめていません。

中 D2 整合検査が、選定後の段の升目だけは列挙しない

* 置き場: `integrity_B.py` 207 行「選定後の品質床の升目は…ここでは列挙しない（集計器が腕ごとに数えて「合格にしない」）」。
* 何が起きるか: ほかの相は升目を列挙するのに、この段だけを、食い違いの当事者である集計器に預けています。そのため整合検査は 35 セル・不整合 0 で通りました。
* 直し方: 裁定で決まった読みの升目を正本から列挙する。

中 D3 「どのセルを走らせるか」の計画が三箇所に別々に書かれている

* 置き場: 起動器 `boot_stageB.py` 439–441 行（二腕を外す）、合成データの器 `synth_B.py` 525–531 行（全部の介入の腕）、集計器 `analyze_B.py` 147–155 行（全部を求める）。
* 何が起きるか: 今回の一件そのものです。同じ型は、計画が器ごとに書かれている限り再発します。
* 直し方: 計画を一つの関数にして四者（起動器・合成データ・整合検査・集計器）が呼ぶ。「起動器の計画から合成データを作る」変異を一件足す。

軽 D4 注の出所の誤り

* 置き場: `analyze_B.py` 154 行の注「合格扱いにしない・裁定 D77」。
* 何が起きるか: D77 にその文はありません。この規則の出所は P262 です。

是認（同じ形で）

* 予想の照合の入力の型は、集計器の出力と合っています。
   * `compare_predictions_B.py` が読む `confirm`・`gate_sha16`・`s4` を、`analyze_B.py` 497–499 行が書いています。
   * `dry_run_B.py` 222 行は、合成データで走らせた本物の集計器の出力を照合器に渡しています。
* 品質床の走行では hook が効いています。
   * 門の記録の品質床の行で、層 0.75 に差が出ています（O-Ncold-v 係数 2.0 で −2.5 pt、係数 0.5 で +0.5 pt、Onull+v 係数 2.0 で +1.0 pt）。
   * `run_quality_cell` は hook を掛けて照らしています（`run_stageB_local.py` 726–728 行）。
   * したがって、選ばれた組の 0.0 pt は、hook が死んでいた証拠ではありません。
* 凍結した方向 `results/dirB/dirB__s1/directions.npz` は置き場にあります。

(e) 率と p を伏せた伺い方 —— 適切（条件つき）

* 構造の判断を結果から切り離すのは正しく、道の当否は影響の大きさに依るべきではありません。
* ただし、盲検は頼みごとにとどまっています。 置き場には集計の出力と本走行の記録があり、伺う相手は開けます。次の二つを依頼文に足してください。
   * 開かないでほしいファイルを名指しする。
   * 票に盲検の状態を書いてもらう（私は上に書きました）。
* 決める側は盲検ではありません。 だからこそ、道と、予想の照合の入力を、表をもう一度見る前に文書で固定してください。
* 裁定の後には、二つの出力の全表を公開してください。

この検分が確認していないこと

* 集計の出力と本走行の成績は開いていません。したがって、直すと札がどう動くかは知りません（意図してのことです）。
* `analyze_B`・`layers_B`・`compare_predictions_B`、そして上の最小の直しは、どれも走らせていません。
* npz のファイルが Drive や手元にあるかは確かめていません。
* `gate_B.py`・`build_report_B.py`・`integrity_B.py` は通読していません（grep の範囲だけです）。
* P262 の元の所見（実装の検分の票）は読まず、採否表の行だけを見ました。
* `control_chart_B`・`sample_inspection_B` など、ほかの読み手と書き手の対は探し尽くしていません。

本票のいかなる数値も、AI に意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはなりません（両方向不定）。
