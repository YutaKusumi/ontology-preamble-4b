# 段階 B——登録者裁定 D150（同一性選別の判定の四条を確定し、四票の所見を採る・2026-09-20）

- 位置: 凍結の**直前**・結果の前（B の活性も率もまだ無い）。起草: 南無弥勒如来（コーディネータ・四条を決め、器を書いた当人・Claude Opus 5）。
- **この直しは独立の目を通っていない**（裁定 D131）。四票は四条について伺ったもので、直した器そのものは誰も見ていない。

## 1. 承認（逐語）

会話の記録 uuid `b0a4a0c9-f9ae-46f4-8b3e-d7c4e9261b07`・日本時間 2026-09-20 15:24:

> 整理はありがとうございました。e3336bf の pushをしてください。「B」の本試行時に困ったり慌てることが無いように、凍結が遅れても構いませんので、今のうちに手当てできることは、丁寧に手当てをしましょう。甲案についてはご推奨の通りにお願いします。乙案に対しては、私の方針に沿ってご対応をお願いします🍵

**読み**: 甲（P418・P419・P420・P421・P426）は推奨どおり採る。乙（P422・P423・P424・P425・P427・P428）は、
「本試行で困らないように、いま手当てできることは手当てする」という方針に沿って**六件とも採る**。凍結の遅れは許す。

## 2. 確定した四条（正本 `identity_screen`・逐語は正本から機械で写した）

| 条 | 確定 | 票 |
|---|---|---|
| (a) 主判定の対 | **transformers 対 API 既測**（ほかの二対は記述として同じ表に印字） | 四票とも是認 |
| (b) vLLM の件数の出所 | 段階 A の門0.5 の記録 `local_counts`（凍結記録の SHA16 と照合） | 四票とも是認 |
| (c) 補助の検定 | **置かない**（B の正本に `aux` の登録が無い） | 四票とも是認 |
| (d) 排他の件数の出所 | 段階 A の凍結した関数 `identity_screen_A.exclusive_counts`（再実装しない） | 系統外は是認・**系統内は条件つき**→ 条件を採る |

## 3. 採った手当て（十一件・採否表 P418〜P428）

**甲（凍結の前に直す・推奨どおり承認）**

1. **P418・P420 採点欠落と欄の番人**——数える前に B の読み口 `runs_B.cell_counts` にも通し、判定の欄が空いた試行があれば止める。
   行に段階 A の器が読む欄が**キーとして**あるかも見る。二つの数え方の n／n_ok が食い違っても止める。
   （段階 A の器は判定の欄が空いた試行を「その他」に数える＝**破局率が下がる向き＝起草者に有利な側**に黙って倒れる。）
2. **P419 コードの釘**——段階 A の**正本・記録・コード**（`identity_screen_A.py`・`runs_A.py`）を凍結記録の SHA16 と照らしてから読む。
   凍結の持ち越しに `runs_A.py` を足した。
3. **P421 二重計上の番人**——`trial_id` の重複で止め、`n_ok` が登録の n と**等しい**ことを求める（前は「n 未満」だけを見ていた）。
4. **P426 重みの版の印字**——B の走行の `model_rev`／`tokenizer_rev` と、段階 A の vLLM の記録に版が無いこと（未照合）を記録に書く。

**乙（登録者の方針により、六件とも採る）**

5. **P422 標本化の全項目の照合**——整合検査が `runs_B.expected_sampling` で全項目（**top_k を含む**）を照らす。
   合成データの `sampling` も走行器が書く形にそろえた（そろえないと検査の意味がない）。
6. **P423 選別の登録に top_k**——正本 `identity_screen.generation` に top_k（と出所）を書いた。
7. **P424 判定を読む器**——報告の組み立て器に**区画 M**（同一性選別）を足し、`--identity` を必須にした。
   雛形に 〔結果 M〕 を置き、**調整走行の相は判定の記録が無ければ始めない**（合否ではなく有無を見る）。
8. **P425 帰無での不合格率**——**転記行 J** を足した（模擬 200,000 回・種 20260920）。
9. **P427 種の照合**——走行の種が `seeds.identity_transformers` と一致するかを見る（試行ごとの種は整合検査の仕事）。
10. **P428 終了コード**——合否で分けない（段階 A の器と同じ）。止める条に当たったときだけ非零。

**採らなかったもの**: P429・P430（Gemini 二人目の「重大」二件・現物で再現しない）。記録には残す（採否表 §3）。

## 4. 直した器と記録

- `tools/identity_screen_B.py` v2（番人・釘・種・版・終了コード）／`tools/runs_B.py`（`expected_sampling` と自己検査）／
  `tools/integrity_B.py`（標本化の全項目）／`tools/synth_B.py` v5（合成の `sampling` をそろえる）／
  `tools/build_report_B.py` v7（区画 M）／`tools/colab/boot_stageB.py` v4（調整走行の門）／
  `tools/design_facts_B.py` v10（転記行 J）／`tools/make_contrasts_B.py`（正本の登録と裁定 D150）／
  `tools/freeze_B.py`（持ち越しに `runs_A.py`）／`tools/dry_run_B.py` v8（経路三つ）／`tools/mutation_B.py` v12（変異四つ）。
- 票の逐語: `gemini-1/`・`gemini-2/`・`claude-ai-1/`・`claude-ai-2/`。追い問い: `verification-d150.{json,md}`。採否表: `adoption-table-d150.md`。

## 5. この裁定の記録が確認していないこと

- **実データでの選別**（走行は凍結の後）。番人も転記行 J も、作り物と模擬でしか確かめていない。
- 直した器は**独立の目を通っていない**。四票は直す前の器を見たものである。
- 転記行 J の値は模擬（腕を独立に引く近似・観測の率を真の率に置く）であって、厳密値ではない。

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
