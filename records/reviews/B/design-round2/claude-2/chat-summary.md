まず手順どおり、ファイル読み取りの指針を確認してから資料を読みます。束の三部とも、字数と SHA16 が索引と一致しました。依頼文の指定どおり part1 → part2 の順で読み、part3 は自分の所見を書いた後に読みます。part1 を読み終えました。次に part2（正本 JSON）を読みます。
正本 JSON を取り出した結果、SHA16 が 19277DD2CC7AE36B と一致しました（逐語が保たれています）。中身を節ごとに読みます。正本を読み終えました。ここから、依頼の作法 1 に従って転記行の数を正本から自分で計算し直します。転記行 A は正本から完全に再現できました。次に B・C を検算します。転記行 B・C の解析量はすべて再現しました。次に、転記行 C のモンテカルロ（選定確率と「格子の位置による差」）を自分で回します。転記行 D も両側 Fisher の全数列挙で再現しました（一部は末位の丸め差）。次に転記行 E と、転記行 D に載っていない S4 反証の検出力を計算します。主要な検算が揃いました。part3 を読む前に、自分の所見を先に書き出して固定します。所見を固定しました。ここで part3（一段目の採否表）を読み、私の所見のうちどれが既出でどれが新規かを確かめます。part3 まで読み終えました。所見は part3 を読む前に固定済みなので、既出との重なりも確認できます。票を作成します。検分票を作りました（判定は条件つき）。逐語保全と SHA16 の計算がしやすいよう markdown ファイルにしてあります。
読んだ順は依頼文の勧めどおり part1 → part2 → 所見を固定 → part3 です。転記行 A〜F・I は正本の定数から独立に計算し直し、すべて一致しました（D の 2 欄のみ末位 1 の丸め差）。
重大は 4 件で、うち 3 件は一段目の手当てが作った隙間です。

* 書式外の希釈が本走行で塞がれていない — D73 は選定にだけ門を置きました。門の上限 0.3 は基底 0.53 で 15.9 pt の見かけの低下を許し、これは設計が検出力を置いた ±15 pt と同じ大きさです。しかも門は「和集合」なので v 腕 0.30・vrand 腕 0.00 という最悪の配置が楽に通ります。私の計算では、書式が崩れただけで Holm 初段が 0.74 の確率で立ちます。
* 品質床を D69 で広げたのに、落ちたときの札も走行の順も無い — `procedure` に選定後の品質床の段が無く、`denominators` にも報告の定型にも品質床の欄がありません。2,600 試行を足して使わない量になっています。
* td 統制が不利な材料になりえない — 正本の目的文が求める比較を、読み条項 (xiii) が禁じています。
* 確証族に符号の封印が無い — 封印は S4 だけで、両側登録と (ix) の組み合わせでは上がっても下がっても「動いた」になります。

中で一つ挙げるなら、転記行 C の「位置による差」を再現したところ、同点を添字順で割ったときにだけ一致しました（0.707／0.691／0.680・同点は 7.2% の走行で発生）。無作為に割ると位置差は消えます。つまり D68 が廃した「係数の小さい方・層の浅い方」が同点の回だけ生き残っており、同じ行の「帰無なら 0.111」はその前提と食い違っています。

---

追補を承りました。ご指摘の型（本走行向けに書いた規則が他の走行に移っていない）を、正本の鍵ごとに機械的に洗い出します。まず語の出現位置を確かめます。正本の鍵ごとの出現位置が取れました。ご指摘の型が複数あります。数を詰めます。正本の鍵から、ご指摘の型を複数見つけました。数も出ました。公開の置き場に候補一覧があるとのことなので、確認を試みます。リポジトリの README に、B の正本へ移っていない**プログラム全体の柵**を見つけました。正本と突き合わせます。正本の突き合わせが終わりました。追補への答えを票として作成します。追補1 への票を作りました（判定は**条件つき**のまま、条件を並べ直しています）。ご指摘の型で三件、重いものが出ました。

**重大-16: プログラムの柵が B の正本に移っていない。** 追補を受けて公開の置き場の README を読んだところ、プログラム全体の柵の第 4 項が「各セルは（破局率／refuse 率／書式外率）の三つ組で報告し、率の単独引用を禁じる」と定めていました。B の正本には `trial_record` の「機械判定（三つ組）」という記録項目があるだけで、報告を三つ組で出す規則も、refuse 率・書式外率という語もありません。先の票の重大-1 は、私が外から持ち込んだ懸念ではなく、**この柵が B に移っていないこと**でした。直し方は既にプログラムの中にあります。

補強材料もあります。書式外は追補 V′（83,200 試行）・追補 M（221,760 試行）・段階 F（30,600 試行）でいずれも零でしたが、これはすべて介入なしの API 走行です。段階 A のパイロットで書式外が出たのは 0.6B の 13 セル（測定不能）だけ——器が弱ったときです。B は係数 2.0 まで置いて活性を加減する走行なので、その実績は移りません。

**重大-17: ランダム方向を調整走行と本走行で引き直すかが未登録。** seed は 71001 の一つだけなので、素直に実装すれば同じ 3 本を使い回します。ベクトルは固定物なので、**選定の偏りは試行を新しくしても消えません**。試算では、3 本に層・係数ごとの癖が σ=5 pt あるだけで、v̂ が普通のランダム方向と何ら変わらない世界でも本走行の札が名目 0.23／Holm 初段 0.13 で立ちます。直すのに試行は一つも増えません。

**重大-18: 生成の設定が `identity_screen` の中にしかない。** temperature・top_p・max_tokens は正本で各一箇所、すべて同一性選別の節です。調整走行・本走行・品質床の設定は登録されていないので、束縛にも登録検査にもかかりません。

追補 1 の 1 については、**採点の仕方と不可分**だと考えます。強制デコードなら「EOS まで」は移せず、帯は位置の語で書く必要があります。加えて帯の長さが本走行と桁違いなので、介入の総量が小さく、**門が甘い側にずれます**。強制デコードを推しますが、その場合は書式の崩れを見る門が設計から消えるので、重大-1 と重大-16 を必ず同時に閉じてください。

中では、正本の `arms.noop`（3 本）と `noop_by_scenario` の和集合（4 本）が食い違っており、P190 で入れた自己検査がどちらの鍵を読むかで転記行 A が 6,600 と 6,400（総 23,280 と 23,080）に分かれる点を挙げました。一段目と同じ型が同じ場所で再発する経路です。
