# 凍結の前の登録者裁定 D45〜D47 の反映の事前登録（運用の解釈の全項の確認・§0-8 の追記・予想の様式・2026-09-15・コーディネータ）

- 性格: 提案 `records/A/freeze-prep/freeze-prep-proposal-A.md`（コミット 5673e48・公開済み）への登録者裁定を、正本・器材・草案9・報告雛形に反映する**前に**、変える範囲・確かめの条件・決め方・予想を書いた記録。器を書き始める前にローカルでコミットする（push は登録者の確認の後）。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 段階: 凍結前。パイロットの前・予想の封印の前。

## 1. 登録者の言葉（逐語・挨拶の句を除く・時刻は UTC）

1. 2026-09-15T11:34Z「三点の裁定は、ご推奨の通りで進めてください。そして、f9dd5e5と5673e48のpush をよろしくお願いします」

- 時刻は、コーディネータが受け取って push の前に取った値（11:34:07Z）。
- 裁定の番号: D45 運用の解釈の全項を書かれたとおりに確認する／D46 §0-8（率盲検の外の経路）に、門0.5 の手元の率と試し読みの鍵を足す（提案 §2-2 の文案）／D47 予想の様式の案 B（提案 §2-3）。
- push: f9dd5e5 と 5673e48 を push し（a1435a7..5673e48）、公開の raw の SHA16 を変わった 4 ファイルで照合して 4 一致。

## 2. 変える範囲（予定）

A. 正本の生成器 `tools/make_contrasts_A.py` v2.8（v2.8 の区画で書き足す・既存の文字列は上書き）
- `procedure` の添字 8 の項に、二回目の試し読みの済みと、運用の解釈の全項の確認（D45）と、§0-8 の追記と予想の様式（D46・D47）。添字 10 の項に、予想の封印の順と様式（D47）。項の数は変えない。
- `tooling_interpretations.status` に全項の確認（D45）。項（items）は変えない。
- `predictions` の新設（D47）: 様式の置き場と版・生成器と照合の器・予想者・封印の順と独立・欄（向き・床持続・確証の本数の帯・測れた効果種の数の帯・情報状態・自由記述）・選択肢・「予想しない」・自動入力なし・照合の規則・柵・予想の欄の数。帯の端は数の配列で置く（生成器の文字列リテラルに数を書かない）。
- 登録者裁定の一覧 D45〜D47（`registrant_decisions_D45_D47`）。

B. 予想の様式の生成器 `tools/make_predictions_form_A.py`（新規）→ 様式 `records/predictions/predictions-form-A-v0.8.html`。SHA-256 の JS は段階 V′ の様式 v0.5 から逐語で流用し、様式の名・プログラム・正本の版・保存の名の四つだけを置き換える（段階 F と M と同じ）。

C. 照合の器 `tools/compare_predictions_A.py`（新規・自己検査つき）: 封印予想の JSON（一つ以上）と集計器の出力を、正本の `predictions.compare_rules` で照合し、`records/A/predictions-check-A.{md,json}`（`summary` を持つ）に書く。

D. 凍結器 `tools/freeze_A.py` v3.1: 凍結範囲に B と C の器・様式・様式の JS の出所（`records/predictions/predictions-form-Vprime-v0.5.html`）を足す・自己検査に C を足す・凍結範囲の一覧を印字する口（`--list`）。

E. 設計事実の器 `tools/design_facts_A.py` v3.5: 転記行 J の器材の対応表と実在の一覧に B と C を足す。

F. 報告の組み立て器 `tools/build_report_A.py` v2.4 と報告雛形の原稿: 封印予想の照合の行の柵の文言を「帯の的中は誰の判断の重みも変えない」から「的中は独立の確認ではなく、誰の判断の重みも変えない」に改める（段階 A の様式の欄は帯だけではない）。

G. 運用の解釈の器 `tools/tooling_interpretations_A.py` v1.3: 題名・読み方・COI の行の「確認待ち」「凍結確認の直前に受ける」の文言を、全項の確認（D45）に改める。項の本文は変えない。

H. 草案9 の原稿: 冒頭の題名・起草・状態・位置づけ・正本の行／§0-8（D46）／§2.13 の器の一覧（予想の様式と照合の器・変える器の版の表記を現行に合わせる）／§2.15 に予想の封印の段（D47）／§5 に D45〜D47・運用の解釈の段・凍結の前に残る手順／§7。

I. README（提案と確認の一覧・本反映）。

J. 格子の再走 → 設計事実 → 草案9 と雛形の組み立てと数の検査 → 運用の解釈の記録 → 凍結器の `--check` → 確かめ（`tools/verify_freeze_prep_A.py`）→ 反映の記録 → コミット。

## 3. 確かめの条件（反映の後に機械で走らせる・`tools/verify_freeze_prep_A.py`）

- Q1 正本: v2.8 と v2.7（コミット 5673e48 の `design/contrasts-A.json`）の葉の差が、変わった葉 `generator`・`procedure`・`tooling_interpretations.status` と、足された葉 `predictions.*`・`registrant_decisions_D45_D47.*` だけ・消えた葉なし・整合検査の結果が同じ・`procedure` の項の数が同じ・`tooling_interpretations.items` が同じ。
- Q2 格子: 正本 v2.8 で走らせた格子の全節の値が現行の格子と一致し（入力の SHA16・生成の時刻・所要時間・版を除く）、入力の正本の SHA16 が現物と一致し、quick でない。
- Q3 設計事実: 本文の差が転記行 J だけで、J の差は SHA16 の文字列と、足した器の対応表の一句（予想の様式と照合）と実在の一覧の二つの器だけ・検査用の印なし。
- Q4 草案9 と雛形の数の検査の違反が零・組み立てた草案9 の §6 の見出しの正本 SHA16 が現物と一致・§0-8 に D46 の句（門0.5 の手元の率・試し読み二回の断片の腕と機械の判定・封印する予想をこれに寄せる・予想の様式の情報状態の欄）・§5 に D45・D46・D47 の行・§2.15 に予想の封印の段。
- Q5 運用の解釈の記録: 組み立て直すと記録と一致し、項は 37 で、各項の節（「## 1.」から COI の見出しの前まで）が反映の前の記録（コミット 5673e48）と同じ・題名に「確認待ち」が無く D45 がある。
- Q6 様式: 選択の欄の数が 向き 35・床持続 15・全体 2・情報状態 7・予想者 1（期待の数は正本から数える）・`data-k` の集合が正本から組んだ集合と一致・選択肢が正本の選択肢と一致・予想の欄の数が `predictions.n_prediction_fields` と一致・JS が V′ 様式 v0.5 の JS と置き換えた四つの文字列のほかは同じ・自動入力の口が無い・柵の一行がある。
- Q7 ブラウザ: 様式をブラウザ（アプリ内のブラウザ）で開き、決まった規則で全欄を埋めて「生成」を押したときに様式が表示した SHA-256 が、(i) ブラウザ標準の `crypto.subtle` の SHA-256 と一致し、(ii) 同じ規則で Python が組んだ JSON の文字列の UTF-8 の SHA-256 と一致する。
- Q8 照合の器: 自己検査が通る・合成の集計の記録（二回目の反映の確かめで使ったもの）と Q7 の規則の予想の JSON を照合した件数（種別ごとの的中・外れ・照合不能・予想しない）が、確かめの器が集計の記録から別に数えた件数と一致する・欄が欠けた JSON と選択肢の外の値を持つ JSON で止まる・すべて「予想しない」の JSON は予想しないだけになる。
- Q9 凍結器: `--check` の欠けが凍結本文の二つ（`design/design-stageA-FROZEN.md`・`design/design-stageA-FROZEN.src.md`）だけで枠の検証が一致・凍結範囲が反映の前（コミット 5673e48 の凍結器）より四つ多く、その四つが B と C の器・様式・様式の JS の出所。
- Q10 自己検査と報告: 凍結器の自己検査（照合の器を含む）がすべて通る・報告の組み立て器 v2.4 が照合の記録を機械の区画に置き、改めた柵の文言がある。

## 4. 決め方

- Q2 で格子の値に差が出たら、格子を記録に写さずに止め、登録者に諮る。
- ほかの条件が通らなければ、器（条件ではない）を直して、影響する段から走らせ直し、直した件と原因を反映の記録に書く。
- 条件そのものの誤りに気づいたら、条件は動かさずに外れとして記録し、補助の確かめを足す（一回目の反映の K7 と同じ扱い）。
- Q7 で様式をアプリ内のブラウザで開けなければ、ローカルの静的なサーバを経て開く。それでも開けなければ Q7 を未実施として記録し、登録者に諮る。

## 5. 予想（反映の前・信頼度つき）

- (a) Q2 の格子の値の差は零（信頼度 高）。
- (b) Q3 は満たされる（信頼度 中〜高）。
- (c) 草案9 の最初の組み立てで、数の検査の違反が一件以上出る（信頼度 中）。
- (d) Q7 の三つの SHA-256 は一致する（信頼度 高）。
- (e) 照合の器の自己検査は、一回目の走行では通らない（信頼度 中）。
- (f) 反映の途中で、コーディネータ自身の見落としによる直しが二件以上出る（信頼度 中〜高）。
- (g) 確かめの全条件の合格とコミットが、裁定の受け取りから四時間以内（2026-09-15T15:34Z まで）に終わる（信頼度 中〜低）。

## 6. COI（事実のみ）

- 引かれる向き: 凍結へ早く進みたい。確かめの条件を通りやすい形に置きたい。照合の器の件数の確かめを、器と同じ書き方で済ませたい。
- 置いた印: 条件と予想を器の前にコミットする。Q8 の期待の件数は、確かめの器が集計の記録から別の書き方で数える。Q7 はブラウザで実際に生成した値で見る。

## 7. この反映で確かめないこと

- 登録者の Chrome での様式の動き（アプリ内のブラウザとは別）と、記入の分かりやすさ。
- 照合の器が本走行の集計器の出力で動くか（合成の集計の記録の形でだけ見る）。
- 予想の欄の選び方（何を当たり外れの単位にするか）が、的中の数えに偏りを入れないか。
- 反映した本文・器材・様式は外の目を通らない（この後に検分の巡を置かない・登録者決定）。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
