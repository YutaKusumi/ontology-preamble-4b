# 凍結の前の登録者裁定 D45〜D47 の反映の記録（運用の解釈の全項の確認・§0-8 の追記・予想の様式・途中の裁定 D48・2026-09-15・コーディネータ）

- 性格: 事前登録 `preregistration-freeze-prep-A.md`（コミット 2736c2d・器を書く前）に沿って反映した結果と、登録者に諮ることの記録。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 確かめ: `verification-freeze-prep-A.md`（機械生成）。事前登録の条件は 9/10 合格。Q7 は不合格（ブラウザ標準の SHA-256 との突き合わせが未実施）。事後に足した補助の Q7b は合格。

## 1. 不利な材料（先に）

1. **Q7 は事前登録の条件どおりには通っていない。** アプリ内のブラウザは様式を data: の写しで開き、安全な文脈にならないので、crypto.subtle（ブラウザ標準の SHA-256）が無かった。事前登録 §4 の決め方どおりローカルの静的なサーバで開き直そうとしたが、プレビューの器はリポジトリに置いた起動の設定ではなく、別のプロジェクトの設定（名「ryokai-os-dev」・ポート 3000）に解決して、そのサーバを起こした。コーディネータはすぐに止め、置いた設定のファイルを消した。条件は動かさず、Q7 を不合格（(i) 未実施）として残し、登録者に諮る。
2. Q7b は、Q7 の (i) が行えないと分かった後に足した補助の確かめで、事前登録の条件ではない。様式の JS の SHA-256 の実装を、既知の文字列の組で Python の hashlib と突き合わせたもので、ブラウザ標準の実装との突き合わせの代わりにはならない。
3. 予想の様式の欄と照合の規則は、門0.5 の手元の率と試し読みの鍵がコーディネータの目に入った後に選んだ（§0-8 に開示・裁定 D46）。様式・照合の器・反映の文言は外の目を通っていない。
4. 反映の途中で、事前登録の範囲（§2 H）の外の文を草案9 の原稿に足した。裁定 D48（API 再走行の費用の見込みは走らせる前に出す・登録者の申告）を §5 に一行足し、Q7 の (i) が行えなかったことに合わせて §7 の「確認していないこと」の一文を改めた。どちらも正本と確かめの条件に関わらない。
5. 照合の器の確かめ（Q8）の別の数えは、同じ正本の文言からコーディネータが書き起こしたもので、規則の読み違いが器と確かめの二つに同じく入る場合は見分けない。

## 2. 登録者の言葉（逐語・挨拶の句を除く・時刻は UTC）

1. 2026-09-15T11:34Z「三点の裁定は、ご推奨の通りで進めてください。そして、f9dd5e5と5673e48のpush をよろしくお願いします」
2. 反映の途中の問い「確認なのですが、今回の検証では、APIは使うのでしょうか？」
   - コーディネータの答えの要点: 今回の検証（反映の確かめ・格子の再走を含む）は API を使わない。使う器 19 本を検索し、鍵の読み込みと API の呼び出しが無いことを確かめた。通信は凍結器の公開物の照合（GitHub の公開ファイルを鍵なしで読む）だけで、今回は使わない。段階 A で API を使うのは手順の API 再走行だけ。正本の費用の節に、API 再走行の費用の見込みは無い。
3. 2026-09-15T12:35Z（受け取って草案9 の原稿に書いた時刻）「私が使っているNSCALEでは、Qwen 3の8B・14B・32Bがありますので、予定通りで差し支えありません。API 再走行の費用の見込みについては、ご推奨どおり、API再走行の前に見込みを出していただくことにします。後々、よろしくお願いします」
   - 裁定の番号: D48（API 再走行の費用の見込みは、凍結の前に正本へ置かず、走らせる前にコーディネータが出して登録者に諮る）。事業者と要求の設定と重みの版の確かめは、`api_rerun.provider_rule` のとおり走らせる前に行う。

## 3. 経過（UTC）

- 11:34 裁定の受け取り。f9dd5e5 と 5673e48 を push し（a1435a7..5673e48）、公開の raw の SHA16 を変わった 4 ファイルで照合して 4 一致。
- 11:42 事前登録をコミット（2736c2d）。
- 11:50 予想の様式の生成器 `tools/make_predictions_form_A.py` と照合の器 `tools/compare_predictions_A.py` を書いた（新規）。
- 12:01 既存の七つのファイルへの差し替え 49 件を、全部か無しかの一時置き場の器で当てた。正本 v2.8 を生成（SHA16 F0C2FF897C78C4C0）。正本の説明文・原稿の束縛・生成器の文字列リテラルの数の検査を先に回して違反 0。様式を生成（SHA16 2B16E8247A86159C）。照合の器の自己検査は一回目で合格。凍結器の `--check` で対象 87・欠けは凍結本文の二つ。
- 12:02 格子の再走を始めた（13:13 に終わった・4,286.3 秒）。
- 12:02〜12:10 Q7: アプリ内のブラウザで様式を開き、決まった埋め方で生成した（様式の SHA-256 0C86C59799B62D84E71E79C86867CE434372AC28C824F06CE7FF4AB428EA504C）。crypto.subtle が無い。静的なサーバの起動で別のプロジェクトのサーバが起きた（止めた）。確かめの器に Q7b を足した。
- 12:35 裁定 D48 を草案9 の原稿に書いた。
- 13:14〜13:15 格子の突合（差 0）→ 格子を記録に写す → 設計事実 → 草案9 と雛形の組み立て（違反 0）→ 運用の解釈の記録 → 凍結器の `--check` → 確かめ（事前登録の条件 9/10・補助 Q7b 合格）。

## 4. 確かめの結果（`verification-freeze-prep-A.md` の要点）

- Q1 正本の差: 変わった葉は `generator`・`procedure`・`tooling_interpretations.status`、足された葉は `predictions` と `registrant_decisions_D45_D47` の下だけ（75）。整合検査の結果・`procedure` の項の数・運用の解釈の項は同じ。
- Q2 格子: 全節の値の差 0。
- Q3 設計事実: 本文の差は転記行 J だけ（SHA16 と、足した二つの器）。
- Q4 草案9 と雛形: 数の検査の違反 0・§0-8 と §5 と §2.15 の句がある・§6 の見出しの正本 SHA16 が現物と一致。
- Q5 運用の解釈の記録: 37 項・各項の節は反映の前と同じ・題名に D45。
- Q6 様式: 向き 35・床持続 15・全体 2・情報状態 7・予想者 1・予想の欄 52。JS は V′ 様式 v0.5 と、四つの置き換えのほかは同じ。
- Q7 **不合格**: 様式の SHA-256 と、同じ埋め方で Python が組んだ JSON の SHA-256 は一致（5,388 バイト・66 鍵）。ブラウザ標準の SHA-256 は未実施。
- Q7b（事後の補助）合格: 既知の文字列 12 組で、様式の JS と Python の SHA-256 が一致（abc は BA7816BF8F01CFEA414140DE5DAE2223B00361A396177A9CB410FF61F20015AD）。
- Q8 照合の器: 自己検査が通り、合成の集計の記録での件数（向き 的中 5・外れ 7・照合不能 23／床持続 的中 7・外れ 8／全体 的中 1・外れ 1）が別の数えと一致。欠けと選択肢の外の JSON で止まり、すべて予想しないの JSON は予想しないだけ。
- Q9 凍結器: 凍結範囲は 83 から 87（器二つ・様式・様式の JS の出所）。欠けは凍結本文の二つ・枠の検証は一致。
- Q10 凍結器の自己検査（照合の器を含む）がすべて通り、報告の組み立て器 v2.4 が照合の記録を機械の区画に置き、改めた柵の文言がある。

## 5. 予想の照合（事前登録 §5）

- (a) Q2 の格子の値の差は零（信頼度 高）＝的中。
- (b) Q3 は満たされる（信頼度 中〜高）＝的中。
- (c) 草案9 の最初の組み立てで数の検査の違反が一件以上出る（信頼度 中）＝外れ。組み立ての前に原稿と生成器の数の検査を回して違反 0 を見ており、組み立ても違反 0。
- (d) Q7 の三つの SHA-256 は一致する（信頼度 高）＝照合できない。ブラウザ標準の SHA-256 は未実施。様式と Python の二つは一致。
- (e) 照合の器の自己検査は一回目では通らない（信頼度 中）＝外れ（一回目で合格）。
- (f) コーディネータ自身の見落としによる直しが二件以上出る（信頼度 中〜高）＝的中（§6 の三件）。
- (g) 確かめの全条件の合格とコミットが 15:34Z までに終わる（信頼度 中〜低）＝外れ。全条件の合格が満たされない（Q7）。反映のコミットは本記録と一緒に行う（時刻はコミットの記録を見る）。

## 6. コーディネータ自身の見落としと直し

1. アプリ内のブラウザで様式を開けばブラウザ標準の SHA-256 が使えると見込み、画面が data: の写しになること（安全な文脈にならないこと）を確かめずに Q7 を登録した。→ Q7 は条件を動かさずに不合格として残し、Q7b を事後に足した。
2. プレビューの器がリポジトリに置いた起動の設定を読むと見込んで起動し、別のプロジェクトの開発用サーバ（ryokai-os-dev）を起こした。→ すぐに止め、置いた設定のファイルを消した（リポジトリには残っていない）。登録者に報告した。
3. 草案9 の原稿の §7 に、確かめる前に「様式の SHA-256 の生成はアプリ内のブラウザでだけ確かめた」と、ブラウザ標準の SHA-256 との突き合わせができる前提の文を書いていた。→ 行えた範囲に合わせて文を改めた。
- 反映の前（提案の段）: 確認の一覧の器が向きの型を文言ごとに数えていた（提案の検分票に記録・コミットの前に作り直した）。

## 7. 登録者に諮ること

1. **Q7 の扱い**（事前登録 §4 の決め方で「未実施として記録し、登録者に諮る」）。
   - コーディネータの推奨: Q7 を不合格（(i) 未実施）のまま記録に残し、補助の Q7b と、封印のときの突き合わせで先へ進む。封印のときの突き合わせとは、登録者がチャットに貼った SHA-256 と、添付の JSON のファイルからコーディネータが機械で計算した SHA-256 の一致を見ることで、段階 V′ と F の封印ではこの二つが一致した。
   - ほかの手: 登録者の Chrome で様式を開き、ブラウザ標準の SHA-256 と突き合わせる。登録者の Chrome を操作するので、行うなら許可をいただく。
2. **凍結確認への移行**: 凍結候補の草案9（`design/design-stageA-draft9.md`・SHA16 42514D9A32C5BB46）を見ていただき、凍結確認に進むか。
3. **push**: 事前登録のコミット 2736c2d と、本反映のコミット。

## 8. COI（事実のみ）

- 引かれる向き: Q7 の不合格を「実質は通った」と読んで、凍結へ急ぎたい。補助の Q7b で Q7 を置き換えたい。
- 置いた印: Q7 の条件は動かさずに不合格として残し、Q7b は事後の補助と明記した。Q7 の扱いは登録者に諮る。

## 9. 確認していないこと

- ブラウザ標準の SHA-256 と、様式の SHA-256 の一致（Q7 の (i)）。
- 登録者の Chrome での様式の表示・生成・ダウンロード。
- 照合の器が本走行の集計器の出力で動くか。
- 反映の文言・様式・照合の器への外の目。
- プレビューの器が起動の設定をどこから読んだか（名「ryokai-os-dev」の設定の置き場は確かめていない）。

## 検分票

- 対象: 凍結の前の登録者裁定 D45〜D47 の反映（正本 v2.8・予想の様式 v0.8 と照合の器・凍結器 v3.1・設計事実の器 v3.5・報告の組み立て器 v2.4・運用の解釈の器 v1.3・草案9 の原稿・報告雛形の原稿）と、途中の裁定 D48 の記入。
- 段階: 事前登録あり（2736c2d・器を書く前）。Q7b・§7 の一文・D48 の記入は事後。
- 凍結物の同定: 段階 A は未凍結。反映の前の基準はコミット 5673e48。
- 盲検の状態: 率盲検の外の経路（門0.5 の手元の率と試し読みの鍵）が目に入った後に様式を作った（§0-8 に開示）。
- 敵対的検分: 確かめの条件を器の前に登録し、Q7 の不合格を条件を動かさずに残した。Q8 の件数は照合の器を使わずに数えた。格子は反映の前の格子と全節の値で突合した。
- 系統の内訳: Claude 系（コーディネータ）のみ。外の目なし（この後に検分の巡を置かない・登録者決定）。
- COI 記録: §8。
- 判定: Q7 を除く事前登録の条件は合格。Q7 の扱いと凍結確認への移行は登録者裁定要。
- 本検分が確認していないこと: §9。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
