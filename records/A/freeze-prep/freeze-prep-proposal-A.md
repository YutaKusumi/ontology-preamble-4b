# 凍結の前に残る手順の段取り（提案・登録者の裁定待ち・2026-09-15・コーディネータ）

- 性格: 登録者の指示「凍結の前に残る手順の段取りを進めてください」（2026-09-15）を受けて、草案9 §5 の「凍結の前に残る手順」のうち残る段と、そのために登録者に諮る三点（裁定 D45〜D47 の案）を、正本と器材に手を入れる前に書いた記録。裁定の後に、反映の事前登録を別に書いてから手を入れる。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 基にした版: 正本 `design/contrasts-A.json`（SHA16 677710EC040022BD・コミット 3ea9644）・草案9 `design/design-stageA-draft9.md`（SHA16 B71A55EFCCBEB896）・運用の解釈の記録 `records/A/tooling-interpretations-A.md`（SHA16 6B6AB79D76E4DD39）。

## 1. 不利な材料（先に）

1. 門0.5 の手元の率（N1 × 13 腕 × 4B-2507・`records/A/identity-screen-A.md`）と、試し読み二回の鍵（断片ごとの腕と機械の判定・同じ応答から抜いた断片・`records/A/judge-trial/` と `records/A/judge-trial2/`）は、すでにコーディネータの目に入っている。本提案の予想の様式の欄は、それを見た後に選んだ。草案9 §0-8（率盲検の外の経路）の一覧に、この経路はまだ無い（`records/A/gate05-run-A.md` §1-4）。
2. 段階 A の予想の様式は、草案9 にも正本にも定めが無い（正本 `procedure` に「凍結・予想封印・記録先行公開」の一行があるだけ）。様式の欄と照合の規則は本提案で初めて決まり、凍結前の最終検分を通らない（この後に検分の巡を置かない・登録者決定）。
3. 正本を一行でも変えると、格子の入力の SHA16 が現物と合わなくなり、設計事実の器が止まる（`tools/design_facts_A.py` の入力の照合）。凍結の直前に正本を更新するなら格子の再走が要る。前の二回の所要は 2,712.9 秒（`records/A/power-grid-A.log`）と 6,433 秒（同時に別の走行があったとき・`records/A/judge-arrangement2/judge-arrangement2-A.md` の L6）で、見積りには二度外れた既往がある（§0-4）。
4. 運用の解釈の 37 項のうち 4 項は、§0-1 (a) の引かれる向き（傾向が立ってほしい）と同じ側に効く読みである（`records/A/tooling-interpretations-confirmation-A.md`）。うち 3 項は登録者裁定 D25 で決めた読み。
5. 照合の規則を器に固定しても、どの結果を予想の欄に置くか（何を当たり外れの単位にするか）はコーディネータが選んでいる。

## 2. 登録者に諮る三点

### 2-1. 運用の解釈の全項（37 項）の確認（裁定 D45 の案）

- 一覧: `records/A/tooling-interpretations-confirmation-A.md`（元の記録から、各項の見出し・なぜ要ったか・採らなかった案・向きを器で抜き出した・文言は打ち直していない）。向きの型: 札に効かない 26 項・決まらない 5 項・同じ側 4 項・逆の側 2 項。
- 推奨: 書かれたとおりに確認する。
- とくに見ていただきたい 4 項（引かれる向きと同じ側）:
  - 10 測れた効果種の計算で、実測で外した規模を戻さない（戻すと測れる見込みが実際より高く見える。外すと測れた効果種が減り、「規模非依存」の読みに届きにくい）。
  - 26 様式門を、札を決める規模（残存規模）にだけ当てる（登録者裁定 D25）。
  - 27 解釈条項の飽和を、除外の後に残った規模で数える（登録者裁定 D25）。
  - 28 refuse 門の refuse を答えの JSON の refuse に限り、読み取れない散文の拒否は書式外に数える（登録者裁定 D25）。
  - 26〜28 は、保留や降格が減り、確証が増えうる側に効く。
- 選び直したい項があれば、その番号を挙げていただく（その項だけを反映の対象にする。読みを変えると、格子の再走と合成データの確かめをやり直す）。
- 確認の後: 正本の `tooling_interpretations.status` に全項の確認（裁定 D45）を足し、運用の解釈の記録の題名の「登録者の確認待ち」を、器（`tools/tooling_interpretations_A.py`）の版を上げて改める。

### 2-2. §0-8（率盲検の外の経路）への追記（裁定 D46 の案）

- 追記の案（草案9 §0-8 の文に太字の部分を足す・数は原稿で正本のキーに束ねる）:

> 8. **率盲検の外の経路（採否表 P104・P138・裁定 D35・D46）**: 本走行の前にコーディネータの目に入る率がある（パイロットの様式の記録の層ごとの破局数・環境帯の引き直しの腕別の率・門0 の既存の率・**凍結の前に走らせた門0.5 の手元の率〔N1 × 13 腕 × 4B-2507〕と、同じ応答から抜いた試し読み二回の断片の腕と機械の判定〔鍵を開けた後〕**）。引かれる向き＝これを見て規則の運用や報告の強調を寄せる・**封印する予想をこれに寄せる**。置いた印＝閾値と帯は動かさない（裁定 D6・D21）・門2 は機械規則・報告は雛形と走査器で組む・**予想の様式の情報状態の欄に、門0.5 の記録と試し読みの記録の読了を置く**・率盲検の器（整合検査・抽出検査）はこの経路を覆わないことをここに開示する。

- 推奨: 追記する（門0.5 の走行記録 §1-4 で、凍結確認の前に諮るとした事項）。
- 採らなかった案: 門0.5 の率だけを足し、試し読みの鍵を足さない（鍵は同じ応答の一部で、断片ごとに腕と機械の判定が見える）。

### 2-3. 段階 A の予想の様式（裁定 D47 の案）

先例は段階 F の様式 v0.7（100 欄・登録者とコーディネータの双方が封印・`tools/make_predictions_form_F.py`・`tools/compare_predictions_F.py`）。

**推奨（案 B）**: 予想の欄 52。

1. 確証の対比 35 本ごとの向き（四つから選ぶ）:「規模が大きいほど、A の破局率が B に比べて上がる側へ動く」／「下がる側へ動く」／「規模に沿った動きは区別できない（非有意）」／「予想しない」。
2. 床持続の 15 セル（O・Nk・Osec × 5 場面）の 0/1（三つから選ぶ）:「1（全 6 規模で上限が censor.low 未満）」／「0（上限が censor.low 未満にならない規模がある）」／「予想しない」。
3. 全体の 2 欄: 確証の本数（35 本中）の帯（0 本／1〜3 本／4〜8 本／9〜17 本／18〜35 本）・測れた効果種の数（7 種中）の帯（0 種／1〜2 種／3〜5 種／6〜7 種）。
4. 予想の欄に数えない欄: 情報状態（本プログラム・追補 V′・追補 M・段階 F の結果報告の読了、段階 A の凍結文書の読了、門0.5 の記録の読了、試し読みの記録の読了）・COI の自由記述（望む向きを先に書く）・自由記述・予想者（登録者／コーディネータ）・日付。

- 自動入力: 置かない（6 規模の既測が無い。4B-2507 の既測は並びの線に載せない別記号の点）。
- 照合の規則（照合の器に固定し、結果を見てから変えない）:
  - 向き: 本走行の札が「確証」で β₃ が正なら「上がる側」、負なら「下がる側」。「非有意」なら「区別できない」。記述（解釈条項・対数オッズ尺度でのみ）・判定保留（refuse 転位・様式転位・環境）・判定不能は「照合不能」。
  - 床持続: 集計器の 0/1 をそのまま写す（0/1 が無いセルは「照合不能」）。
  - 全体: 札の数えの確証の本数と、測れた効果種の数を帯に写す（測れた効果種の計算が無ければ「照合不能」）。
  - 「予想しない」は照合しない。的中は独立の確認ではなく、誰の判断の重みも変えない。照合は記録であり評価ではない。
- 封印の順と時機（段階 F の先例）: 凍結 → コーディネータの封印（JSON を保存し、SHA-256 をファイルから機械計算）→ 登録者の封印（ブラウザで様式を開いて記入 → JSON と SHA-256 を生成 → SHA-256 をチャットに貼り、JSON を添付）→ 凍結記録に記帳 → 記録先行公開（push は確認をいただいてから）→ パイロット。登録者が封印するまで、コーディネータの予想のファイルは開かないでいただく。
- 器材: 様式の生成器 `tools/make_predictions_form_A.py`・照合の器 `tools/compare_predictions_A.py`（合成データの集計器の出力で自己検査）・様式 `records/predictions/predictions-form-A-v0.8.html` を作り、凍結範囲に入れる（`tools/freeze_A.py` の版を上げる）。正本に `predictions` の節を足す。

他の案:

- 案 A（最小）: 全体の 2 欄と COI だけ。記入は短いが、対比ごとの向きの予想が残らない。
- 案 C（詳しい）: 案 B に、腕 × 場面 × 規模の破局率の帯（13 × 5 × 6＝390 セル）を足す。自動入力の出所が無く、手で 390 セルを埋めることになる。

## 3. 裁定の後の段取り（見込みはコーディネータの見積り・外れの既往あり）

| 順 | 段 | する人 | 見込み |
|---|---|---|---|
| 1 | 反映の事前登録（変える範囲と確かめの条件を先に書いてコミット） | コーディネータ | 20 分 |
| 2 | 反映: 正本 v2.8（`procedure` の済の印・`tooling_interpretations.status`・`predictions` の節・裁定 D45〜D47）・草案9 の原稿（§0-8・予想の様式の段・§5・§7）・様式の生成器と照合の器・`freeze_A.py` と `tooling_interpretations_A.py` の版上げ・README | コーディネータ | 2 時間 |
| 3 | 格子の再走（値が変わらないことを突合で確かめる） | 器 | 45 分〜1 時間 50 分 |
| 4 | 設計事実 → 草案9 と雛形の組み立て（本文の数の検査の違反 0）→ 運用の解釈の記録 → `freeze_A.py --check`（欠けは凍結本文だけ）→ 確かめの記録とコミット | コーディネータ | 30 分 |
| 5 | 凍結確認（凍結候補の草案9 を見て、凍結の可否を決める） | 登録者 | — |
| 6 | 凍結: 草案9 を `design/design-stageA-FROZEN.md` に逐語で写す（題名と凍結行だけ改める）→ `freeze_A.py` でマニフェストを発行（自己検査つき）→ `--verify` → 凍結記録に記帳 | コーディネータ | 30 分 |
| 7 | 予想の封印（コーディネータ → 登録者）と記帳 | 両者 | コーディネータ 30 分・登録者の記入の時間 |
| 8 | 記録先行公開（push の確認をいただいてから）→ `freeze_A.py --verify-public` | コーディネータ（push は登録者の確認の後） | 15 分 |
| 9 | パイロット（Colab・登録者の Chrome 越し） | 両者 | 凍結の後の別の段 |

- 裁定から凍結候補まで（1〜4）: 3〜4 時間。凍結確認から公開まで（6〜8）: 1〜1.5 時間（登録者の記入の時間を除く）。

## 4. COI（事実のみ）

- 引かれる向き: 凍結へ早く進みたい。37 項を「書かれたとおりに確認」に寄せたい（選び直しは格子の再走と遅れを招く）。予想の様式を小さく保ちたい（器材が増えない側）。§0-8 の追記を短くしたい。
- 置いた印: 同じ側の 4 項を名指しで先に挙げ、逐語を一覧の後段に置いた。案 A と案 C を並べ、それぞれの手間と欠けを書いた。§0-8 の追記に、試し読みの鍵と「封印する予想をこれに寄せる」向きまで入れた。

## 5. 本提案が確認していないこと

- 37 項の一つ一つの読みが、本物の出力で札をどれだけ動かすか（本走行の前には測れない）。
- 予想の欄の選び方（何を当たり外れの単位にするか）が、的中の数えに偏りを入れないか。
- 格子の再走と反映の実際の所要（同時の走行の有無と、反映の中で見つかる直しで変わる）。
- 登録者が門0.5 の記録と試し読みの記録の率をどこまで読んだか（様式の情報状態の欄で自記していただく）。
- 照合の器が本走行の集計器の出力で動くか（合成データの集計器の出力の形でだけ確かめる予定）。

## 検分票

- 対象: 凍結の前に残る手順の段取りと、登録者に諮る三点（裁定 D45〜D47 の案）。
- 段階: 事前（正本と器材に手を入れる前・裁定の前）。
- 凍結物の同定: 段階 A は未凍結。基にした版は冒頭の三つの SHA16。
- 盲検の状態: 率盲検の外の経路（門0.5 の手元の率と試し読みの鍵）がコーディネータの目に入った後に書いた（§1-1）。
- 敵対的検分: 正本を変えると格子の再走が要ることを、設計事実の器の入力の照合の行で確かめた。照合の規則が集計器の出力に写せるかを、合成データの集計器の出力の欄（札・β₃・測れた効果種・床持続の 0/1）で確かめた。同じ側の 4 項は器で抜き出して逐語で並べ、件数は型ごとに器で数えた（最初に作った一覧は同じ型を文言ごとに二行に分けて数えていたので、型ごとに数え直す形に器を直して作り直した・最初の一覧はコミットしていない）。
- 系統の内訳: Claude 系（コーディネータ）のみ。系統外の目は通していない（この後に検分の巡を置かない・登録者決定）。
- COI 記録: §4。
- 判定: 登録者裁定要（D45〜D47 の三点）。
- 本検分が確認していないこと: §5。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
