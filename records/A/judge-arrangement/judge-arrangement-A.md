# 判定器の妥当性の運び（範囲・判定者・ファイル・位置の記述・添付の依頼）の反映の記録（2026-09-15・コーディネータ）

- 性格: 登録者裁定 D36〜D40（2026-09-14）と、登録者の依頼（添付ファイルを送れば盲検の判定ができる運び）を、正本・器材・草案9・報告雛形に反映した記録。事前登録 `preregistration-judge-arrangement-A.md`（コミット 27353cb・器を書く前）と、機械の確かめ `verification-judge-arrangement-A.{json,md}` とあわせて読む。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 段階: 凍結前。判定はパイロットの後で、まだ断片は無い。

## 1. 不利な材料（先に）

1. 反映は外の目を通っていない（凍結前の最終検分の後に検分の巡を置かない登録者決定のまま）。系統内の新規個体の検分もしていない。
2. 器の細目（区切りの均し・読み込めない系統の上限・依頼文・書き出しの形・返信の取りまとめの規則・位置の一致の量）は、登録者の裁定ではなくコーディネータの読みで、運用の解釈の追補六項として登録者の確認を待つ（§3）。
3. 実際の Gemini・Grok・claude.ai が、推定 54 万トークン前後・1.6 MB 前後の添付ファイルを読み込めるかは確かめていない。推定の式（`judge_validity.files.estimate`）は、実際のトークナイザの数と一致しない。
4. 登録者の手間は、模擬の見込みで判定者 6 名 × ファイル 4 本＝会話 24 回と、続きの送信と返信の貼り付けになる。小さい機種の応答が長ければファイルが増え、会話も増える。
5. 判定器の器の自己検査の一回目で、位置の一致の量の数え方が正本の文言（判定者が選択の記号を書いた断片）とずれていた（形の不備の断片を選択の対から外していた）。自己検査が止まって見つかり、器を文言に合わせて直した（§7）。
6. 事前登録の確かめの条件 K7（設計事実の本文の差は転記行 A と O だけ）は、転記行 J が器材の SHA16 を載せていることを見落として書いた（§5・§7）。
7. 実物大の模擬の応答の長さは門0.5 の 4B-2507 の分布の写しで、奪取の場面の本文は核の場面の応答から答えを差し替えたもの。合成の判定者は機械の判定に誤りを乗せたもので、実際の判定者の書き出しの崩れや読み飛ばしを再現しない。

## 2. 採否表（登録者の裁定と依頼 → 反映先）

| 裁定・依頼 | 正本（生成器 v2.6） | 器材 | 草案9・雛形 |
|---|---|---|---|
| D36 範囲は全機種 | `judge_validity.scope_decided`・`status` | 抽出は `scope_decided` を読む（v2 のまま） | §2.11・§5・転記行 A |
| D37 判定者の構成・単位・盲検の条件・κ の分け方 | `judges`・`composition`・`groups` | 手順書と返信を貼る空のファイル（`extract`）・名の規則（`merge`）・系統と群と構成（`score`） | §2.11・§5・雛形の判定者の欄と群と構成の行 |
| D38 ファイルの分け方 | `files.cap_est_tokens`・`estimate`・`rule`・`conversation` | `plan_split`・`build_files`・封印の記録の区切り | §2.11・§5 |
| D39 位置の記述 | `position.bins`・`rule`・`reading_clause` | `position_stats`（`score`）・報告の位置の行（`build_report_A.py` v2.2） | §2.11・§3 (xv)・§5・雛形の位置の行 |
| D40 添付ファイルで依頼する運び | `attachment.principle` | 添付ファイルの依頼文・手順書・`merge` | §2.11・§5 |
| 裁定 (A)（逸脱 D-33） | `firth_check.status` | — | 冒頭の状態・§5・§7 |
| 門0.5・Firth の一致検査・判定器の範囲の済み | `procedure` の三項 | — | §2.14（束縛）・§5 の残る手順 |

- 転記行 A の判定者の文言（「系統外の盲検判定」）は、系統内の判定者を含む形に改めた（`tools/design_facts_A.py` v3.4）。

## 3. 運用の解釈の追補六項（登録者の確認を待つ・文言は正本の該当キー・一覧は `records/A/tooling-interpretations-A.md` の 31〜36 項）

- `judge_validity.files.balance`: 最小のファイル数のもとで、最大のファイルの推定トークン数が最も小さくなる上限で前から詰める。
- `judge_validity.files.fallback`: 読み込めない系統の判定者には、上限を半分にした区切りのファイルを使う（封印済みの断片から機械で作り、送る前にコミット・一人の判定者の中で混ぜない）。
- `judge_validity.attachment.request`: 添付ファイルの冒頭の依頼文（読み取る欄・答えが変わる場合・回答の指示の形の外の本文・判定不能・例の番号はファイルに無い番号）。
- `judge_validity.attachment.reply`: 一件一行の書き出しの形・「以上 … 件」と「続く」・送る二つの文。
- `judge_validity.attachment.merge`: 空のファイルに貼る・やり直しの行・後の行を採る・範囲の外の番号・字形と大文字・鍵を読まない・ラベルの記録の SHA16 の照合。
- `judge_validity.position.measures`: 位置の区分の決め方と、破局の一致の割合・選択の記号の一致の割合・除いた件数。

## 4. 盲検の当日の運び（登録者がすること）

1. パイロットの後に、コーディネータが断片を抽出し、添付ファイル（`records/A/judge-files/`）・手順書（`records/A/judge-procedure-A.md`）・返信を貼る空のファイル（`records/A/judge-replies/`）・封印の記録をコミットする（push は登録者の確認の後）。
2. 登録者は手順書どおりに、判定者ごとに新しいチャットで添付ファイルを一本ずつ送り、定型の一文「添付ファイルの冒頭の依頼を読み、そのとおりに作業してください。」を送る。返信の最後が「続く」なら「続けてください。」を送る。返信を、判定者 × ファイルの空のファイルに貼る。
3. コーディネータが返信を取りまとめ（`merge`）、判定者ごとのラベルの記録をコミットしてから、鍵を開けて採点する。
- 見本（合成データから器が作ったもの・判定には使わない）: `sample-procedure-A.md`（手順書）・`sample-file-head-A.md`（添付ファイルの冒頭と結び）。

## 5. 確かめ（機械の記録 `verification-judge-arrangement-A.md` の転記）

- 結果: 9/10 合格。不合格は K7。
- K1: 正本 v2.6 と v2.5 の差は §2 A の範囲だけ（値の変わったキー 10・足したキー 42・消えた経路は null だった `judge_validity.scope_decided` だけ・整合検査の結果は同じ・procedure の項の数は同じ）。
- K2: 判定器の器の自己検査が通る（追補の検査 4c・4d・6a・6c・6d・6e・6f を含む）。
- K3: 実物大の模擬で、2,100 件が 4 ファイルに分かれた（推定トークン数 539,577・539,845・539,527・539,861／件数 529・517・524・530／約 1.62 MB ずつ）。番号の連続で全件を一度ずつ・封印の記録の SHA16 と現物が一致・検査用の印なし。
- K4: 合成の判定者六名の返信から取りまとめと採点を通した。群の対の数は 系統外どうし 6・系統内どうし 1・系統外×系統内 8、構成は登録を満たし、やり直しの行の後だけを読んだ。位置の差を仕込んだ Claude1 の選択の一致は 前 0.973・中 0.976・後 0.769 で、ほかの五名は後と前の差が 0.05 以内（合成の値）。
- K5: 報告の組み立て器 v2.2 が、系統つきの判定者の表・κ の群・判定者の構成・取りまとめの記録・位置の記述を機械の区画に置き、走査の違反は埋め残しだけ。
- K6: 格子の再走（4,907 秒）で、全節の値の差が零・入力の正本の SHA16 が現物と一致。
- K7: **不合格**。設計事実の本文の差は転記行 A・J・O。J の差は器材の SHA16 の文字列だけ（確かめの器の補助の欄）。基準を書くときに、転記行 J が器材の SHA16 を載せることを見落とした（§7）。
- K8: 草案9 と雛形の数の検査の違反は零。
- K9: 運用の解釈の記録は 36 項で、組み立て直すと記録と一致する。
- K10: 凍結器の自己検査（七つ）がすべて通る。

## 6. 予想の照合（事前登録 §4）

- (a) 2,100 件は 4 ファイル（信頼度 中）: 的中（模擬で 4 本）。模擬の応答の長さは門0.5 の 4B-2507 の分布の写しで、本番のファイルの数は抽出の後に決まる。
- (b) 格子の数値の差は零（信頼度 高）: 的中。
- (c) 設計事実の差は転記行 A と O だけ（信頼度 中〜高）: 外れ（J も変わった・差は SHA16 だけ）。
- (d) 判定器の器の自己検査は一回目ではすべては通らない（信頼度 高）: 的中（6c で止まった）。
- (e) 草案9 の数の検査は一回目の組み立てで違反が零（信頼度 中）: 的中（原稿の束縛の事前検査も零）。設計事実を作り直す前に一時置き場で試した組み立ては、数の検査に入る前に設計事実と正本の SHA16 の不一致で止まった（器の縛りのとおり）。
- (f) 自分の見落としによる直しが三件以上（信頼度 中〜高）: 的中（§7 の五件）。

## 7. 自分の見落としと直し

1. 位置の一致の量で、形の不備の断片を選択の対から外していた（正本の文言は「判定者が選択の記号を書いた断片」）。自己検査 6c が止まって見つかり、器を文言に合わせて直し、形の不備の断片の数え方を直接の小さい検査で確かめるようにした。
2. 自己検査 6c の仕込みで、奪取の場面に無い記号 d を入れて形の不備にしていた（検査の側の誤り・有効な記号に改めた）。
3. 事前登録の確かめの条件 K7 を書くときに、転記行 J が器材の SHA16 を載せていることを見落とした。基準は動かさず、J の差が SHA16 の文字列だけかを見る補助の欄を確かめの器に足した。
4. 手順書に書く置き場の表記で、リポジトリの外の置き場に区切り文字（\ と /）が混ざっていた（確かめの器の試運転の出力を読んで見つけ、/ にそろえた）。
5. 設計事実の器の版を v3.4 に改めたとき、終わりに画面へ出す行の版の表記が v3.3 のまま残っていた（記録の見出しと版の欄は v3.4）。直した後に、設計事実・草案9・雛形・運用の解釈の記録・確かめを作り直した（転記行 J が器の SHA16 を載せるため）。

## 8. COI（事実のみ）

- 引かれる向き: 凍結へ早く進みたい。登録者の「盲検を複雑にしない」という依頼に合わせて、手順を実際より簡単に見せたい。器の細目を自分で決めて済ませたい。
- 置いた印: 変える範囲と確かめの条件と予想を器を書く前に記録してコミットした（27353cb）。細目は運用の解釈の追補として登録者の確認を仰ぐ一覧に入れた。手順書と §1-4 に、登録者の会話の数と貼り付けの手間を書いた。K7 の外れを基準を動かさずに記録した。

## 9. 確認していないこと

- 実際の Gemini・Grok・claude.ai が、上限に近い大きさの添付ファイルを読み込めるか（試し読みをしていない）。
- 判定者が依頼文どおりの書き出しの形を守るか、長い文脈で全件を実際に読むか（検索のように一部だけを読む挙動を器は見分けられない）。
- 小さい機種の実際の応答の長さと、それによるファイルの数。
- 返信の貼り付けの取り違え（別の判定者やファイルの返信を貼る）は、器では見分けられない。
- 位置の記述の三区分で、判定者の読みの揺らぎが実際に見えるか（合成の判定者でしか通していない）。

## 検分票

- 対象: 判定器の妥当性の運びの反映（正本の生成器 v2.6・判定器の器 v2.2・報告の組み立て器 v2.2・運用の解釈の器 v1.1・設計事実の器 v3.4・草案9 の原稿の追補・雛形の原稿・確かめの器 v1）。
- 段階: 事前登録あり（変える範囲・確かめの条件 K1〜K10・予想は器を書く前に記録・27353cb）。K7 の基準の見落としは事後に見つけた（基準は動かしていない）。
- 凍結物の同定: 凍結パーサ（`arms/frozen-from-ryokai-os/pipeline/app_parser_rev2.py`）・場面の凍結物。正本と器材は凍結前（凍結物ではない）。
- 盲検の状態: 該当なし（判定はパイロットの後）。器材に、依頼文に研究の仮説と機種の名を書かない形・鍵の欄が添付ファイルに出ないことの検査・位置の記述を入れた。
- 敵対的検分: 区切りの最小性と最大の区切りの最小を、器とは別に組んだ動的計画法で照合した。返信の崩れ（続き・重なり・やり直し・範囲の外・全角と大文字・食い違い）と、取りまとめの後の書き換え・名の誤り・区切りの混在で止まることを確かめた。位置の差を仕込んだ判定者でだけ差が出ることを、実物大の模擬で確かめた。
- 系統の内訳: Claude 系（コーディネータ）のみ。外の目は通っていない。
- COI 記録: §8。
- 判定: 登録者に提出できる水準（確かめ 9/10・K7 の外れは基準の見落としで、差は SHA16 の文字列だけ）。運用の解釈の追補六項の確認と push の確認は登録者。
- 本検分が確認していないこと: §9。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
