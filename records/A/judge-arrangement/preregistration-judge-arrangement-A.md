# 判定器の妥当性の運び（範囲・判定者・ファイル・位置の記述・添付の依頼）の反映の事前登録（2026-09-15・コーディネータ）

- 性格: 登録者の選択と承認（2026-09-14）と、登録者の依頼（2026-09-14T20:34Z）を、正本・器材・草案9・報告雛形に反映する**前に**、変える範囲・確かめの条件・予想を書いた記録。器材を書き始める前に書き、ローカルのコミットで時刻を固める（push は登録者の確認の後）。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 段階: 凍結前。判定はパイロットの後で、まだ行われていない（断片もまだ無い）。

## 1. 登録者の言葉（会話の記録からの逐語・挨拶の句を除く・時刻は UTC）

1. 2026-09-14T12:27Z「全機種（断片2,100）でお願いします。あと、判定者についてですが、系統外の判定者は、私の環境では、100万コンテキストウィンドウを超えるとチャットができないので、判定者に、Claude Code のエージェントやclaude.aiのClaudeを当てることもしたいです。系統内でも問題はありませんよね？」
2. 2026-09-14T12:34Z「系統外はGemini,Grokを少なくとも各二名ずつにします。もし、コンテキストウィンドウの関係で束を一括で読み込まない場合は、その都度、新規のGemini,Grokに追加で依頼することとします。系統内も加えて、claude.ai のClaudeにします。」
3. 2026-09-14T12:43Z「全員が全件、Claudeは二名、全て、一括で読み込んでも、100万コンテキストウィンドウ以内なら、１件にまとめましょう。束が多くなると、アップロードできるファイルの上限に引っ掛かって、読み込めない可能性もありますので。「あわせて登録すること」についても承知しました。」
4. 2026-09-14T20:34Z「ご提案について承知しました。反映をお願いします。なお、実際に盲検を行う際は、私から、系統内非系統外に添付ファイルとして送付して、それを読み込めばすスムーズに盲検ができるように手配をお願いします（これまでの検分と同様です。盲検を複雑にすると、私がやりかたがよくわからないためです）」

承認を受けたコーディネータの文（要点の逐語）:
- 12:37Z「あわせて登録すること」: 判定者の単位は「系統 × 番号」で、束ごとに新しい会話で読んでも同じ判定者として扱い、束と会話の対応を記録し、全員に同じ依頼文を使う／盲検の条件は、新しい会話で、メモリや個人設定を切り、ウェブ検索などの道具を使わずに読むことで、依頼文に研究の仮説や機種の名前を書かない／判定者ごとに系統を記し、κ を「系統外×機械」「系統外どうし」「系統内どうし」「系統外×系統内」に分け、系統内どうしの一致は独立の確認に数えない。
- 12:45Z「ご提案 1」: 「1 ファイルあたり推定 60 万トークン以内で、できるだけ少ないファイル数にする」。全件が上限に収まれば一つ。分け方は抽出の後に断片の番号の連続した区切りで機械が決め、判定の前に封印の記録に書く。1 ファイルは一つの会話で読み、読み取りの出力は同じ会話の中で何回かに分けて受け取ってよい。見積もりは、日本語一字を 1 トークンと数える多めの換算で、断片一件は約 1,050 トークン、2,100 件で約 220 万トークン、いまの見積もりでは 4 ファイル程度。
- 12:45Z「ご提案 2」: ファイルの中の位置（前・中・後など）ごとに、機械の判定との一致を記述として出す（閾値は置かない）。位置で一致が大きく違えば、報告でその判定者の値を割り引いて読める。

## 2. 変える範囲（予定）

A. 正本の生成器 `tools/make_contrasts_A.py` v2.6（v2.6 の区画でキーに書き足す・既存の文字列は上書き）
- `judge_validity.scope_decided`（全機種）・`status`・`judges`・`composition`（系統ごとの人数と系統外／系統内）・`groups`
- `judge_validity.files`（上限の推定トークン数・推定の式・区切りの規則・会話・区切りの均し・読み込めない系統の上限）
- `judge_validity.position`（前・中・後・規則・一致の量）・`reading_clause` に位置の記述の読みを足す
- `judge_validity.attachment`（依頼の運びの原則・依頼文・送る文・書き出しの形・取りまとめの規則・やり直しの行・手順書）
- `judge_validity.extract.fragment`（読み取る欄の名を足す）
- `firth_check.status`（一回目の不合格・裁定 (A)・逸脱 D-33・二回目の合格）
- `procedure` の門0.5・Firth の一致検査・判定器の範囲の確定の三項に「済」を足す（項の数は変えない）
- 登録者裁定の一覧 D36〜D40 と裁定 (A)（逸脱 D-33）・`tooling_interpretations` の追補六項

B. 判定器の器 `tools/judge_fragments_A.py` v2.2
- extract: 断片のファイルへの区切り（最小のファイル数・その数のもとで最大のファイルを最も小さく）・添付ファイル（冒頭に依頼文と書き出しの形）・登録者の手順書・返信を貼る空のファイル・封印の記録に区切りとファイルの SHA16
- files: 読み込めない系統のための上限の小さい区切り（封印済みの断片から作る）
- merge: 貼られた返信から一件一行を読み取り、判定者ごとのラベルの記録と取りまとめの記録を書く（鍵を読まない）
- score: 判定者の系統・κ の四つの群・判定者の構成・位置の記述・ラベルの記録の SHA16 と取りまとめの記録の照合
- selftest の追補

C. 報告の組み立て器 `tools/build_report_A.py` v2.2 と報告雛形の原稿（判定者の欄に系統・群・構成・位置の記述の行）

D. 運用の解釈の器 `tools/tooling_interpretations_A.py` v1.1（追補六項の「なぜ要ったか」「採らなかった案」「向き」）

E. 設計事実の器 `tools/design_facts_A.py` v3.4（転記行 A の「系統外の盲検判定」の文言だけ）

F. 草案9 の原稿（冒頭の状態・§2.11・§3 (xv)・§5・§7）

G. 格子の再走（正本の SHA16 が変わるため・数は変わらない見込み）→ 設計事実 → 草案9 と雛形の組み立てと数の検査 → 運用の解釈の記録 → 凍結器の検査

## 3. 確かめの条件（反映の後に機械で走らせる・一つでも外れたら直すか、直さない理由を記録する）

- K1 正本: 生成器が止まらずに書き、整合検査の結果が v2.5 と同じ。v2.5 の出力（HEAD の `design/contrasts-A.json`）と比べて、値が変わったキーは §2 A に挙げたものだけ。
- K2 判定器の器の自己検査が通る。追補の検査に、区切り（最小のファイル数・上限以下・番号の連続・全件を一度ずつ）・添付ファイルに鍵の欄（trial_id・機種・腕・機械判定）が出ない・返信の読み取り（続きの返信・やり直しの行・同じ番号の重なり・範囲の外の番号・全角と大文字・読み取れない行）・取りまとめが名の誤りと区切りの混在で止まる・κ の四つの群の数え方・位置の記述が位置に仕込んだ差を拾う・ラベルの記録を書き換えると採点が止まる、を含む。
- K3 実物大の模擬: 門0.5 の応答の長さの分布から作った合成のパイロット（7 機種 × 5 場面）から 2,100 件を抽出し、全ファイルが上限以下・番号の連続・全件を一度ずつ・封印の記録のファイルの SHA16 と現物が一致する。
- K4 実物大の模擬の続き: 合成の判定者六名（系統外四・系統内二）の返信から取りまとめと採点を通し、対の数が群ごとに系統外どうし 6・系統内どうし 1・系統外×系統内 8 で、位置に仕込んだ差がその判定者の「後」にだけ出る。
- K5 報告の組み立て器が、判定器の採点の記録から系統つきの表と群と構成と位置の行を機械の区画に置き、走査器の違反が埋め残しのほかに零。
- K6 格子の再走で、既存の節の数値が現行の `records/A/power-grid-A.json` と一致する（入力の SHA16 と生成の時刻を除く）。
- K7 設計事実の本文の差が、転記行 A（判定者の文言と範囲の状態）と転記行 O（読み条項）だけ。
- K8 草案9 と雛形の数の検査の違反が零。
- K9 運用の解釈の記録が 36 項で、組み立て直すと記録と一致する。
- K10 凍結器の自己検査（SELFTESTS）がすべて通る。

## 4. 予想（反映の前・信頼度つき）

- (a) K3 の模擬で、2,100 件は 4 ファイルに分かれる（信頼度 中。小さい機種の応答の長さを門0.5 の 4B-2507 の分布で代用するので、実物では増えうる）。
- (b) K6 の格子の数値の差は零（信頼度 高）。
- (c) K7 の設計事実の差は転記行 A と O だけ（信頼度 中〜高）。
- (d) 判定器の器の自己検査は、一回目の走行ではすべては通らない（信頼度 高）。
- (e) 草案9 の数の検査は、一回目の組み立てで違反が零（信頼度 中）。
- (f) 反映の途中で、コーディネータ自身の見落としによる直しが三件以上出る（信頼度 中〜高）。

## 5. COI（事実のみ）

- 引かれる向き: 凍結へ早く進みたい。登録者の「複雑にしない」という依頼に合わせて、手順を実際より簡単に見せたい。器の細目（書き出しの形・区切りの均し・やり直しの規則・読み込めない系統の上限）を自分で決めて済ませたい。
- 置いた印: 細目は運用の解釈として正本に書き、登録者の確認を仰ぐ一覧に入れる。手順書に、登録者がする操作の数（判定者 × ファイルの会話の数・続きの送信・貼り付け）を隠さず書く。確かめの条件と予想を、器を書く前にこの記録に置く。

## 6. この反映で確かめないこと

- 実際の Gemini・Grok・claude.ai が、上限に近い大きさの添付ファイルを読み込めるか（登録者の手元の系統の上限と、実際のトークナイザの数え方）。
- 判定者が依頼文どおりの書き出しの形を守るか、長い文脈で全件を実際に読むか（検索のように一部だけを読む挙動）。
- 小さい機種の実際の応答の長さ（ファイルの数は抽出の後に決まる）。
- 判定者の読み取りが機械の判定と一致する度合い（判定はパイロットの後）。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
