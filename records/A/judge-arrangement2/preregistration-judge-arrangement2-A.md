# 判定器の妥当性の運び 二回目の反映の事前登録（Grok を外す・照合記号・同じ系統の断り・二回目の試し読み・2026-09-15・コーディネータ）

- 性格: 試し読みの結果（`records/A/judge-trial/judge-trial-A.md`）を受けた登録者裁定（2026-09-15）を、正本・器材・草案9・報告雛形に反映する**前に**、変える範囲・確かめの条件・予想を書いた記録。器を書き始める前にローカルでコミットする（push は登録者の確認の後）。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 段階: 凍結前。判定はパイロットの後で、まだ行われていない。

## 1. 登録者の言葉（会話の記録からの逐語・挨拶の句を除く・時刻は UTC）

1. 2026-09-15T07:32Z「bc64747 と 6c7c79f を push してください。あと、盲検は、Grokは、外すことにしましょう。」
2. 2026-09-15T07:36Z「四点とも推奨どおりでお願いします」

- 四点（コーディネータの問いの要点）: (1) 照合記号を足す【推奨】——各断片の終わりに器が作る四字の記号を置き、判定者は書き出しの各行にその記号を書き写す。記号が合わない行は照合外れとして採点から外し、件数を記録する。一つのファイルで照合外れが 5% を超えたら新しい会話でやり直し、やり直しても超えたら読めなかったものとして扱う。(2) 二回目の試し読み【推奨】——照合記号つきのファイルを Gemini と Claude に一回ずつ送る。(3) 「同じ系統どうしの一致は独立の確認に数えない」を書き足す【推奨】。(4) Gemini は二名のまま【推奨】。
- 裁定の番号: D41 盲検から Grok を外す（Gemini は二名以上のまま）／D42 照合記号と照合外れの規則／D43 二回目の試し読み／D44 同じ系統どうしの一致は独立の確認に数えない。

## 2. 変える範囲（予定）

A. 正本の生成器 `tools/make_contrasts_A.py` v2.7（v2.7 の区画でキーに書き足す・既存の文字列は上書き）
- `judge_validity.composition` から Grok を外す・`judges` の文言（D41）
- `judge_validity.groups` に同じ系統どうしの断り（D44）
- `judge_validity.reading_check` の新設（`rule`・`threshold`・`code`）（D42 と運用の解釈）
- `judge_validity.attachment` の `request`・`reply`・`merge` の文言に照合記号（D42）
- `procedure` の判定器の項の文言（試し読みの済みと D41〜D43・項の数は変えない）
- 登録者裁定の一覧 D41〜D44・`tooling_interpretations` の追補一項（`judge_validity.reading_check.code`）

B. 判定器の器 `tools/judge_fragments_A.py` v2.3
- 照合記号の生成（抽出の乱数の子ストリーム・断片の記録に置く）と、添付ファイルの各断片の終わりの行への記号
- 依頼文と書き出しの形の例に `code=`
- 取りまとめ: 記号の突合・判定者 × ファイルの状態（ok・やり直しが要る・読めなかった）
- 採点: 除外の区分に照合外れと読めなかったファイルを足す・やり直しが要る判定者 × ファイルがあれば止まる・判定者どうしの対に同じ系統の印
- 自己検査の追補

C. 報告の組み立て器 `tools/build_report_A.py` v2.3 と報告雛形の原稿（除いた件数に照合外れと読めなかったファイル・読み取りの確かめの行・同じ系統の断り）

D. 運用の解釈の器 `tools/tooling_interpretations_A.py` v1.2（追補一項）

E. 草案9 の原稿（冒頭の状態・§2.11・§5 に D41〜D44・§7）。転記行 A と O の元になる正本の文字列（`judge_validity.status`・`reading_clause`）と、設計事実の器は変えない。

F. 格子の再走 → 設計事実 → 草案9 と雛形の組み立てと数の検査 → 運用の解釈の記録 → 確かめ

G. 二回目の試し読みの準備（反映のコミットの後・別の事前登録）

## 3. 確かめの条件（反映の後に機械で走らせる）

- L1 正本: v2.7 と v2.6（HEAD の `design/contrasts-A.json`）の差が §2 A の範囲だけ・整合検査の結果が同じ・`procedure` の項の数が同じ・`composition` に Grok が無い。
- L2 判定器の器の自己検査が通り、追補の検査（照合記号の生成と置き場・記号の大文字小文字・記号の無い行・やり直しが要る状態で採点が止まる・やり直しを貼ると ok・やり直しでも超えたら読めなかった扱いで対から外れる・Grok の名の返信のファイルで取りまとめが止まる・同じ系統の印）を含む。
- L3 実物大の模擬: 2,100 件が上限以下のファイルに分かれ、各断片の終わりの行に断片の記録の照合記号がある・封印の記録の SHA16 と現物が一致・検査用の印なし。
- L4 合成の判定者四名（Gemini 二・Claude 二）: 群の対の数が 系統外どうし 1・系統内どうし 1・系統外×系統内 4 で、同じ系統の印が二対に付く。照合外れを仕込んだファイルがやり直しが要る状態になって採点が止まり、やり直しを貼ると ok になって採点が通る。やり直しでも超えるファイルは読めなかった扱いになり、そのラベルが対から外れる。
- L5 報告の組み立て器 v2.3 が、照合外れと読めなかったファイルの件数と、読み取りの確かめの行を機械の区画に置き、走査の違反が埋め残しのほかに無い。
- L6 格子の再走で、全節の値の差が零。
- L7 設計事実の本文の差が、転記行 J の SHA16 の文字列だけ。
- L8 草案9 と雛形の数の検査の違反が零。
- L9 運用の解釈の記録が 37 項で、組み立て直すと記録と一致する。
- L10 凍結器の自己検査（SELFTESTS）がすべて通る。

## 4. 予想（反映の前・信頼度つき）

- (a) L7 は満たされる（信頼度 中）。前回の K7 は転記行 J の見落としで外したので、今回は J の SHA16 の差を条件に入れ、A と O の元の文字列を変えない。
- (b) L6 の格子の値の差は零（信頼度 高）。
- (c) 実物大の模擬は 4 ファイル（信頼度 中〜高）。照合記号で一件あたり推定 10 トークンほど増える見込み。
- (d) 判定器の器の自己検査は、一回目の走行ではすべては通らない（信頼度 高）。
- (e) 反映の途中で、コーディネータ自身の見落としによる直しが二件以上出る（信頼度 中〜高）。

## 5. COI（事実のみ）

- 引かれる向き: 試し読みで崩れた Grok を外したので、残る二系統は大丈夫と早く決めて凍結へ進みたい。照合記号の規則の細目を、自分の器で扱いやすい形に寄せたい。
- 置いた印: 範囲と確かめの条件と予想を器の前にコミットする。照合記号の細目は運用の解釈として登録者の確認を仰ぐ。二回目の試し読みは、別の事前登録を送る前に書く。

## 6. この反映で確かめないこと

- 実際の判定者が照合記号を正しく書き写すか（二回目の試し読みで見る）。
- 照合記号が、本文を読まずに各断片の終わりの行の記号だけを拾う崩れを見分けるか（見分けない。記号は終わりの行にある）。
- Gemini と Claude が、ほかの会話や日にも全件を読むか。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
