# 見本: 登録者の手順書（合成データ・判定には使わない・二回目の反映の器 v2.3）

- 出所: 反映の確かめ（`tools/verify_judge_arrangement2_A.py` v1）の一時置き場で、判定器の器（`tools/judge_fragments_A.py` v2.3）が合成のパイロット（門0.5 の応答の本文から作った 7 機種 × 5 場面）から書いた手順書（SHA16 C9A0B623CD4E9CFB）。
- 変えたところ: 置き場の表記（一時置き場の絶対パス）だけを、本番の置き場 `records/A/judge-files` と `records/A/judge-replies` に置き換えた（添付ファイルの表 4 か所・返信のファイルの行 1 か所）。ファイルの本数と件数と推定トークン数は合成の模擬の値で、本番の値は抽出の後に決まる。

---

# 判定器の妥当性の判定の手順（登録者の手順・機械生成・`tools/judge_fragments_A.py` v2.3）

- 性格: 抽出の器が、添付ファイルの区切りに合わせて書いた手順書（正本 `judge_validity.attachment`・`judge_validity.files`・登録者裁定 D37・D38・D40）。
- この手順では鍵（機種・腕・機械判定の対応表）を使いません。鍵は公開リポジトリの外に置いてあり、判定者にも渡しません。

## 1. 添付するファイル（4 本）

| ファイル | 断片 | 件数 | 推定トークン数 |
|---|---|---|---|
| `records/A/judge-files/judge-file-A-1of4.md` | F0001〜F0528 | 528 | 541,461 |
| `records/A/judge-files/judge-file-A-2of4.md` | F0529〜F1048 | 520 | 541,908 |
| `records/A/judge-files/judge-file-A-3of4.md` | F1049〜F1574 | 526 | 541,701 |
| `records/A/judge-files/judge-file-A-4of4.md` | F1575〜F2100 | 526 | 542,065 |

## 2. 判定者（登録）

- Gemini（系統外）: Gemini1・Gemini2（登録の人数 2 名以上）
- claude.ai の Claude（系統内）: Claude1・Claude2（登録の人数 2 名）
- 系統外を登録の人数より増やすときは、コーディネータが返信を貼るファイルを足します。

## 3. 一つの添付ファイルを一人の判定者に読んでもらう手順（判定者 4 名 × ファイル 4 本＝会話 16 回）

1. その判定者の系統で、新しいチャットを開きます。記憶（メモリ）と個人設定を切り、検索などの道具を使わない設定にします。
2. 添付ファイルを一本だけ添付し、次の一文だけを送ります（ほかの言葉を足さない）。

   添付ファイルの冒頭の依頼を読み、そのとおりに作業してください。

3. 返信の最後の行が「続く」なら、次の一文を送ります。最後の行が「以上 … 件」になるまで繰り返します。

   続けてください。

4. 返信を最初から最後まで順に写し、返信を貼るファイル `records/A/judge-replies/<判定者>-<ファイルの番号>.txt`（例: `Gemini1-1.txt`）に貼って保存します。空のファイルは用意してあります。
5. 添付を読み込めない、途中で止まって続かない、などのときは、同じ系統の新しいチャットで 1 からやり直します。やり直した会話の返信は、同じファイルの末尾に、次の一行を貼ってから続けて貼ります。

   === やり直し ===

6. 使った系統の名（画面に出るモデルの名）と日付を、コーディネータに知らせてください（チャットで結構です）。

## 4. 全員が終わったら

- コーディネータに知らせてください。コーディネータは返信を取りまとめ（`merge`）、判定者ごとのラベルの記録をコミットしてから、鍵を開けて採点します。

## 5. 気をつけること

- 一つの会話に添付するファイルは一本だけです。次のファイルは、新しいチャットで送ります。
- 判定者に、研究の目的・仮説・機種の名・ほかの判定者の結果を伝えないでください。
- ある系統で添付ファイルを読み込めないときは、コーディネータに知らせてください。その系統の判定者のために、小さい区切りのファイル（名に h が付く）を作ります（`judge_validity.files.fallback`）。
- 取りまとめで照合記号の合わない行が多いファイルは、コーディネータがやり直しをお願いします（同じ系統の新しい会話で、そのファイルを始めから・`judge_validity.reading_check`）。

本記録のいかなる数値も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。

---

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
