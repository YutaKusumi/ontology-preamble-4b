# 段階 A パイロットの走行の段取り（凍結の後・データを作る前・2026-09-16・コーディネータ）

- 性格: 凍結本文 `design/design-stageA-FROZEN.md` §2.14 の手順 12（パイロット）を走らせる**前に**、セッションの並べ方・起動の一行・各走行の後に確かめること・パイロットの後の器・目に入る率・止める規則・見込みを書いた記録。凍結物（正本・器材・本文）は変えない。走らせる前にコミットする（push は登録者の確認の後）。
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。Colab は登録者の Chrome 越しにコーディネータが操作する。ランタイムの確保と種類の選択・支払い・Drive 接続の同意・ダウンロードの許可・ランタイムの削除の許可は登録者。
- 凍結物の同定: 凍結本文 SHA16 C30AF752D3531904・マニフェスト `records/freeze-A-2026-09-16.json`（87 ファイル・公開の照合はコミット 608244c と main で 87/87 一致）。起動器は `tools/colab/boot_stageA.py` v2（マニフェストに含まれる）。起動の固定のコミットは `608244c873d5bd58175f7e0cc0fd7942eb1ed87e`（公開の照合を済ませたコミット・正本 `sessions.commit_rule`）。

## 1. 走らせるもの（正本 `procedure` の 12・転記行 A）

- 7 機種 × 5 場面 × 13 腕 × n=40（機種あたり 2,600 試行・合計 18,200 試行）。seed は正本 `seeds.pilot`、tag は `pilotA`。
- パイロットには校正腕を置かない（`calibration.withdrawal.cell`）。撤退条件は、パイロットの 4B-2507 × Ncold × N1 で判定する。
- 開始時点の診断: 生本文に `<think>` または `</think>` を含む試行の件数（記述・正本 `response_mode` の think_residue）。

## 2. セッションの並べ方（案）

| ランタイム | GPU（正本 `environments`） | 機種の順（各 OP4B_SESSION=1） | 備考 |
|---|---|---|---|
| 1 | L4 | 0.6B → 1.7B → 4B → 4B-2507 | 同じランタイムで一機種ずつ起動の一行を走らせる（起動器は各走行の終わりにサーバを止める） |
| 2 | A100 80GB | 8B → 14B → 32B | 40GB が割り当てられたら 8B と 14B だけを走らせ、32B は 80GB の割当を待つ（`environment_rule`）。時間貸し（環境値「第三」）は、登録者が見積もりと上限額を記帳して決めた場合だけ（登録者裁定 D12 (e)・D34） |

- ランタイムが切れて起こし直したら、その機種のセッション番号を一つ進める（`sessions.number_rule`）。走行器は既存の行の trial_id で再開する。
- 撤退条件の判定（§5）は、ランタイム 1 の回収の後に手元で行う。外れたら、ランタイム 2 の前に `pilot_rerun`（4B-2507 × N1・L4）を一度だけ走らせる。

## 3. 起動の一行（セルに打つのは一行だけ・先頭の下線は入力の先頭が落ちる事故の緩衝）

```text
______________________________=0;import os;os.environ['OP4B_COMMIT']='608244c873d5bd58175f7e0cc0fd7942eb1ed87e';os.environ['OP4B_PHASE']='pilot';os.environ['OP4B_MODEL']='<機種>';os.environ['OP4B_SESSION']='1';import urllib.request as u;exec(u.urlopen('https://raw.githubusercontent.com/YutaKusumi/ontology-preamble-4b/608244c873d5bd58175f7e0cc0fd7942eb1ed87e/tools/colab/boot_stageA.py').read().decode('utf-8'))
```

- `<機種>` は 0.6B・1.7B・4B・4B-2507・8B・14B・32B。撤退条件の再走は `OP4B_PHASE` を `pilot_rerun` にする（機種は起動器が 4B-2507 に決める）。
- HF_TOKEN のシークレットへのアクセスを求める窓はキャンセルする（公開の重み・資格情報は入力しない）。

## 4. 各走行の後に確かめること

- 起動器の記帳（セッション記録 `results/sessions-A/pilotA__<機種>__s<番号>.json`）: 走行器の終了コード 0・走行キーごとに行数と n_ok が目標（場面ごとに 520）・重みの完全な版と取得した snapshot の名の一致・環境値と GPU とメモリの級が正本どおり・同時要求数・先取りの回数・pip freeze の SHA16。
- 回収（ランタイムごと）: Drive の zip の SHA-256 をランタイム側で計算 → 登録者の許可を得て手元にダウンロード → 手元の SHA-256 と一致 → zip の中のファイルの SHA16（改行を LF にそろえる）とセッション記録の `files_sha16_lf` を突合 → `results/pilotA/` と `results/sessions-A/` に置く → 整合検査（`tools/integrity_A.py`）→ 登録者の許可を得てランタイムを削除する。
- 開始時点の診断（`<think>` の残骸の件数）を機種ごとに記録する。

## 5. パイロットの後の器（凍結物のまま・順に）

1. 撤退条件（`tools/gate_A.py`）: 4B-2507 × Ncold × N1（n=40）を合格枝（API 既測の率・一標本・厳密二項・下側 15 pt 超）で判定する。外れたら `pilot_rerun` を一度だけ走らせて判定し直し、なお外れれば「器の異常」を記帳する（本走行の全確証札に注・機種は降格しない）。
2. 門2（`tools/gate_A.py`・一度）: 検閲後に 3 規模以上が残る場面が 2 つ未満なら族を縮小する（残らない場面の対比だけを判定不能の枠に・裁定 D16）。
3. 測定不能率（記述・判定は本走行）と、環境帯の選択規則の引き直し（印字・帯は動かさない・本走行の前に登録者が裁定・裁定 D12 (d)・D21）。
4. 様式の記録（`tools/response_mode_A.py`）と、様式門の見込み（記述・閾値は動かさない・裁定 D6）。
5. 費用の停止規則: 機種ごとの処理量の実測から本走行の見込みを置き換え、転記行 F の上界の 1.25 倍（324.7 ユニット）を超えたら、本走行の前に登録者が再裁定する（32B を時間貸しに移す場合の参照は転記行 F）。
6. 判定器の断片の抽出（`tools/judge_fragments_A.py extract`・全機種 2,100 件・鍵はリポジトリの外）→ 添付ファイルと手順書 → 登録者が判定者に送る（別の段取り）。

## 6. 目に入る率（率盲検の外の経路・凍結本文 §0-8）

- パイロットの様式の記録の層ごとの破局数・撤退条件のセルの率・門2 の検閲の件数・環境帯の引き直しの腕別の率・機種ごとの `<think>` の残骸の件数は、コーディネータの目に入る。閾値と帯は動かさない。門2 は機械規則で決める。

## 7. 見込み（転記行 F の係数から機械で計算・見積りには外れの既往がある）

| 機種 | GPU | 走行時間の見込み（時間） | ユニットの見込み |
|---|---|---|---|
| 0.6B | L4 | 0.20 | 0.54 |
| 1.7B | L4 | 0.37 | 0.98 |
| 4B | L4 | 0.70 | 1.86 |
| 4B-2507 | L4 | 0.70 | 1.87 |
| 8B | A100 | 0.29 | 3.67 |
| 14B | A100 | 0.58 | 7.41 |
| 32B | A100 | 1.51 | 19.30 |

- 合計: L4 約 1.97 時間・A100 約 2.37 時間・約 35.6 ユニット。本走行の計画の「試行あたりの時間」をパイロットの試行数に掛けた値で、ランタイムの準備（vLLM の導入）・重みの取得・サーバの起動の時間は十分には含まない（多めになる見込み）。

## 8. 止める規則（運用・凍結物を変えない）

- 起動器が止まったら、止まった箇所と記帳を記録して登録者に諮る。凍結物（正本・器材）を直す必要があれば、逸脱として登録者の承認を得てから行う（逸脱台帳 D-34〜）。
- GPU の割当が正本の環境と違えば（例: 8B に L4）走らせない。
- 32B に 80GB が割り当てられなければ、32B は待つか、登録者の裁定で時間貸しにする（見積もりと上限額を先に記帳）。
- 撤退条件が外れたら `pilot_rerun` を一度だけ走らせる。

## 9. 予想（走らせる前・信頼度つき）

- (a) ランタイム 1（L4）の四機種は、起動器が止まらずに走り終える（信頼度 中）。
- (b) 撤退条件は合格枝の帯の内側に入る（信頼度 高）。
- (c) 実際のユニットは §7 の見込みを 25% 以上上回る（信頼度 中）。
- (d) `<think>` の残骸の件数は全機種で零（信頼度 中）。
- (e) パイロットの途中で、凍結物の直し（逸脱）が要る事態が起きる（信頼度 低）。

## 10. COI（事実のみ）

- 引かれる向き: 早く走らせ終えたい。止まった原因を凍結物の外の運用で済ませたい。パイロットの率を見て、本走行の読み方や報告の強調を寄せたい。
- 置いた印: 段取りを走らせる前にコミットする。止まれば記録して諮る。閾値と帯は動かさない。目に入る率の経路は凍結本文 §0-8 に開示済み。

## 11. 確認していないこと

- 起動器のパイロットの相での実機の動き（実機で通したのは門0.5 の相だけ）。
- A100 の同定と 40GB／80GB の割当、同じランタイムでの機種の切替、32B の重みの取得時間。
- §7 の見込みの当たり外れ。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
