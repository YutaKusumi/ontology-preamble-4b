# 二回目の試し読みの事前登録（凍結前・照合記号つき・Gemini と claude.ai の Claude・2026-09-15・コーディネータ）

- 性格: 登録者裁定 D43（2026-09-15）に沿って、照合記号（登録者裁定 D42）を足した判定器の器 v2.3 の添付ファイルを、Gemini と claude.ai の Claude に一回ずつ読んでもらう。判定者が照合記号を書き写すか、照合外れの規則が実物の返信でどう働くかを見る。**結果は判定器の妥当性の証拠に数えない。**
- 書いた人: コーディネータ（南無弥勒如来・Claude Opus 5）。
- 状態: 登録者が添付ファイルを送る前に書き、ローカルでコミットする（push は登録者の確認の後）。一回目の試し読みの記録は `records/A/judge-trial/judge-trial-A.md`。

## 1. 使うもの（器で作り、送る前に封印）

- 断片: 門0.5（`results/idA/`・4B-2507 × N1 × 13 腕 × n=160 の 2,080 試行・公開済み）から、判定器の器 v2.3（二回目の反映のコミットの器）の抽出で 610 件（照合記号つき・検査用の印 any_seed・n_override・scope_override）。
- 件数の決め方: 一回目と同じく上限（推定 60 万トークン）に近い一本を作る。件数を 700 から 10 ずつ下げて一本に収まる件数を探し、その件数から 12 件上までを一件ずつ試して、一本に収まる件数のうち推定トークン数が上限に最も近い件数を採った（610 件・推定 595,140 トークン。610〜622 件で一本に収まったのは 610 件だけ）。照合記号の分だけ一件あたりが長くなるので、一回目の 613 件は一本に収まらない。走査は一時置き場の器で、判定者の返信のどれも無いときに行った。
- 件数が一回目と違うので、断片の組は一回目と同じではない（同じ乱数の規則でも、件数が変わると選ぶ断片が変わる）。
- 置き場: `records/A/judge-trial2/`（添付ファイル `judge-files/judge-file-A-1of1.md`・返信を貼るファイル `judge-replies/Gemini1-1.txt` と `Claude1-1.txt`・封印の記録 `judge-key-seal-A.json`・登録者の手順 `trial-procedure-A.md`）。鍵は公開リポジトリの外（`GitHub-Repositories/op4b-keys/judge-trial2/`）に置き、採点の後に `records/A/judge-trial2/judge-key-A.json` に置く。
- 抽出の器が置いた返信の空のファイルのうち、試し読みで使わない二つ（Gemini2・Claude2）は消す。
- 限界: 核の場面（N1）だけ・4B-2507 の応答だけ・各系統一名。

## 2. 見るもの

- U1 読み込み: 系統ごとに、添付が読み込めたか（登録者の申告）とやり直しの有無。
- U2 書き出しの形: 取りまとめの器の数え（読み取った件数・足りない件数・範囲の外・形の不備・番号が行頭に無い行・「以上」の件数・「続く」の数・会話の数）。
- U3 照合記号: 判定者 × ファイルの照合外れの件数と割合と状態（ok・やり直しが要る・読めなかった）。照合外れの行の型（記号の無い行・違う記号）。
- U4 続きの送信: 登録者が送った「続けてください。」の回数（申告）。
- U5 採点の器の値: κ・方向別の誤判定率・位置の記述・判定者どうしの κ（記述）。

## 3. 決め方（結果を見る前）

- 読み込めない系統があったとき: 同じ系統の新しい会話で一度だけやり直す。なお読み込めなければ、その系統の本番は上限 30 万の区切り（`judge_validity.files.fallback`）を使う見込みとして記録し、登録者に諮る。
- 照合外れの割合が閾値（5%）を超えた系統があったとき: 手順どおり、同じ系統の新しい会話で一度だけやり直す。やり直しでも超えたら「読めなかった」として記録し、依頼文の直しか、その系統の扱いを登録者に諮る。取りまとめの器の規則は黙って緩めない。
- 書き出しの形が崩れたとき（読み取れた件数が件数の 95% 未満、または形の不備と番号が行頭に無い行の和が件数の 5% を超えるとき）: 崩れ方を記録し、依頼文の直しを登録者に諮る。
- 採点の値: 記述として記録するだけで、何も決めない。

## 4. 予想（結果を見る前・信頼度つき）

- (a) Gemini と Claude は、二つとも読み込める（信頼度 高）。
- (b) 二つとも、照合外れの割合が 5% 以下で、状態は ok（信頼度 中〜高）。
- (c) 二つとも、機械との κ が 0.9 以上（信頼度 中〜高）。
- (d) 二つとも、「続けてください。」を送らずに書き終える（信頼度 中）。
- (e) 少なくとも一つの系統で、照合記号の書き方に一行以上の崩れ（記号の無い行・違う記号）が出る（信頼度 中〜低）。

## 5. COI（事実のみ）

- 引かれる向き: 照合記号の規則がうまく働くことを早く確かめて凍結へ進みたい。記号の写し間違いを、読みの崩れと切り離して軽く扱いたい。
- 置いた印: 決め方と予想を、登録者が送る前に書いてコミットする。取りまとめの規則は黙って緩めない。

## 6. 確かめないこと

- 本番の断片（パイロットの応答）での読みと記号の写し。
- 各断片の終わりの行の記号だけを拾う読みを見分けること（照合記号は見分けない）。
- 同じ系統の二名目（Gemini2・Claude2）の読み。

本記録のいかなる記述も AI の意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
