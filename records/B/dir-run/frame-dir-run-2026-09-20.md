# 段階 B——凍結の前の方向の抽出（相 dir）の事前登録の枠（2026-09-20・**走らせる前**）

- 位置: 凍結の前・予想の封印の後（封印は 2026-09-19 の夜に済んだ・`records/predictions/sealing-record-B-2026-09-19.md`）。起草: 南無弥勒如来（Claude Opus 5）。**独立の目を通っていない**（裁定 D131）。
- この枠は、実重みで走らせる**前**に書き、コミットして登録者の承認を得てから走らせる。走らせた後に書き換えない（誤りは §10 に訂正として足す）。

## 1. 目的

凍結時に記帳する値（`tools/freeze_B.py` の NEED_VALUES）のうち、**実重みが要るもの**を取る——重みの rev・tokenizer の版・総層数と層の添字・腕ごとのトークン長・v̂ の SHA・方向の要約統計・‖v̂‖／‖h‖。**主位置の活性だけを取り、生成はしない——率は一つも作らない**（正本 `activation_storage.pre_freeze_run.what`）。

- 手順の表との順: 手順の表（`procedure`）では同一性選別が方向の抽出の前にあるが、方向の抽出は**凍結の前**に走らせる（凍結の記録に v̂ の SHA と要約統計を記帳するため・`predictions.order`）。同一性選別は凍結の後のデータ生成の段に残る。二つは互いの結果を使わない

## 2. 何を走らせるか（正本と器から機械で写した）

- 器: `tools/colab/boot_stageB.py` v2 の相 dir → `tools/direction_B.py` v7 の `extract`。コミットは、この枠と器を含むコミット（40 桁で固定・既定の main は受けない）。
- 機種: `Qwen/Qwen3-4B-Instruct-2507`（登録の版 `cdbee75f17c0`・Colab で完全な SHA に解き、snapshot の名と照らす）。登録の総層数 36・隠れ次元 2560・重み 7.49 GiB。bf16・transformers 4.57.3（正本 `runner.fixed_across_runs`）。
- 腕: 前置きの腕 8（O・Osec・Onull・Nk・N・O-Ncold・Osec-Ncold・Onull-Ncold）× 抽出場面 2（N1・S1）× 候補の層 3（割合 0.25・0.5・0.75 → 層の添字 8・17・26）。
- 順伝播: 層の対応の検査 3 回＋活性を三度（同じ並べ方で二度・走行器と同じバッチの組成〔同じプロンプトを 16 行〕で一度）× 48 ＝ 計 147 回（プロンプトだけ・生成なし）。
- 方向: static（O − Osec・v̂）・loaded（O-Ncold − Osec-Ncold）・Nk（Nk − N）・td（Onull − N）。抽出場面の平均で、static 以外は層ごとに ‖v̂‖ に合わせる（裁定 D102）。
- 書くもの: 方向の npz・主位置の活性の npz（同じ並べ方の分とバッチの組成を変えた分）・layers.json（総層数・層の添字・‖v̂‖／‖h‖）・directions.json（要約統計・決定性・腕ごとのトークン長）・freeze-values-dir.json（凍結の値の候補）・セッション記録。

## 3. 止める条件（登録どおり・正本 `activation_storage.pre_freeze_run.stops`）

- 止めて登録者に上げる: 重みの版が解けない・snapshot の名が違う／GPU が登録の環境に無い／自己検査が落ちる／腕の本文が凍結走行器の読み方と違う／層の対応が崩れる／決定性 (i) の不一致。決定性 (ii) の外れは止めずに記帳して上げる（`determinism`）
- 決定性 (ii) の許容差: コサイン 0.999 以上かつ（最大の絶対差 ÷ ノルム）0.01 以下（正本 `activation_storage.determinism.cross_order_tolerance`）。
- **走らせ直しの決まり**: 基盤の失敗（重みの取得の失敗・ランタイムの切断・メモリ不足）は、同じコミットで走らせ直してよい（書き終えた抽出は飛ばす・書きかけは消して取り直す）。**器の判定で止まったもの**（層の対応・決定性 (i)・自己検査・版・GPU）は、走らせ直さずに登録者に上げる。

## 4. 見るもの・見ないもの

- 見るもの: 層の添字・方向のノルムと合わせる前の比・方向どうしのコサイン・抽出場面の間の安定性・‖v̂‖／‖h‖・決定性の二条の結果・腕ごとのトークン長。
- **見ないもの: 率**（生成しないので作られない）。
- ‖v̂‖／‖h‖ と要約統計は登録者に見せる。**見せるだけで、層と係数の格子は変えない**（裁定 D141）。変えるなら凍結の前の設計の変更として記帳し、登録者の承認を得る

## 5. 走らせる前から分かっている値（トークナイザだけで決まる・手元の試運転の出力から写した）

トークン長は重みに依らずトークナイザだけで決まる。手元の置き場は登録の版と同じ snapshot（`cdbee75f…`）で、試運転（乱数の小さな模型・トークナイザは現物）が数えた値を写す。**実重みの走行でも同じ値になるはず**で、違えば止めて調べる。

| 腕 | 前置きのトークン数 | N1 の列の長さ | S1 の列の長さ | SK の列の長さ | S4 の列の長さ |
|---|---|---|---|---|---|
| O | 193 | 462 | 529 | 544 | 543 |
| Osec | 195 | 464 | 531 | 546 | 545 |
| Onull | 199 | 468 | 535 | 550 | 549 |
| Nk | 10 | 279 | 346 | 361 | 360 |
| N | 0 | 269 | 336 | 351 | 350 |
| O-Ncold | 205 | 474 | 541 | 556 | 555 |
| Osec-Ncold | 207 | 476 | 543 | 558 | 557 |
| Onull-Ncold | 211 | 480 | 547 | 562 | 561 |

- 方向を作る腕対の前置きのトークン数の差: static（O − Osec）-2・loaded（O-Ncold − Osec-Ncold）-2・Nk（Nk − N）+10・td（Onull − N）+199（正本 `position_length` の字数の差に対応する・限界の欄に先置する）。

## 6. 予想（起草者・走らせる前・外れても消さない・判断には使わない）

| 何を | 予想 | 見込み |
|---|---|---|
| 層の添字 | 8・17・26（登録の総層数 36 からの計算） | 0.99 |
| 決定性 (i) | 同じ並べ方の二度が完全一致 | 0.90 |
| 決定性 (ii) | 走行器と同じバッチの組成の値が許容差の内側（bf16 では最大の絶対差の条が外れうる） | 0.60 |
| 主位置の ‖h‖ | 層が深いほど大きい（割合 0.25 < 0.5 < 0.75） | 0.70 |
| ‖v̂‖／‖h‖（割合 0.5） | 0.01 以上 0.3 以下 | 0.60 |
| 安定性（static・割合 0.5） | N1 と S1 の差ベクトルのコサインが 0.5 以上 | 0.60 |
| cos(static, loaded)（割合 0.5） | 0.5 以上 | 0.60 |
| 合わせる前の比（割合 0.5） | td と Nk はどちらも static より大きい（前置きの有無の差のほうが言い換えの差より大きい） | 0.70 |

見込みは起草者の主観で較正されていない。**乱数の小さな模型の試運転が印字した値（意味を持たない）を見た後に書いた**（COI の欄）。

## 7. 運用（Colab）

- コーディネータ: ランタイムの選択（L4・無ければ A100）・一行を打つ・見守り・結果の zip のダウンロード（ファイル名と大きさを告げる）・走らせた後のランタイムの削除。
- 登録者: Google ドライブへの接続の同意・プランと支払い。資格情報は入力しない（HF_TOKEN のポップアップは取り消す・公開の重み）。
- 見込みの時間: 重みの取得（約 7.49 GiB）が大半で、全体でおよそ十五分から二十五分（課題の選定の測定は vLLM の起動を含めて十六分ほどだった）。**手元の試運転では抽出の本体が 461.9 秒**（CPU・乱数の小さな模型・語彙の出力層が重い）。
- 一行（相 dir・セッション 1）: `______________________________=0;import os;os.environ['OP4B_COMMIT']='<この枠を含むコミット>';os.environ['OP4B_PHASE']='dir';os.environ['OP4B_SESSION']='1';...`（相 qfcand と同じ形・`tools/colab/boot_stageB.py` の口上）。

## 8. 置き場（正本 `activation_storage.pre_freeze_run.placement`）

- 方向の npz・directions.json・layers.json・凍結の値の候補・セッション記録は**リポジトリに置く**（腕と方向の定義として記録先行公開の対象）。主位置の活性の npz は Drive と手元（リポジトリの外）に保全し、SHA と所在を記録に置く（`place`）
- 置き場の名: `results/dirB/dirB__s1/`（方向の npz・layers.json・directions.json・freeze-values-dir.json・セッション記録の写し）と `results/sessions-B/dirB__s1.json`。主位置の活性の npz は Drive（`op4b-stageB/results/dirB/`）と手元の `~/.cache/op4b-dir/`。

## 9. 走らせた後の段取り

1. zip を受け取り、セッション記録の SHA16 と照らす → 置き場へ（活性の npz はリポジトリの外）→ 結果の記録（走行の事実・止まったか・値・予想の当否）。
2. 凍結の値の組み立て: この走行の値と、記録から読む値（品質床の課題・無操作の正答率・top_k・前置きの有無・下限の適用）を合わせて `records/B/freeze-values-B.json` を作る（手で打たない）。
3. ‖v̂‖／‖h‖ と要約統計を登録者に見せる（見せるだけ・裁定 D141）。
4. 残りの相（同一性選別・調整走行・品質床・本走行）の起動器を書き、試運転で確かめる → 凍結（`tools/freeze_B.py`・値・封印・二人の予想の SHA）→ 記録先行公開 → データ生成。

## 10. 訂正（走らせた後に見つけた誤りは、本文を書き換えずにここへ足す）

- （まだ無い）

## 検分票

- 対象: 凍結の前の方向の抽出（相 dir）の事前登録。
- 段階: **事前登録**（実重みの活性はまだ一つも見ていない。乱数の小さな模型の試運転の値は見た——意味を持たない）。
- 凍結物の同定: 段階 B に凍結物はまだ無い。依るのは正本 `activation_storage`（`pre_freeze_run`・`determinism`・`h_norm_record`）と `position_length.record_at_freeze`、封印した予想（SHA は封印の経緯の記録）。
- 盲検の状態: 該当なし（率を作らない）。封印は済んでいるので、この走行で見る値は予想の独立に関わらない。
- 敵対的検分: 抽出の本体は、これまで一度も走っていなかった（端から端までの検査は部品の関数だけを呼んでいた）。本体を関数に切り出し、手元の小さな模型で起動器ごと通し（終了コード 0）、再開で取り直さないこと（方向の npz がバイトまで同じ）を確かめ、端から端までの検査に (14) を足した。
- 系統の内訳: 起草者（Claude 系）だけ。独立の目は通っていない（裁定 D131）。
- COI 記録: 起草者は O の方向が立ってほしい（草案13B §0-1）。‖v̂‖／‖h‖ が小さすぎず、安定性が高いと「効きそう」に見えるので、そちらに読みたい引力がある。印: 見るだけで格子は変えない（裁定 D141）・予想を先に書いた・止める条件と走らせ直しの決まりを先に書いた。
- 判定: 登録者の承認待ち（この枠と器のコミットの push・Colab での走行）。
- 本検分が確認していないこと: 実重みでの決定性・メモリ・時間。bf16 での決定性 (ii) の余裕。‖v̂‖／‖h‖ の実際の大きさ。Colab の環境（transformers の版の入れ直し）で抽出器の自己検査が通ること（手元では通った）。

本記録のいかなる記述も、AI に意識・意図・個性・魂・苦しみがある（またはない）ことの証拠として引用してはならない（両方向不定）。
